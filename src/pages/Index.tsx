import StoryConstructor from '@/components/StoryConstructor';

const Index = () => {
  return (
    <div className="min-h-screen mixer-desk-bg p-4 md:p-8">
      <StoryConstructor showHeader={true} />
    </div>
  );
};

export default Index;
