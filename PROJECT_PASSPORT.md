# Fairyteller Project Passport

Last updated: 2026-08-26

## Project Context

Fairyteller is a public personalized storybook generator at `fairyteller.ru`.

The current public app is a Vite/React static site. The active generation path sends `/create` form data to n8n through a same-origin nginx webhook proxy, then tracks progress through the persistent Fairyteller Job API. The migration target is to complete the remaining text, visual, and HTML/CSS-to-PDF render pieces inside this n8n-backed pipeline.

## Current Production

- Print checkout offers two server-priced products: soft cover (3,500 ₽) and 20×20 hard cover (6,000 ₽). The public payment form submits only `softcover` or `hardcover_20x20`; the Job API resolves the amount, records the choice in YooKassa metadata/payment state, and includes it in fulfilment notifications. Hard-cover 20×20 PDF assembly remains the existing editor-side action after editorial approval.

- VPS: `82.26.198.127`
- Hostname: `tr-vmv2-nano`
- SSH: `root@82.26.198.127` with local key `~/.ssh/baku_tr_ed25519`; password authentication is disabled in production, root login is key-only.
- Public site root: `/var/www/fairyteller/current`
- Releases root: `/var/www/fairyteller/releases`
- Current static site release: `/var/www/fairyteller/releases/20260820-dialogue-constructor-v5-codex`
- Nginx site: `/etc/nginx/sites-available/fairyteller`
- Domain: `https://fairyteller.ru`
- Node on VPS: `v22.22.2`
- npm on VPS: `10.9.7`

## Local Source

- Local repo: `/Users/nikita0shch/Documents/Cursor/Codex/Make/Lovable migration/fairy-tailer`
- GitHub remote: `https://github.com/aukenbro1-crypto/fairy-tailer.git`
- Branch: `main`

## Local Fairyteller Lab

- Setup: `npm run lab:setup`; start/stop: `npm run lab:start` and `Ctrl+C`; health: `npm run lab:status`.
- Constructor: `http://127.0.0.1:8081/test-create`; local n8n: `http://127.0.0.1:5679`; local Job API: `http://127.0.0.1:3098`.
- Setup exports the current active production workflows and current static site release read-only over SSH, then prepares isolated localhost copies. The pinned local n8n version is `2.14.2`.
- Runtime, database, generated files, secrets, and the site snapshot live under gitignored `.lab/`; n8n itself lives under gitignored `lab/node_modules/`.
- All URLs inside prepared workflows point at the local Job API, intake job IDs use the `ft_lab_` prefix, and the browser server blocks third-party analytics/network requests. Email, Telegram polling, and payment credentials are not loaded.
- The OpenLux key is read at process start from `~/.config/openlux/api_key`; it is not copied to git or workflow JSON. Lab jobs older than 24 hours are deleted at startup.
- Production changes remain a separate explicit export/import/publish/verification operation. A successful local run never deploys a workflow or frontend bundle.

## Website Submit Flow

Current production default is the n8n intake webhook through `https://fairyteller.ru/webhook/fairyteller/create`.

The `/create` form is now compatible with both response shapes:

- legacy Make response: any successful HTTP response marks the request as accepted and keeps the email-based UX
- n8n response: JSON with `jobId` and optional `statusUrl` starts public status polling from the generation overlay
- limit response: `429` JSON with `code=daily_limit_exceeded`, `booksUrl`, `payUrl`, reset timing, and support contacts shows the "3 сказки в сутки" customer state without starting generation
- current n8n UX: no intermediate first-chapter reader. The overlay shows a staged full-book progress/timer flow while generation is running, then switches to a clean in-modal `preview.pdf` reader with a bottom print-payment CTA when render is ready.

Frontend migration environment variables:

- `VITE_FAIRYTELLER_CREATE_URL`: target create endpoint; production build uses `/webhook/fairyteller/create`
- `VITE_FAIRYTELLER_STATUS_BASE_URL`: public status API base; defaults to `/api/fairyteller/jobs`

## n8n Migration Workflows

The new n8n pipeline is intentionally split into modular workflows:

| Workflow | ID | Purpose |
| --- | --- | --- |
| `fairyteller_intake` | `Vty7vSn4vd5Nduht` | Receive order, create `jobId`, start text pipeline, return fast `202`. |
| `fairyteller_text` | `kCtpw2d7pEOI3QRF` | Story bible, chapter 1 text, then starts first visuals and full-text generation automatically. |
| `fairyteller_visuals` | `RXzoJ7Bdlr2y3l60` | Portraitizer, image generation, retries/cache. |
| `fairyteller_continue` | `Y7Bt3zq9XTContinue` | Legacy/manual continuation webhook kept for old jobs and recovery, no longer part of the default UX. |
| `fairyteller_full_text` | `C2Pcin7lctSY5nc2` | Background continuation generator for chapters 2-5 and `full-text.json`. |
| `fairyteller_full_visuals` | `FVFullVisuals20260523` | Background generator for chapter 2-5 illustrations using the existing visual bible and per-hero reference cards. |
| `fairyteller_cover` | `FVCover20260523` | Background cover-art generator using full text, visual bible, and per-hero reference cards. |
| `fairyteller_render_publish` | `XAaFdi6hJjnQFiAQ` | Print PDF render, preflight, publish/email. |

Current state as of 2026-08-25: the internal text, full-text, visuals, cover, and PDF render paths are published and connected to the Job API. A visible owner-operated Code node named `РУЧНОЕ РЕЛЕ — GEMINI / OPENAI / OPENLUX / GROK` in `fairyteller_intake` snapshots independent `textProvider` and `imageProvider` choices into every new order. The current production defaults are `textProvider=openlux` and `imageProvider=grok`. Text may use `gemini`, `openai`, or `openlux`; images may additionally use `grok`. There is no percentage split, A/B routing, or automatic cross-provider fallback. The text workflow writes `text.json`, then starts `fairyteller_visuals` and `fairyteller_full_text` asynchronously from the same payload. The website no longer waits for a user continuation click; it shows staged full-book progress until `book.pdf` is ready. The visuals workflows generate per-hero reference cards, optional combined hero reference sheet, chapter illustrations, cover art, and `visuals.json`; the PDF render step persists only `book.pdf` plus `render.json`. The permanent `/book/:jobId` preview is a versioned JPEG cache derived directly from `book.pdf` and remains visible after payment.

Activation state:

- `fairyteller_intake`: active public production intake.
- `fairyteller_text`: active internal sub-workflow.
- `fairyteller_visuals`: active internal sub-workflow.
- `fairyteller_continue`: active public recovery/legacy continuation webhook.
- `fairyteller_full_text`: active internal sub-workflow.
- `fairyteller_full_visuals`: active internal sub-workflow.
- `fairyteller_cover`: active internal sub-workflow.
- PDF render: active host-side renderer invoked by the Job API after cover generation.
- `fairyteller_render_publish`: legacy placeholder workflow, superseded for current PDF smoke by the Job API renderer.

Current placeholder status flow:

1. `received`
2. `text_generating`
3. `chapter_1_ready`
4. `visuals_generating`
5. `visuals_ready`
6. `rendering`
7. `done`

Text generation note:

- `fairyteller_text`, `fairyteller_full_text`, and the print-fit correction step read the persisted `order.textProvider`. Direct Gemini and OpenLux both use `gemini-2.5-pro`; OpenAI uses `gpt-5.6-terra` through the Responses API in JSON mode. OpenLux keeps the Gemini-compatible `generateContent` contract but sends it through the OpenLux base URL and Bearer credential.
- Requests use `$env.GEMINI_API_KEY`, `$env.OPENAI_API_KEY`, or `$env.OPENLUX_API_KEY`; never store literal key values in workflow JSON or git.
- The first-chapter provider call is wrapped in a code node with controlled transient retries/backoff (`10s`, `20s`, `35s`, `55s`). If the manually selected provider still returns errors, the workflow patches the Job API to `failed` before throwing so the frontend and Telegram do not hang silently. It does not silently switch providers.
- The first-chapter normalizer extracts fenced/embedded JSON and repairs raw control characters inside strings before `JSON.parse`.
- The first-chapter normalizer also patches the Job API to `failed` before throwing if Gemini returns unrecoverable JSON or an invalid print-ready block shape.
- The first-chapter and full-text normalizers remove generated quotation marks from `textBlocks`, avoid direct-speech examples wrapped in guillemets in prompts, and repair Russian dash-dialogue typography. They keep author attributions inline (`— Реплика! — сказала Женя.`), split narrative actions into separate paragraphs (`— Реплика.\n\nЖеня кивнула.`), and only insert paragraph breaks before true new dialogue turns. Recalled phrases, thoughts, messages, and labels should be paraphrased without quote marks rather than treated as dialogue.
- OpenAI was enabled on the production VPS on 2026-08-07. Key validation, a `gpt-5.6-terra` structured-text smoke, and an image-generation smoke with a base64 image reference all returned `200`.
- The first-chapter contract writes `text.preview.imageStatus = "pending"` so `fairyteller_visuals` can prioritize the first chapter illustration next.
- `fairyteller_text` starts the full book automatically after chapter 1 is normalized. It fans out to `fairyteller_visuals` for the portrait sheet/chapter 1 image and to `fairyteller_full_text` for chapters 2-5. `fairyteller_continue` remains available only for old jobs or manual recovery.

Gemini integration note:

- n8n Docker env includes `GEMINI_API_KEY` and `GOOGLE_API_KEY`.
- Do not store literal Gemini keys in workflow JSON or git.
- Gemini API smoke on 2026-05-23 returned `200` from `GET /v1beta/models` and listed available models.
- Previous n8n container backup before Gemini env injection: `baku-n8n-docker-bak-20260523054556`.
- Deployed `identity_first_edit_v2` for hero reference generation on 2026-08-07. Only the per-hero reference-card and optional combined-reference-sheet stage now routes to GA `gemini-3.1-flash-image`; chapter 1 and chapters 2-5 remain on `gemini-2.5-flash-image`, while the later cover migration is recorded in the next entry. The reference prompt makes observable identity preservation the absolute priority, removes story role and final-book style from photo-based identity cards, standardizes output to `1:1` at `1K`, and uses `strict_identity_retry_with_source_photo` when the provider returns no image. The original deployment also added a free-text keyword classifier for `human` / `animal` / `fantasy_creature`; that classifier was removed on 2026-08-08 after job `ft_1786124513750_otbw5k` proved it could match `кот` inside the unrelated word `некоторое` and explicitly route a photographed adult woman through the animal prompt. Reference generation now has no programmatic species inference from name, description, or relation: ordinary heroes default semantically to human, while a clearly photographed or described animal is interpreted directly by the image model from the complete photo and description. The image provider relay and `gemini-3.1-flash-image` reference model remain unchanged; new cards record `referenceMode=identity_first_edit_v3`. Before/import/after snapshots: `/root/fairyteller-n8n-exports/20260808-animal-classifier-before`, `/root/fairyteller-n8n-imports/20260808-no-free-text-animal-classifier`, and `/root/fairyteller-n8n-exports/20260808-no-free-text-animal-classifier-after`. Verified all ten Code-node syntaxes, absence of classifier/animal-keyword routing, active published workflow parity, n8n health, public Job API, and site `200`.
- Deployed Gemini 3.1 cover generation on 2026-08-07. `fairyteller_cover` now pins every Gemini cover attempt—primary referenced request, referenced safety retry, and final no-reference fallback—to GA `gemini-3.1-flash-image` with `imageConfig={aspectRatio:"3:2",imageSize:"1K"}`; all chapter images remain on `gemini-2.5-flash-image`. Cover metadata records the pinned model and requested image config, while the existing per-hero card attachment, reference mapping, dimension parser, and accepted `1.3-1.85` landscape-ratio guard remain unchanged. One paid isolated two-reference cover smoke returned `200`, `STOP`, and `1264x848`, kept both reference characters separate, and did not modify the source job or generate a full book. Production backup: `/root/fairyteller-n8n-exports/20260807-111637Z-cover-gemini31-before`; import: `/root/fairyteller-n8n-imports/20260807-111633Z-cover-gemini31`; after-export: `/root/fairyteller-n8n-exports/20260807-111633Z-cover-gemini31-after`. Verified Code-node syntax, exactly three cover requests using the shared 3.1 config, successful `npm run build`, active/current production version parity, n8n health, public Job API, and site `200`.

OpenLux integration note:

- Production n8n reads `OPENLUX_API_KEY` from its Docker environment; the literal key is not present in workflows or git. The recoverable pre-key container is `baku-n8n-docker-pre-openlux-20260825`.
- `textProvider=openlux` routes book text for chapter 1, chapters 2-5, short-block correction, and print-fit regeneration through `https://api.openlux.ai/v1beta/models/{model}:generateContent` with `gemini-2.5-pro`, matching the direct Gemini route. The synchronous pre-generation Story Brief is a latency-sensitive exception: it uses `gemini-2.5-flash` with `thinkingBudget=0` so the browser receives the two-paragraph brief well inside its 30-second connection window.
- `imageProvider=openlux` mirrors the direct Gemini stage routing: the private visual identity map uses `gemini-2.5-flash`; chapter 1 and chapters 2-5 use `gemini-2.5-flash-image`; per-hero reference cards and cover art use `gemini-3.1-flash-image`. Existing `1:1 / 1K` chapter/reference and `3:2 / 1K` cover contracts are unchanged.
- OpenLux is a third-party proxy. When this provider is selected, story prompts and any attached customer reference photos transit OpenLux rather than only Google; do not describe this route as direct Google processing.
- OpenLux returns the same `candidates[].content.parts[].inlineData` shape consumed by the existing n8n normalizers. Isolated contract smokes verified structured JSON from `gemini-3.1-pro-preview`, a 17-field multimodal identity map, and a two-reference `gemini-3-pro-image` inline JPEG at `1264×848`.
- No automatic fallback crosses providers. Job artifacts record `provider=openlux` and the selected model so failures and costs remain attributable.

Grok image integration note:

- `imageProvider=grok` uses OpenLux only as the transport and pins every generated image to `grok-imagine-image-2.0`; text generation stays on the independently selected text provider. Private identity analysis remains a non-image `gemini-2.5-flash` request through OpenLux.
- Per-hero internal reference cards request explicit `resolution=1k`, `quality=low`, and `aspect_ratio=1:1`. Printed chapter illustrations request `2k / low / 1:1`; cover art requests `2k / low / 3:2`. The adapter calls `/v1/images/edits` when generated hero cards or source photos are attached and `/v1/images/generations` otherwise, then downloads the returned xAI-hosted image and converts it into the existing Gemini-compatible inline-data shape for downstream normalizers.
- The Grok adapter forwards the complete stage prompt without a provider-specific character cap. A manual `1k / low` OpenLux test sent 1,527 characters with the decisive instruction beginning at character 1,286; the request returned HTTP `200` and the image followed that late instruction. The previously assumed 1,000-character ceiling belonged to an OpenLux page for a different image adapter and does not apply to this route.
- xAI accepts at most three source images per edit. Continuation chapters already select the reference cards named by `chapter.heroNumbers`; chapter 1 selects names present in its `visualBrief`. The generic adapter enforces the same three-image ceiling and records how many excess references were omitted from a request instead of sending an invalid payload.
- The mode never silently falls back to Gemini or OpenAI image generation. `visuals.json` records `provider=grok` and `model=grok-imagine-image-2.0`; the prior `gemini`, `openai`, and `openlux` branches remain available by changing the single image relay constant.
- Reference photos sent in this mode transit both OpenLux and xAI. This is not a direct xAI enterprise integration and must not be represented as one; customer consent/privacy copy must cover the third-party image-processing route before it is treated as a permanent default.

OpenAI integration note:

- Production n8n reads `OPENAI_API_KEY` from its Docker environment; the literal key is not present in workflows or git.
- Text model: `gpt-5.6-terra` through `POST /v1/responses` with JSON output mode.
- Image generation uses the Responses API image-generation tool. Per-hero cards are sent as base64 `input_image` references; chapter workflows attach only the heroes selected for that scene when `heroNumbers` is available.
- Square chapter/reference assets request `1024x1024`; cover art requests `1536x1024`. OpenAI chapter and reference assets use the owner-selected economy setting `quality=medium`, while cover art stays at `quality=high`. The reference smoke used `quality=low` to validate the contract without an unnecessary full-price image.
- Production workflow backup before relay deployment: `/root/fairyteller-codex-backups/20260807-provider-relay`.
- Published production exports after key installation and smoke tests: `/root/fairyteller-n8n-exports/20260807-openai-relay-after`.
- Economy image-quality deployment on 2026-08-07: production `fairyteller_visuals` and `fairyteller_full_visuals` now request OpenAI `quality=medium`; `fairyteller_cover` remains `quality=high`. Before/import/after snapshots: `/root/fairyteller-n8n-exports/20260807-102103Z-image-quality-before`, `/root/fairyteller-n8n-imports/20260807-102103Z-image-quality-economy`, and `/root/fairyteller-n8n-exports/20260807-102103Z-image-quality-after`. Verified all three workflows published and active, local workflow behavior aligned with the published exports, n8n/API health, and the public site response. No paid synthetic generation was run.
- The owner restored both relay constants to `gemini` on 2026-08-07 after OpenAI returned `429 insufficient_quota / credit_balance_exhausted` for job `ft_1786098428696_lyxkyz`. Production import: `/root/fairyteller-n8n-imports/20260807-gemini-relay-restore`. Verified the published `fairyteller_intake` row is active with both Gemini constants, plus n8n/API health and public site `200`. The OpenAI implementation and environment key remain installed but inactive for new orders.
- Recoverable pre-key container retained after recreation: `baku-n8n-docker-pre-openai-20260807`.

Visual generation note:

- `fairyteller_visuals` prioritizes `chapter_1` before cover and later chapter images.
- `fairyteller_intake` detects uploaded hero photo binary fields and stores per-hero metadata: `photoField`, `photoMime`, `photoFileName`, and `hasPhoto`.
- The `/create` form sends an explicit `heroN_age_group` for each filled hero. The customer-facing choices are `child`, `teen`, and `adult`; this explicit value is the source of truth for character proportions.
- `fairyteller_intake` still has legacy fallback inference from free-form name/description/relation text. Current internal values are `child`, `teen`, `adult`, `senior`, or `unknown`, with `ageGroupSource` recorded as `explicit`, `age_number`, `keyword`, or `not_detected`. Keyword inference checks adult/senior terms before child terms to avoid misclassifying descriptions like "мама Макса ... мальчик".
- `fairyteller_visuals` now uses `per_hero_reference_cards_v1`: original uploaded hero photos are used only inside the portraitizer to generate separate `hero-1-reference.*`, `hero-2-reference.*`, etc. Each card is tied to exactly one hero and carries name, description, relation, and `ageGroup`.
- The combined `hero-reference-sheet.png` is optional overview/fallback material, not the primary identity source. If Gemini cannot generate one individual card from a source photo after retries, the card is marked `source_photo_fallback`; the original private photo is then used only to build the combined sheet inside the same workflow and is not persisted in public artifacts.
- Chapter 1, chapter 2-5, and cover image generation attach the ready per-hero reference cards inline to the manually selected image provider as the primary identity canon. If some individual cards are missing, later image workflows attach both the ready individual cards and the combined sheet so secondary heroes still have a visual reference instead of dropping back to text-only identity.
- Portraitizer, chapter, and cover prompts treat hero age group as mandatory visual canon: children, teenagers, adults, and seniors must keep distinct proportions, maturity, and relative height/scale instead of being averaged into the main hero's apparent age.
- `fairyteller_visuals` persists `visuals.visualBible` as the reusable visual canon for the book: selected style prompt, world visual direction, hero descriptions, per-hero reference card URLs/statuses, optional portrait sheet URL/status, and consistency rules. Future cover and chapter 2-5 image jobs must reuse this `visualBible` and the same per-hero reference cards instead of regenerating character identity from scratch.
- Chapter illustration prompts must target the actual interior image page: `136 x 136 mm`, square `1:1`, full-bleed, roughly `1606 x 1606 px` at 300 DPI. Key faces, hands, silhouettes, and the important object must stay away from the left 15% gutter/spine area and outer 3 mm trim edge.
- Image requests preserve the existing book-art targets across every provider: `1:1` for chapter 1 and chapter 2-5 illustrations, and `3:2` for cover art because it is the closest supported ratio to the front-cover frame's `about 1.46:1` target.
- Chapter illustration prompts must explicitly forbid rendered typography: no chapter numbers, titles, captions, signs, labels, fake letters, posters, banners, UI, white margins, blank paper backgrounds, or decorative borders. If a scene contains papers/books/signs, they must be abstract unreadable texture only; the PDF renderer owns all visible text.
- Cover-art prompts target the front-cover image frame in the PPTX-derived cover template, not the whole wraparound spread: about `1.46:1` landscape artwork placed into the white front-cover frame. The final PDF page remains `268.5 x 136 mm`. The cover workflow parses generated PNG/JPEG dimensions and rejects non-landscape outputs outside the safe ratio band before the renderer can contain-fit a vertical portrait with white side fields.
- First chapter image generation uses `gemini-2.5-flash-image` through either direct Google `generateContent` when `imageProvider=gemini` or the OpenLux Gemini-compatible route when `imageProvider=openlux`; `imageProvider=openai` uses the OpenAI Responses image-generation tool; `imageProvider=grok` uses OpenLux `/v1/images/edits` with `grok-imagine-image-2.0`.
- The `minibrick` style is explicitly mapped to brick-built minifigure/toy diorama prompts; do not rely on fallback style mapping for it.
- The generated file is stored through the Job API under `/api/fairyteller/jobs/:jobId/files/chapter-1.png`.
- The public status preview is updated with `imageStatus: "ready"`, `imageUrl`, and `imageAbsoluteUrl`.
- Cover and chapter 2-5 images are generated by the background visual workflows after the full text is ready.

## Job API

The job API is the source of truth for order status between the website, n8n, and the render service.

Local implementation:

- `server/fairyteller-api.mjs`
- `docs/fairyteller-job-api.md`
- `.env.api.example`

Planned production service:

- systemd service: `fairyteller-api.service`
- listen address: `127.0.0.1:3099`
- production data dir: `/data/fairyteller`
- public proxy: `https://fairyteller.ru/api/fairyteller/`
- environment file: `/etc/fairyteller/api.env`
- service source file: `/opt/fairyteller-api/fairyteller-api.mjs`
- PDF renderer source file: `/opt/fairyteller-render/fairyteller-render-pdf.mjs`
- nginx backup before API proxy change: `/root/nginx-codex-backups/fairyteller.bak_codex_20260523050814`
- nginx also proxies `/webhook/` to Docker n8n on `127.0.0.1:5680`; backup before webhook proxy change: `/root/nginx-codex-backups/fairyteller.bak_codex_20260523061350_webhook`

Core endpoints:

- `GET /healthz`
- `POST /api/fairyteller/chat/messages`
- `GET /api/fairyteller/chat/sessions/:sessionId/messages`
- `POST /api/fairyteller/telegram/webhook`
- `POST /api/fairyteller/jobs`
- `GET /api/fairyteller/jobs/:jobId`
- `GET /api/fairyteller/jobs/:jobId/full`
- `GET /api/fairyteller/my-books/:token`
- `PATCH /api/fairyteller/jobs/:jobId`
- `PUT /api/fairyteller/jobs/:jobId/artifacts/:fileName.json`
- `PUT /api/fairyteller/jobs/:jobId/files/:fileName`
- `GET /api/fairyteller/jobs/:jobId/files/:fileName`
- `GET /api/fairyteller/jobs/:jobId/files/:fileName?base64=1` returns authenticated JSON/base64 for internal workflow reuse of generated files.
- `POST /api/fairyteller/jobs/:jobId/render-pdf` starts the authenticated PDF render step and writes the single persistent standard artifact `book.pdf` plus `render.json`.

Generated job files are mutable operational artifacts, not static assets. Admin rebuilds atomically replace `book.pdf`, so file responses use `Cache-Control: no-store`; admin/status PDF links include a `v=<file updatedAt>` query value. Web-preview pages live under a source-version directory derived from the current `book.pdf`, use immutable cache headers, and are regenerated automatically after a rebuild.

The admin book editor uses one multipart form for text and image edits. Its save-and-rebuild action persists the submitted text plus any selected replacement images, then queues the PDF rebuild in the API process and redirects immediately with `renderQueued=1`; the background render updates the normal job status and versioned PDF links when it finishes.

The editor-facing book description is `text.bible.coverSummary`. Older generated books may also have `text.bible.readerBlurb`; the renderer must prefer `coverSummary`, and editor saves sync `readerBlurb` to the same value so stale generation text cannot override manual edits.

The last story text page has a decorative footer below the text block. Its text layout must reserve vertical space above that footer instead of using the normal full-height chapter-final text box, otherwise longer edited endings can overlap the underline and page number.

The two hard-cover 20×20 outputs use a separate internal render source. Their interior paper is selected through the shared page-paper setting; it applies to both the regular adult 20×20 render and the 12 pt adult render, while full-page illustrations remain artwork-only.

Cover typography now uses a 30 pt source title, which becomes approximately 42 pt on the finished 20×20 front panel. The cover annotation is set in DejaVu Serif and justified on every output; its adaptive range remains 7.6–10.3 pt in the finished 20×20 cover, depending on copy length.

The admin book editor stores the story text size mode in `text.printLayout.storyFontMode`. The default `uniform` mode deterministically reflows the unchanged text inside each chapter across the declared `[4, 4, 6, 6, 5]` text-page map, then selects one size for the entire book from `10.5`, `10.25`, or `10` pt. Legacy per-page auto sizing, balanced sizing, and manual maximum-size modes remain available for existing/operator-selected jobs. The renderer records the requested/applied size and pagination diagnostics in `render.preflight.storyFont`.

Mutating/internal endpoints require:

```txt
Authorization: Bearer <FAIRYTELLER_API_TOKEN>
```

Never commit the production token.

## Website Support Chat

The public site includes a lightweight support chat widget mounted globally from `src/components/FairytellerChat.tsx`.

Message flow:

1. Visitor messages are posted to `POST /api/fairyteller/chat/messages`.
2. The Job API stores chat sessions under `${FAIRYTELLER_DATA_DIR}/chat/sessions`.
3. Each visitor message is sent to the configured Telegram admin chat.
4. The admin replies in Telegram by replying directly to the bot message, or by sending `/reply <sessionId> <text>`.
5. Telegram calls `POST /api/fairyteller/telegram/webhook`; the API appends the operator reply to the matching session.
6. The widget polls `GET /api/fairyteller/chat/sessions/:sessionId/messages` while open and shows new replies.

If the latest visitor message remains unanswered for the configured delay, the API appends one automatic operator-style fallback reply to the chat session. Production default is two seconds and asks the visitor to leave WhatsApp or Telegram for a follow-up.

If Telegram cannot register a public webhook for the domain, set `FAIRYTELLER_CHAT_TELEGRAM_POLLING=1` on the API service. In that mode the same server reads bot updates with Telegram `getUpdates` and uses the same reply handling logic.

Required production env for two-way chat:

- `FAIRYTELLER_CHAT_TELEGRAM_BOT_TOKEN`
- `FAIRYTELLER_CHAT_TELEGRAM_CHAT_ID`
- `FAIRYTELLER_CHAT_TELEGRAM_WEBHOOK_SECRET`
- optional fallback: `FAIRYTELLER_CHAT_TELEGRAM_POLLING=1`
- optional auto-reply tuning: `FAIRYTELLER_CHAT_AUTO_REPLY_DELAY_MS` and `FAIRYTELLER_CHAT_AUTO_REPLY_TEXT`

Generation progress and failure alerts use a separate Telegram bot identity:

- `FAIRYTELLER_ALERT_TELEGRAM_BOT_TOKEN`
- `FAIRYTELLER_ALERT_TELEGRAM_CHAT_ID`

Service-level health alerts use the same alert bot identity. Production runs `fairyteller-service-watchdog.timer` every five minutes; it executes `/opt/fairyteller-monitor/fairyteller-service-watchdog.mjs`, sourced from `ops/fairyteller-service-watchdog.mjs` in this repo, and dedupes state under `/data/fairyteller/monitor/`. The watchdog checks root disk usage, suspicious SSH activity, Job API `/healthz`, n8n `/healthz`, the `baku-n8n-docker` container state, recent n8n webhook start errors, recent failed jobs, and fresh stuck jobs. It was installed and verified on 2026-07-08 after the full-disk ENOSPC incident; a TaleBoy test alert was sent successfully, and a normal run reported `activeIssues:0`.

Production also runs `fairyteller-cleanup-unpaid-pdfs.timer` daily. It executes `/opt/fairyteller-monitor/fairyteller-cleanup-unpaid-pdfs.mjs`, sourced from `ops/fairyteller-cleanup-unpaid-pdfs.mjs`, and deletes `book.pdf` plus any legacy PDF artifacts for unpaid jobs older than `FAIRYTELLER_UNPAID_PDF_RETENTION_DAYS` days. The production default is 30 days. Paid jobs and non-PDF job data are not deleted by this timer.

Successful payment alerts use `FAIRYTELLER_PAYMENT_TELEGRAM_BOT_TOKEN` and `FAIRYTELLER_PAYMENT_TELEGRAM_CHAT_ID`, falling back to the site chat bot (`@fairysender_bot` in production) when dedicated payment variables are absent. The alert includes job/payment IDs, amount, customer email, phone, recipient name, delivery address, customer-email delivery status, and the public book URL.

Legacy `FAIRYTELLER_TELEGRAM_*` variables remain as fallback only when the split role-specific variables are absent. Production should keep the site support chat on the support bot and generation alerts on the operations bot.

The webhook should be registered with Telegram using `secret_token` and URL `https://fairyteller.ru/api/fairyteller/telegram/webhook`. Do not store bot tokens or webhook secrets in git.

## Print Target

Final output target from print shop upload requirements:

- PDF version: `1.7` (accepted range shown by print shop: `1.3-1.7`)
- Fonts: embedded in PDF
- Color space: RGB
- PDF protection: none
- Total page count, including cover: 41
- Page 1 (cover/back-cover spread): `268.5 x 136.0 mm`
- Pages 2-41: `136.0 x 136.0 mm`
- Current PDF UX uses the combined print PDF (`book.pdf`) as the only persistent standard PDF artifact. `book.pdf` contains mixed page sizes: page 1 is the cover/back-cover spread, pages 2-41 are square interior pages. The web preview derives 42 lightweight JPEG pages directly from it by splitting the first spread into front and back covers around the 40 interior pages.
- Versioned web-preview page caches are rebuildable and are not canonical book artifacts. The permanent `/book/:jobId` URL shows the same preview before and after payment; payment only controls prompts and protected PDF download access.

Reference production PDF:

- Local reference file: `/Users/nikita0shch/Downloads/Изнанка старого Дуба-2 (1).pdf`
- Observed page objects: 41
- Page 1 MediaBox: `761 x 385 pt` (cover/back-cover spread; matches about `268.5 x 136.0 mm`)
- Pages 2-41 MediaBox: `385 x 385 pt` (square pages; matches about `136.0 x 136.0 mm`)
- No explicit `TrimBox`, `BleedBox`, or `CropBox` entries were found in the PDF byte structure during the quick metadata pass.

Preferred render architecture:

- Current active renderer: Node.js `pdf-lib` generator with embedded fonts, exact page sizes, Job API file outputs, extracted PPTX template assets from `MasterTemplate_book.pptx` and `MasterTemplate_cover_romantic.pptx`, and a versioned layout contract at `server/render-layouts/fairyteller-pptx-v2.json`.
- The renderer composes the final one-file `book.pdf` by joining two logical templates: page 1 is the wider cover/back-cover spread, and pages 2-41 are square interior pages. A single source PPTX cannot safely hold mixed slide sizes, so the source-of-truth templates remain separate while the print artifact is combined.
- Dynamic content should replace template placeholders only: cover title, cover subtitle/summary, cover art, front matter, chapter title pages, full-page chapter illustrations, the declared chapter text-page map, and the final outro/QR pages. Do not add extra colored rectangles or duplicate `by FairyTeller` outside the PPTX page plan.
- Pinned template fonts currently embedded for repeatability: `Rubik Mono One`, `Inter Light`, `Inter SemiBold`, and `Amatic SC`; the cover blurb uses the renderer's serif fallback in the Georgia-like slot unless a licensed Georgia-compatible font is installed.
- The PDF renderer must remain a deterministic layout/preflight layer. It must not rewrite or silently trim story content. In the default `uniform` mode it may change page-break positions inside a chapter while verifying that the normalized source text is byte-for-byte equivalent after whitespace collapse. It keeps the declared text-page count, applies one book-wide size from `10.5`, `10.25`, or `10` pt, and fails with the overflowing chapter when the text does not fit even at the 10 pt floor.
- The current print rhythm uses 40 interior pages with physical chapter opener spreads. Interior page 1 is title/year, page 2 is subtitle plus story summary, page 3 is table of contents, then chapters open on pages `4, 10, 16, 24, 32` with a left title/teaser page and a right full-page illustration. Text-page counts by chapter are `[4, 4, 6, 6, 5]`, page 39 is a decorative ending, and page 40 is the QR/coauthor page.
- The n8n text chain should preserve the Make-era author/editor split: the author creates the story, then an editor/prepress step reshapes it into `printReadyChapters` with the current block map `[4, 4, 6, 6, 5]` and text sized for the fixed PDF template.
- Future renderer option after layout polish: HTML/CSS template with Playwright/Chromium PDF export, pinned browser/runtime version, and preflight checks for page count, page size, image resolution, text overflow. Keep the current PPTX-extracted renderer as the stable baseline until the HTML path proves better.

Google Slides/Drive should be phased out because OAuth reauthorization has been unstable every 2-3 weeks.

## Operational Rules

- Treat production as the current source of truth before changing live behavior.
- Keep Make fallback code available until the n8n render/publish path is feature-complete and stable.
- Do not commit secrets, generated books, customer photos, or runtime job data.
- Prefer small deployable pieces:
  1. job API
  2. n8n intake -> job API
  3. website submit -> intake/job status
  4. text generation
  5. visuals
  6. render service

## Change Log

### 2026-08-27

- Deployed the generation-speed v2 PDF/image path without changing the active story-text model. New OpenLux Grok requests in `fairyteller_visuals`, `fairyteller_full_visuals`, and `fairyteller_cover` explicitly request `format: 'jpeg'`; the renderer also accepts existing PNG artifacts and converts them to JPEG with a 2048 px cap and high-quality Lanczos/MJPEG settings before embedding. The renderer now lazy-loads only the selected cover, paper, ornament, outro, and contents assets; embeds shared fonts/assets once; adds cover and interior pages to one `PDFDocument`; and saves one final PDF instead of producing and merging intermediate cover/interior files. Production backup: `/root/fairyteller-codex-backups/20260827T-generation-speed-v2-predeploy`; deployed renderer SHA-256: `871ca67af5c3dca8e2b4eba708811f863807eee12e4149175673ab82e80fd8c3`. Three isolated production-job copies rendered unchanged 41-page structures in `22.315`, `19.137`, and `15.907` seconds versus the prior `46`, `45`, and `39` second baselines, a 51-59% renderer speed-up. Local visual comparisons retained exact template/text pages and image-page SSIM of `0.9957-0.9976`; all ordinary and 20x20/12 pt preflight checks passed. After deployment all three visual workflows were active, n8n and the Job API were healthy, and public `/create` plus `/test-create` returned `200`.
- Prepared, but did not activate on production, a switch of both final story-text stages from OpenLux `gemini-2.5-pro` to `gemini-3.1-flash-preview`. The model appeared in OpenLux `/v1/models`, but repeated native Gemini and OpenAI-compatible smoke calls returned HTTP `429` with `Current group upstream load is saturated` and `code=model_not_found`; deploying that route knowingly would have made new books fail before text generation. The production-aligned workflow exports therefore remain on `gemini-2.5-pro`; `ops/fairyteller-generation-speed-v2.mjs --activate-flash-preview` retains the staged switch for activation after a successful structured-JSON smoke.

### 2026-08-26

- Deployed the optimized Grok/OpenLux production generation path across `fairyteller_intake`, `fairyteller_text`, `fairyteller_full_text`, `fairyteller_visuals`, `fairyteller_full_visuals`, and `fairyteller_cover`. Final story text remains on OpenLux `gemini-2.5-pro`; all book artwork is pinned to OpenLux `grok-imagine-image-2.0`. The visual chain now freezes a shared outfit canon from the later chapter-art direction, carries explicit physical/spatial scene briefs, starts eligible image work earlier, and renders chapter illustrations with concurrency `2`. The optional visual-controller regeneration remains disabled unless explicitly enabled by environment. No Wes Anderson/Lab-only style override or `ft_lab` routing is present in production. Pre-deploy backup: `/root/fairyteller-codex-backups/20260826T140455Z-optimized-v1-predeploy`; authoritative after-export: `/root/fairyteller-n8n-exports/20260826-optimized-v1-after`. Verified active workflows, n8n and Job API health, and public site availability after deployment.
- Tightened the generated-dialogue contract and PDF pagination. `fairyteller_text` and `fairyteller_full_text` now require each dialogue paragraph to contain one complete utterance followed by a terminal speaker attribution and forbid the split form `— Реплика, — сказал герой. — Продолжение`; both normalizers also deterministically rewrite that malformed continuation when a model still emits it. The PDF renderer treats dialogue paragraphs as atomic pagination segments, while narrative text may still split normally. Production workflow versions are `8d2bbe53-da4d-4ac1-8d2d-3c5ef3437d5b` for `fairyteller_text` and `6fb5b747-b2c9-4b98-a636-1951f1b7e2e3` for `fairyteller_full_text`. Pre-deploy backup: `/root/fairyteller-codex-backups/20260826T143804Z-dialogue-pagination-predeploy`; after-export: `/root/fairyteller-n8n-exports/20260826-dialogue-pagination-after`; deployed renderer SHA-256: `07e53bef8357e6c64ae0f4053377266709c812129a344886c6e7e409f768c73c`.
- Verified the dialogue/pagination renderer change with three isolated copies of existing jobs, without model calls and without mutating the originals: `ft_1787724889256_j3t7yo` rendered 41 pages in 46 seconds, `ft_1787709863097_xvofp7` rendered 41 pages in 45 seconds, and `ft_1787709691815_e0jpby` rendered 41 pages in 39 seconds. The explicitly excluded customer job `ft_1787650483741_txqn18` was never copied, rendered, or modified; its before/after manifest remained `40ca3baf75dd50a5488611d2be79eee3378ba968e3bec9fba4afa810c834d72b`.
- Restored `https://fairyteller.ru/test-create` inside the active frontend release `/var/www/fairyteller/releases/20260825-book-only-preview-v2-codex` after that release omitted `test-create/index.html`. The restored entry uses the active release bundle, has the canonical `/test-create`, the correct title/description, and `noindex,nofollow`; curl and browser smoke both returned the full constructor with HTTP `200`. This is an active-release hotfix rather than a complete packaging repair: the next frontend build/deploy must explicitly retain or regenerate `/test-create/index.html` so the route is not dropped again.

### 2026-08-25

- Fixed the production form-intake failure introduced when the manual image relay was renamed to include `/ GROK`. Story Brief execution `3350` completed successfully in 4.2 seconds, but the following form submission failed in intake execution `3351` after 5 ms with `Cannot read properties of undefined (reading 'name')`: the relay node itself and downstream code references had the new name, while the connection from `Normalize Order and Create Job` still targeted the former `/ OPENLUX` name. The connection and the Grok migration tool now rename both connection keys and edge targets. Production before/import/after paths: `/root/fairyteller-n8n-exports/20260825-grok-intake-edge-before`, `/root/n8n-docker-data/imports/20260825-grok-intake-edge-fix`, and `/root/fairyteller-n8n-exports/20260825-grok-intake-edge-after`. Verified zero missing graph endpoints, all three intake Code nodes in async syntax context, local/live code parity, active/current published version parity, registered production webhook, n8n and Job API health, and public site `200`. The failed submission created no job and made no paid model request; no synthetic full-form POST was sent after the repair.
- Removed an incorrect 995-character compression step from the production Grok image adapter. A paid isolated `grok-imagine-image-2.0` test sent a 1,527-character prompt whose decisive red-elephant instruction began at character 1,286; OpenLux returned HTTP `200`, raw usage `0.04`, and an image that obeyed that post-1,000 instruction. Hero cards, chapters, and cover art now receive their complete existing stage prompts. The independently verified xAI ceiling of three reference images remains enforced.
- Added and activated the fourth Fairyteller image route, `imageProvider=grok`, and changed only the production image relay default to Grok while keeping `textProvider=openlux`. Hero cards, chapter 1, chapters 2-5, cover art, and every referenced retry now call OpenLux's xAI-compatible image endpoints with pinned `grok-imagine-image-2.0`; private visual identity analysis remains on OpenLux `gemini-2.5-flash`. Internal hero cards use explicit `1k / low`; all printed chapter and cover artwork uses explicit `2k / low`. Chapter scene references are reduced to the named heroes and the adapter enforces xAI's three-reference limit. Two isolated contract calls reached `imgen.x.ai`: `1k low + 1 reference` returned `1024x1024` with raw usage `0.05`, and `2k low + 2 references` returned raw usage `0.08`; at the observed OpenLux group ratio these checks cost about `$0.057` together. No full synthetic book was generated. Production before/import/after paths: `/root/fairyteller-n8n-exports/20260825-grok-live-before`, `/root/n8n-docker-data/imports/20260825-grok-image-provider`, and `/root/fairyteller-n8n-exports/20260825-grok-image-provider-after`. Verified all 16 affected Code nodes in async syntax context, live/local workflow structure parity, current/published version parity, all four workflows active after restart, n8n and Job API health, and public site `200`. Rollback is one relay constant (`IMAGE_PROVIDER='openlux'`) or the saved before exports.
- Added a prompt-only `NON-IDEALIZED IDENTITY LOCK` to the per-hero reference-card builder in `fairyteller_visuals`. Photo-based references now explicitly treat high or receding hairlines, temple recession, uneven hair density, facial asymmetry, wrinkles, under-eye structure, skin texture, and age markers as identity-defining features rather than defects; the model is forbidden to lower/fill the hairline, thicken hair, enlarge eyes, smooth skin, remove wrinkles, normalize facial proportions, or replace the subject with a generic/style-template attractive face. Provider routing, stage models, `style_native_identity_map_v2`, `1:1 / 1K` reference size, retries, and fallback behavior are unchanged. Production before/import/after paths: `/root/n8n-docker-data/exports/20260825-non-idealized-identity-lock-before`, `/root/n8n-docker-data/imports/20260825-non-idealized-identity-lock`, and `/root/n8n-docker-data/exports/20260825-non-idealized-identity-lock-after`. Verified that only `Restore Hero Reference Payload` code changed, all ten Code nodes compile in async context, the published/current workflow versions match and are active, n8n/API health is OK, and the public site returns `200`. No paid image or full-book generation was run. Local production snapshots: `n8n/production-exports/20260825-non-idealized-identity-lock/`.
- Repaired failed OpenLux job `ft_1787632412529_jy345r` and made terminal generation errors visible on the public constructor. n8n execution `3326` failed in `Restore Hero Reference Payload` because `identityModel` was declared only inside `analyzeHeroIdentity` and then referenced later while recording reference-card metadata; `fairyteller_visuals` now selects that model once in workflow scope. Production backup/import: `/root/fairyteller-n8n-exports/20260825-identity-model-scope-before` and `/root/fairyteller-n8n-imports/20260825-identity-model-scope-fix`. The `/` and `/create` constructor now stops its synthetic timer when the Job API reports `failed`, shows `Генерация остановлена`, a customer-safe explanation, the job ID and `Вернуться к форме`, and preserves the completed form instead of leaving the user at `Финальная проверка`. Deployed frontend release: `/var/www/fairyteller/releases/20260825-generation-failure-ui-codex`. The original visuals and full-visuals inputs, including both private photo references, were recovered from stored executions `3326` and `3328` through a one-job wrapper; that wrapper was unpublished and archived after use. The full-visual workflow was made MCP-readable for this controlled recovery, with backup `/root/fairyteller-n8n-exports/20260825-full-visuals-mcp-before`. The same job reached `done / complete / 100`, with two ready hero cards, five non-fallback OpenLux chapter images on `gemini-2.5-flash-image`, a non-fallback cover on `gemini-3.1-flash-image`, a 41-page print book, a 42-page preview, and `error=null`. Verified Code-node syntax, TypeScript `tsc --noEmit`, a clean temporary `npm ci` production build, local and production browser loads without console errors, deployed bundle strings, live `/`, `/create`, asset responses, active/published workflow state, and final Job API/artifact metadata.
- Repaired the production intake topology after the OpenLux model-parity import exposed two stale references to the former relay name `РУЧНОЕ РЕЛЕ — GEMINI / OPENAI`. A real form submission reached nginx as HTTP `500`; n8n execution `3323` failed in the webhook itself after 1 ms with `Cannot read properties of undefined (reading 'name')`, before parsing the form, creating a job, or calling a paid model. `Normalize Order and Create Job` now connects to `РУЧНОЕ РЕЛЕ — GEMINI / OPENAI / OPENLUX`, and `Attach Job API Result` reads the same renamed node. Before snapshot: `/root/fairyteller-n8n-exports/20260825-intake-relay-topology-before`; import: `/root/fairyteller-n8n-imports/20260825-intake-relay-topology-fix`. Verified Code-node syntax, zero missing connection endpoints, local/current/published workflow parity, active intake registration, n8n health, and public site `200`. No synthetic create POST was sent because that would start a paid full-book generation; the owner can safely retry the original submission.
- Fixed Story Brief requests closing with nginx `499` while successful OpenLux executions continued in the background. Executions `3318` and `3319` showed `gemini-2.5-pro` spending 2,913 and 4,713 thinking tokens and taking 30.7 and 43.4 seconds; the browser closed both connections at 30 seconds. OpenLux did not honor the attempted Pro `thinkingBudget=128` cap, so the synchronous pre-generation step now uses `gemini-2.5-flash` with `thinkingBudget=0`; final book text remains on `gemini-2.5-pro`. Production smoke execution `3321` returned HTTP `200` in 6.84 seconds, used the requested Flash model, produced the complete three-field JSON, and reported 517 prompt plus 605 candidate tokens with no thinking tokens. Before snapshot: `/root/fairyteller-n8n-exports/20260825-story-brief-thinking-budget-before`; final import: `/root/fairyteller-n8n-imports/20260825-story-brief-flash-no-thinking`.
- Replaced the initial all-Pro OpenLux experiment with the same stage-specific model map used by direct Gemini so the owner can compare like-for-like book cost. OpenLux now uses `gemini-2.5-pro` for all final book text, `gemini-2.5-flash` for Story Brief and private identity analysis, `gemini-2.5-flash-image` for chapter illustrations, and `gemini-3.1-flash-image` only for per-hero reference cards and cover art. The intake relay persists separate `openluxImageModel`, `openluxHeroReferenceModel`, `openluxCoverImageModel`, and `openluxIdentityModel` fields so artifact metadata remains attributable by stage. Before snapshot: `/root/fairyteller-n8n-exports/20260825-openlux-gemini-model-parity-before`; normalized import: `/root/fairyteller-n8n-imports/20260825-openlux-gemini-model-parity-normalized`; after-export: `/root/fairyteller-n8n-exports/20260825-openlux-gemini-model-parity-after`. Verified 29 Code-node syntaxes across seven workflows, current/published/local parity, all seven workflows active, n8n health, and public site `200`. No paid full-book generation was run after this model switch.
- Added and activated the third Fairyteller provider route `openlux`. The manual intake relay now accepts independent `textProvider` and `imageProvider` values of `gemini`, `openai`, or `openlux`, and both production defaults are set to `openlux`. Story Brief, first/full text, short-block correction, print-fit regeneration, identity-map analysis, hero cards, chapter 1, chapters 2-5, cover generation, and every referenced retry now select the OpenLux base URL and Bearer credential without changing the existing Gemini/OpenAI rollback branches. Text and identity analysis use `gemini-3.1-pro-preview`; all generated images use `gemini-3-pro-image`. Fixed provider/model metadata so `text.json`, `full-text.json`, `visuals.json`, and job status record the actual OpenLux route. Fresh production sources matched local workflow files before mutation. Before snapshot: `/root/fairyteller-n8n-exports/20260825-openlux-live-before`; import: `/root/fairyteller-n8n-imports/20260825-openlux-provider`; after-export: `/root/fairyteller-n8n-exports/20260825-openlux-provider-after`; rollback container: `baku-n8n-docker-pre-openlux-20260825`. Verified all Code-node syntax, OpenLux structured JSON, multimodal identity JSON, two-reference inline image output, active/published workflow parity, n8n/API/site health, and full synthetic job `ft_1787619783903_xtxgfv`. The job completed all five chapters, hero reference, cover, 42-page preview PDF, and 41-page print book; every generated image records `provider=openlux`, `model=gemini-3-pro-image`, while text records `provider=openlux`, `model=gemini-3.1-pro-preview`.

### 2026-08-24

- Stopped new book generation when an uploaded hero photo cannot produce its required reference card after the existing referenced retries. `fairyteller_visuals` now marks the job `failed`, records `error.code=REFERENCE_MODERATION_FAILED`, persists the failed reference-card summary without private base64/prompt data, and exposes the customer message `Ошибка модерации. Пожалуйста, приложите другой референс`. Because full text starts in parallel, `fairyteller_full_text` checks the persisted hero-reference status before launching chapters 2-5 illustrations; a failed reference therefore cannot continue into later illustrations, cover generation, or PDF rendering. Production before snapshots use `/root/fairyteller-n8n-exports/fairyteller_visuals_moderation_20260824112352.json` and `/root/fairyteller-n8n-exports/fairyteller_full_text_moderation_20260824112352.json`; import: `/root/fairyteller-n8n-imports/20260824-reference-moderation-stop`; after-export: `/root/fairyteller-n8n-exports/20260824-reference-moderation-stop-after`. Verified both workflows active with exported node parity, all Code-node syntax, n8n/API health, and public `/` and `/create` responses. No paid synthetic generation was run.

### 2026-08-20

- Added the isolated noindex dialogue-constructor experiment at `/test-create`. It collects up to four heroes, optional browser-held photos, world, location, the free-form instruction «Есть ли что-то, что хотелось бы учесть в будущей истории?», and illustration style; it then generates an editable brief before asking for email or starting the main book job. Rewrites use the latest generated brief, and a manually edited brief is rechecked for an optional object canon before final submission. The current UI uses the production `/create` typography stack (`Avenir Next`, `Helvetica Neue`, Jost, Futura, Arial), a white page/dialogue background, warm light-gold visitor answers, and no box shadows. The role is «Помощник fairyteller.ru» with the header copy «Ответьте на несколько вопросов, чтобы создать книгу». The dialogue opens empty, then stages «Привет!», the short explanation, and the first hero question over 1.7 seconds. Hero-description guidance expands for the second and later characters, world and illustration choices use responsive looping carousels, and the Hogwarts-facing label is «Мир Хогвартса». The location hint is shown as `✨ Подсказать место` and cycles deterministically through six editable suggestions per selected world before repeating. Visible skip actions and all «Это необязательно» prompts remain removed; entered text and the editable story draft use the same legible `/create` font stack. The live-summary sidebar remains removed, and the active progress segment uses the existing brief-thinking shimmer. The existing `/create` release files are unchanged. Current frontend release: `/var/www/fairyteller/releases/20260820-dialogue-constructor-v5-codex`; rollback releases: `/var/www/fairyteller/releases/20260820-dialogue-constructor-v4-codex`, `/var/www/fairyteller/releases/20260820-dialogue-constructor-v3-codex`, `/var/www/fairyteller/releases/20260820-dialogue-constructor-v2-codex`, and `/var/www/fairyteller/releases/20260820-dialogue-constructor-codex`.
- Published the approved-brief/object-canon contract across `fairyteller_story_brief`, intake, text, full-text, first-visual, full-visual, and cover workflows. The brief workflow alone may identify `artifactCanon`; it returns an empty value when the approved brief has no plot-important recurring physical object. Downstream generation receives the approved brief plus that optional canon and no longer invents or forces a compass/object when canon is empty. Before/import/after snapshots: `/root/fairyteller-n8n-exports/20260820-approved-brief-artifact-canon-before`, `/root/fairyteller-n8n-imports/20260820-approved-brief-artifact-canon`, and `/root/fairyteller-n8n-exports/20260820-approved-brief-artifact-canon-after`. Verified active/published export parity, Code-node syntax, n8n/API health, an empty-canon brief smoke, an explicit-object brief smoke, the public test route, desktop/mobile layout, and unchanged production files outside `/test-create`. No full paid story generation was run.
- Deployed the Gemini likeness/style-native reference upgrade across `fairyteller_visuals`, `fairyteller_full_visuals`, and `fairyteller_cover`. Every photo-based hero now gets a private observable identity map from `gemini-2.5-flash` before `gemini-3.1-flash-image` creates the individual reference card in the selected book style; the map explicitly captures facial geometry, natural left-right asymmetry, hair/facial hair, age cues, skin details, accessories and clothing without identifying the person or inferring sensitive attributes. Reference cards record `referenceMode=style_native_identity_map_v2`; identity outranks style in every chapter and cover prompt, and the old `STYLE has highest priority` wording is gone. A combined generated sheet is skipped when all individual cards are ready. When ready cards exist, chapter/cover generation no longer silently drops them or reuses an unrelated earlier image after referenced attempts fail; the visual stage fails explicitly instead. Size contracts are unchanged and verified: hero cards `1:1 / 1K` (`1024×1024` in the isolated smoke), chapters `1:1` for the `136×136 mm` image page (`1024×1024` from the active Gemini chapter model), and cover `3:2 / 1K` (`1264×848` from the active Gemini 3.1 cover model). The exact universal production-prompt smoke retained the source eye/brow asymmetry, hairline, beard, hoop earring, earbud and clothing materially better than the previous reference card. No customer job was mutated and no full paid book was regenerated. Initial before/import/after snapshots: `/root/fairyteller-n8n-exports/20260820-082033Z-likeness-style-native-before`, `/root/fairyteller-n8n-imports/20260820-082033Z-likeness-style-native-v1`, and `/root/fairyteller-n8n-exports/20260820-082033Z-likeness-style-native-after`; identity-map refinement: `/root/n8n-docker-data/exports/20260820-identity-map-v2-before`, `/root/n8n-docker-data/imports/20260820-identity-map-v2`, and `/root/n8n-docker-data/exports/20260820-identity-map-v2-after`. Verified all affected Code-node syntaxes, active/current published version parity, n8n/API health, workflow activation after restart, and public site `200`. Local production snapshot: `n8n/production-exports/20260820-likeness-style-native-identity-map-v2/`.

### 2026-08-13

- Added public print-format choice for Fairyteller: soft cover is 3,500 ₽ and the existing 20×20 hard-cover production format is 6,000 ₽. The existing `/book/:jobId` CTA remains the single soft-cover payment action; `/pay` offers an opt-in checkbox for the hard-cover gift format. The API accepts only `softcover` or `hardcover_20x20`, creates the YooKassa amount and return URL server-side, persists the product in payment metadata/state, includes it in customer/operations notifications, and records the correct revenue/product in Metrika. After editorial approval, fulfilment of hard-cover orders uses the existing protected `Сохранить и собрать твёрдую 20×20` action. Deployed frontend release: `/var/www/fairyteller/releases/20260813-hardcover-gift-checkbox-codex`; API rollback copy: `/opt/fairyteller-api/backups/20260813-hardcover-checkout/fairyteller-api.mjs.before`. Verified API syntax, service health, production build, and matching deployed payment/book bundle hashes. The full-repository linter still has pre-existing errors in unrelated `tmp/` and UI files.
- Added a compact «Подарочный формат» panel below the real-book examples carousel on the main page. It introduces the 20×20 hard-cover option at 6,000 ₽ in the moment where visitors are already looking at physical books, while keeping the primary action focused on creating a book. Frontend release: `/var/www/fairyteller/releases/20260813-home-hardcover-format-codex`; verified the active release bundle and API health.

### 2026-08-05

- Added and deployed the server-authoritative `firstGeneration` flag and the `ft_first_generation_unique` Yandex Metrika goal. The Job API decides uniqueness from all existing jobs for the normalized email while holding the existing per-email create lock, persists the decision in job status, and exposes it in both the authenticated create response and public status. The n8n intake response forwards the flag. All public constructors fire the new goal only when the server returns `true`, with job-id browser deduplication and a polling fallback; the existing `ft_generate_submit` remains an analytics-only event for every generation. Yandex Direct should optimize only for `ft_first_generation_unique`. Frontend release: `/var/www/fairyteller/releases/20260805-first-generation-unique-codex`; API backup: `/opt/fairyteller-api/backups/20260805-153606Z-first-generation-unique/fairyteller-api.mjs.before`; n8n backup/import/after-export: `/root/fairyteller-n8n-exports/20260805-153606Z-first-generation-unique-before`, `/root/fairyteller-n8n-imports/20260805-153606Z-first-generation-unique`, and `/root/fairyteller-n8n-exports/20260805-153832Z-first-generation-unique-after`. Verified the production API and n8n health endpoints, active published `fairyteller_intake`, deployed API/frontend markers, and public `/` and `/create` responses. No paid synthetic production generation was created.

### 2026-08-11

- Added an explicit `Оформить без PDF` checkbox to `/pay`. Without it, the PDF link remains a required field; with it, the PDF input is disabled and a direct checkout is available even without a generated-book id. The page still validates contacts, delivery data, and consent. A direct payment creates a separate YooKassa payment and persists a manual-payment record; a confirmed payment sends the payment Telegram chat the buyer's contacts, optional PDF URL, and an explicit instruction to match the book manually. Deployed frontend release: `/var/www/fairyteller/releases/20260811-pay-no-pdf-checkbox-codex`; API rollback copy: `/opt/fairyteller-api/backups/20260811-150000Z-manual-pay-checkout/fairyteller-api.mjs.before`.
- Added the `Киберпанк` cover treatment to the protected book editor alongside `Стандартная`. The selection is persisted as `text.printLayout.coverStyle` and governs both the ordinary 13×13 PDF and the two 20×20 hard-cover outputs. A new story whose order has `world="cyberpunk_dream"` automatically receives the cyberpunk treatment until an editor explicitly changes it. The supplied flattened PSD canvases are deployed without resizing: `background-cyberpunk-13x13.png` (`3171 × 1606 px`, 300 dpi) and `background-hardcover-template-cyberpunk.png` (`5516 × 2835 px`, 300 dpi / printer-ready `467 × 240 mm`). The 20×20 cyberpunk template itself already contains its turn-ins and spine, so those areas remain background-only.
- Added `Серый киберпанк с крапинками · #eae8df` to the page-paper selector. Cyberpunk stories use it by default; the setting can still be changed deliberately in the editor and is stored as `text.printLayout.pagePaperStyle`. The four deterministic paper variants live at `/opt/fairyteller-render/templates/assets/book/cyberpunk-speckle-paper-{1,2,3,4}.jpg`; full-page illustrations are not overlaid with paper.
- Replaced the illustrated `Конец` page for cyberpunk books in all three print paths (13×13, 20×20, and 20×20 · 12 pt). It uses `/opt/fairyteller-render/templates/assets/book/outro-cyberpunk.png`, centred with a cover crop rather than stretched, with a white `Конец` caption. Standard stories retain their existing final illustration.
- Applied the cyberpunk accent colour `#7B2CBF` only inside cyberpunk books: the annotation subtitle, `Оглавление`, contents flourish, and the initial drop cap of every chapter are violet. Standard books retain their established burgundy accent. The corresponding flourish asset is `/opt/fairyteller-render/templates/assets/book/image15-cyberpunk-purple.png`.
- Production deployment has timestamped rollback copies at `/opt/fairyteller-api/backups/20260811-133000Z-cyberpunk-cover-theme`, `/opt/fairyteller-render/backups/20260811-133000Z-cyberpunk-cover-theme`, and `/opt/fairyteller-render/backups/20260811-140000Z-cyberpunk-purple-interior`.

### 2026-08-02

- Added a dedicated compact contents-page treatment for the 20×20 hard-cover sources (regular and 12 pt only). It uses the existing burgundy `DejaVu Serif Bold` heading, `Inter Light` chapter lines, dot leaders, a centred compact composition, and a burgundy flourish. Its chapter-page numbers now derive from the rendered chapter pagination, so the 12 pt reflow is accurate. The 13×13 contents page is unchanged. Renderer backup: `/opt/fairyteller-render/backups/20260802-142000Z-hardcover-compact-toc`.
- Brought the standard 13×13 interior onto the same compact opening-page system as 20×20: the contents page has the burgundy serif heading, chapter rows with dot leaders and page numbers, and the burgundy original flourish; the annotation uses the same centred heading and text column. Body text now has mirrored physical margins: 10 mm outside and 16 mm at the binding (the added binding clearance is 6 mm); odd interior pages are right-hand pages. Standard page numbers moved down, and base renders without an explicit alignment setting now justify body text. The normal uniform size range now reliably uses any one size between 9.5 and 10.5 pt, rather than failing when an inherited server setting held it at 10.5 pt. Renderer/API backups: `/opt/fairyteller-render/backups/20260803-084500Z-standard-13x13-reading-grid`, `/opt/fairyteller-render/backups/20260803-090000Z-standard-uniform-range`, and `/opt/fairyteller-render/backups/20260803-100000Z-hardcover-alerts-standard13-uniform`.
- Corrected generation notifications for the two 20×20 hard-cover actions. Telegram now selects the actually requested 20×20 artifact (including the 12 pt variant) and its print link, rather than showing a stale failed 13×13 render alongside a completed hard-cover file. The affected ordinary 13×13 job `ft_1785664518241_e0kyya` was rebuilt after the new mirrored binding margin: it completed at a uniform 9.5 pt with no text truncation, 41 combined pages, embedded fonts, PDF 1.7, and no protection.
- Reserved a dedicated safe strip for the end-of-chapter flourish in both 20×20 render variants. The ornament now sits below the real final line with a 3 mm gap, rather than being overlaid at the former fixed text-box baseline after the 20×20 reading-grid margin change. Dense chapters reflow to another text page when necessary; a normal and a 12 pt hard-cover smoke render both passed with no text truncation. Renderer backup: `/opt/fairyteller-render/backups/20260803-104500Z-chapter-divider-safe-zone`.
- Corrected physical-page planning when positioned extra illustrations are present. Pagination, preflight, mirrored text margins, and the contents now count every inserted image before the following source page, so a text page keeps the correct left/right binding margin and no longer fails because it was measured on the opposite side of a spread. Renderer backup: `/opt/fairyteller-render/backups/20260803-091500Z-positioned-images-reading-grid`. A 47-page standard PDF with positioned images passed no-truncation render and PDF 1.7/no-encryption checks.
- Replaced the simplified contents-page rule in the two 20×20 variants with the original hand-drawn `image15` flourish from the book template, recoloured in the existing burgundy. The 13×13 layout remains unchanged. Renderer backup: `/opt/fairyteller-render/backups/20260802-144000Z-hardcover-toc-original-flourish`.
- Added positioned additional illustrations in the protected book editor. Each uploaded image can now be placed after a chosen source-page number (or left blank for the end); it takes part in physical pagination but never receives a printed page number. The renderer inserts it while composing the interior, so following text-page numbers, chapter-ending ornaments, paper treatment, and the contents-page chapter numbers remain aligned. Existing end-of-book images keep their prior placement. Production backups: `/opt/fairyteller-api/backups/20260802-151500Z-additional-image-placement`, `/opt/fairyteller-render/backups/20260802-151500Z-additional-image-placement`, and `/opt/fairyteller-render/backups/20260802-152500Z-additional-image-tail-compatibility`.
- Rebuilt the reading grid for both 20×20 hard-cover sources only. Body text now uses mirrored margins on physical spreads: 25 mm at the binding, 20 mm outside, 20 mm at the top and 25 mm below; the page number moved down to a safe 10 mm from the trim. The 13×13 layout is untouched. Where the narrower line length needs it, the 20×20 renderer preserves the selected body size and adds text pages instead of reducing the type. The two current 20×20 files were rebuilt and visually checked on facing text pages. Renderer backups: `/opt/fairyteller-render/backups/20260802-154500Z-hardcover-mirrored-reading-grid` and `/opt/fairyteller-render/backups/20260802-155000Z-hardcover-mirrored-reading-grid-bottom25`.
- Fixed the hard-cover admin queue: the two 20×20 buttons no longer rebuild the unrelated 13×13 PDF first. Each now directly runs its own 20×20 source render and conversion, so a 13×13 `uniform`-text overflow cannot block a valid 20×20 · 12 pt assembly. Production API backup: `/opt/fairyteller-api/backups/20260802-155500Z-hardcover-queue-without-regular-render`.
- Restored the two Telegram updates for each hard-cover assembly without reintroducing the unnecessary 13×13 render. The queue now marks the requested 20×20 artifact as generating before its source render, and ready or failed afterwards; these state changes produce the usual start and completion/failure notifications in the bot. Production API backup: `/opt/fairyteller-api/backups/20260802-160000Z-hardcover-telegram-render-status`.
- Corrected hard-cover render status handling. A successful 20×20 or 20×20 · 12 pt build now owns its own `rendering → done` status, clears a stale unrelated 13×13 error, and the editor only displays an error for the requested hard-cover artifact itself. This prevents a completed 12 pt file being reported as a `uniform` 13×13 failure. Reconciled the affected `ft_1785132231568_jlp5b1` status after confirming its 12 pt artifact was ready. Production API backup: `/opt/fairyteller-api/backups/20260802-163000Z-hardcover-status-cleanup`.
- Fixed the decorative red drop-cap rule across 20×20 renders, including the 12 pt reflowed version. It is now attached to the first text block of each chapter instead of a static page-number list, so text reflow and inserted pages cannot move it into the middle of a chapter. The affected 12 pt book was rebuilt and visually checked at all five chapter openings. Renderer backup: `/opt/fairyteller-render/backups/20260802-120000Z-dropcap-first-text-page`.
- Restyled the two early-interior headings across ordinary 13×13, 20×20, and 20×20 · 12 pt renders. The annotation-page subtitle now uses `DejaVu Serif Bold` in the established drop-cap burgundy (`#9B1C1C`), and the contents heading is now title case `Оглавление` in the same typeface and colour. Story text, cover, page plan, numbering, and chapter opener styles are unchanged. A temporary 13×13 render visually checked both altered pages. Renderer backup: `/opt/fairyteller-render/backups/20260802-101700Z-opening-headings-serif`.
- Aligned the annotation page to the compact contents-page grid in both 20×20 source variants. Its burgundy subtitle now occupies the same centred heading box as `Оглавление`; the centred summary begins on the contents rows' baseline in the same column. The original 13×13 annotation layout is unchanged. A 20×20 · 12 pt source render was visually checked with no text truncation. Renderer backup: `/opt/fairyteller-render/backups/20260803-020000Z-hardcover-annotation-grid`.
- Added an editor-wide page-paper selector: `Белый` produces a completely plain white interior, while `Кремовый с крапинками` uses the existing four-paper rhythm with an exact `#fff4e6` base and the supplied speckle character. The selection is stored as `text.printLayout.pagePaperStyle` and applies to 13×13, 20×20, and 20×20 · 12 pt; full-page illustrations remain artwork-only. The cream derivatives are reusable local assets generated by `scripts/generate-cream-paper-assets.py` and deployed to `/opt/fairyteller-render/templates/assets/book/cream-speckle-paper-{1,2,3,4}.jpg`. Both treatments were rendered and visually checked. Production backups: `/opt/fairyteller-api/backups/20260802-161500Z-page-paper-style` and `/opt/fairyteller-render/backups/20260802-161500Z-page-paper-style`.

### 2026-08-01

- Replaced the old 20×20 cover-panel approximation with two full printer-canvas templates supplied as PSDs: `Покусанная` and `Белая`. Both use the PSD's exact `5516 × 2835 px` / `467 × 240 mm` canvas at 300 dpi, preserve the technical turn-ins and spine as background, and place the generated cover art only inside the PSD photo opening. The admin editor now stores `text.printLayout.hardcoverCoverTemplate` and offers this choice only for the two 20×20 hard-cover actions; the 13×13 PDFs remain unchanged. Production renderer assets are `/opt/fairyteller-render/templates/assets/cover/background-hardcover-template-{bitten,white}.jpg`; source PSD composites remain the visual authority. A smoke copy passed both variants: 47 pages / 46 interiors, PDF 1.7, RGB, no protection, embedded fonts, cover `467 × 240 mm`, interiors `203 × 203 mm`. Deployment backups: `/opt/fairyteller-render/backups/20260801-150900Z-hardcover-cover-templates` and `/opt/fairyteller-api/backups/20260801-150900Z-hardcover-cover-templates`.

- Added four supplied 20×20 interior-paper PSD variants to both hard-cover flows only. Each was flattened at its native `2398 × 2398 px` / `203 × 203 mm` / 300 dpi size and is selected in a deterministic cycle for text and service pages; full-page illustrations retain their artwork-only background. The original 13×13 paper assets remain untouched. Production renderer assets are `/opt/fairyteller-render/templates/assets/book/hardcover-paper-{1,2,3,4}.jpg`, with rollback copy `/opt/fairyteller-render/backups/20260801-152800Z-hardcover-paper-textures`. Both normal 20×20 and 12 pt smoke files passed the print checks (PDF 1.7, no protection, RGB, embedded fonts, 467×240 mm cover and 203×203 mm interiors).

- Fixed the 20×20 queue regression introduced with the cover-template selector: the API attempted to call the renderer-local `readJson` helper, so the selected template was saved but the queued build stopped before rendering and left the previous PDF download in place. Both normal and 12 pt queue functions now read the saved full text through the API's `readJsonFile` helper. Rebuilt affected job `ft_1785143099526_ru465w` directly with its persisted `hardcoverCoverTemplate="white"`, updated its generated file/version metadata, and verified its result records the white full-PSD template. Backup: `/opt/fairyteller-api/backups/20260801-153600Z-hardcover-template-queue-fix`.

- Restored the intended full-PSD hard-cover back layout: the subtitle (for example, `Путешествие на Марс`) now sits above a narrow left-side blurb column, and the gold `by FairyTeller` mark is again placed over the printer-template arrow. Reduced all printed page numbers to 8 pt in the 13×13 output and an effective 10 pt in the 20×20 outputs. Physical page counting is unchanged, but numbers are now printed only on story-text pages; title, description, contents, chapter opener, illustration, outro, QR, additional-art and padding pages remain unnumbered. Applied to normal 20×20 and 12 pt code paths as well as the ordinary template. Rebuilt both 20×20 files for `ft_1785143099526_ru465w` and verified PDF 1.7/no protection/embedded fonts with correct sizes. Renderer backup: `/opt/fairyteller-render/backups/20260801-154600Z-cover-back-and-page-numbers`.

- Tuned the full-PSD hard-cover back copy after visual review: its subtitle and blurb are now centered in a column shifted 10 mm clear of the fold/illustration edge, while the subtitle sits lower inside the marked safe area. Reduced the 20×20 internal title-page book title from 40 pt to the same 30 pt scale as chapter titles; `2026 год` remains unchanged. Rebuilt the normal and 12 pt files for `ft_1785143099526_ru465w`, refreshed their download cache versions, and visually checked both the cover spread and internal title page. Renderer backup: `/opt/fairyteller-render/backups/20260801-163300Z-back-cover-safe-copy-and-title-scale`.

- Extended the four supplied 20×20 paper textures to the ordinary 13×13 interior flow. Every non-illustration interior page now uses the same deterministic sequence `1 → 2 → 3 → 4`, repeating as needed; full-page illustrations remain artwork-only. The rendering change affects future 13×13 and both 20×20 builds without changing their text layout or pagination. A temporary 47-page 13×13 render visually confirmed the alternating backgrounds on title, contents, chapter, and story-text pages. Renderer backup: `/opt/fairyteller-render/backups/20260801-164800Z-13x13-paper-cycle`.

- Corrected the front-cover title position in the 20×20 full-PSD template. The title block now begins at 27 mm from the top of the 240 mm canvas, centering it between the PSD's front safe-area line and the upper edge of the photo frame while retaining a clear gap above the frame. Rebuilt both 20×20 PDFs for `ft_1785143099526_ru465w`, refreshed their cache-versioned download URLs, and visually inspected the cover spread. Renderer backup: `/opt/fairyteller-render/backups/20260801-165300Z-front-title-center`.

### 2026-07-31

- Added a separate admin action `Сохранить и собрать твёрдую 20×20 · 12 pt`. It keeps the regular 13×13 PDF and the existing large-print 20×20 PDF untouched, creates a temporary source rendering at 8.04 pt, then scales it to the 203 mm interior page for an effective 12 pt print size. Its dedicated adaptive paginator reflows unchanged chapter text into the smallest fitting number of pages, preserves the 20×20 cover flow, and adds an unnumbered padding page only when needed to keep the interior page count even. The resulting file is `hardcover-20x20-12pt.pdf` and appears as `print 20×20 · 12 pt` in the editor. Production smoke test on a copied 47-page book produced 39 total pages / 38 interiors, PDF 1.7, RGB, no protection, embedded fonts, with exact source 8.04 pt becoming 12 pt at 203/136 scale. Deployed API and both renderers with timestamped `20260731-150000Z-hardcover-12pt` rollback copies; API restarted and health returned 200.

- Corrected chapter-end decoration in the adaptive 12 pt workflow. The divider line and the final-page paper treatment are now keyed to the actual final text page of every reflowed chapter instead of the old fixed 13×13 page numbers. A test of consecutive pages confirmed no divider inside a chapter and the divider present at its true end. Deployed `/opt/fairyteller-render/fairyteller-render-pdf.mjs` with rollback copy `fairyteller-render-pdf.mjs.before-20260731-155000Z-hardcover12-chapter-lines`; API health returned 200.

- The second interior page's story description is now always centred line-by-line. This is intentionally independent of the admin's main-story alignment control, which continues to govern only chapter text pages. Deployed `/opt/fairyteller-render/fairyteller-render-pdf.mjs` with rollback copy `fairyteller-render-pdf.mjs.before-20260731-141000Z-page2-summary-center`; production renderer syntax check and API health both passed.

- Replaced the unsafe full-spread scaling in the 20×20 hard-cover converter with a PSD-27236-safe cover template. The converter now places the original back and front cover halves only inside separately clipped content panels, inset by 2 mm from the PSD safety boundaries; the outer turn-ins plus the hinge/spine band use one continuous cranberry paper field sampled from the standard cover background. This preserves the exact red base and fleck frequency without procedural dots or visible tiled seams, while keeping titles, blurbs, faces and framed cover artwork out of the fold, hinge and spine zones. Interior pages remain vector-preserved and unchanged. A copied production book passed visual overlay against `27236.psd` and the hard-cover preflight: PDF 1.7, 47 pages / 46 interiors, 467×240 mm cover, 203×203 mm interiors, no protection and embedded fonts; it emitted only the existing `DeviceGray` mask warning. Deployed `/opt/fairyteller-render/fairyteller-hardcover-20x20.mjs` and `templates/assets/cover/background-hardcover-27236.png` with timestamped rollback copies, and confirmed API health.

- Removed the experimental automatic spine title from the 20×20 cover template. The complete technical band — spine and both hinge clearances — is again background only, while the PSD-safe content panels and continuous red paper texture remain unchanged. Deployed `/opt/fairyteller-render/fairyteller-hardcover-20x20.mjs` with rollback copy `fairyteller-hardcover-20x20.mjs.before-20260731-142100Z-remove-spine-title`; API health returned 200.

### 2026-07-28

- Added the protected admin-editor action `Сохранить и собрать твёрдую 20×20`. It first rebuilds the ordinary book from the current editorial text and original images, then creates a separate `hardcover-20x20.pdf` without overwriting the existing print PDF. The converter preserves the regular book’s vector pages, fonts, line breaks, and page numbering while adapting page 1 to `467 × 240 mm` and each following page to `203 × 203 mm`; it checks the 25–161 page limit and an even number of interior pages. Production files: `/opt/fairyteller-api/fairyteller-api.mjs` and `/opt/fairyteller-render/fairyteller-hardcover-20x20.mjs`. API backup before deployment: `/opt/fairyteller-api/backups/20260728-082114Z-hardcover-20x20-button/fairyteller-api.mjs.before`. Verified the production service health, the rendered button, and a server-side 41-page test PDF: PDF 1.7, no encryption, cover `467 × 240 mm`, interior `203 × 203 mm`.

### 2026-07-21

- Added an early exact story-layout guard after two of twelve post-density generations overflowed only at final PDF assembly (`ft_1784637935837_qjb9fa`, chapter 1; `ft_1784633258921_5bbzql`, chapter 4). The authenticated Job API endpoint `POST /api/fairyteller/jobs/:jobId/text-preflight` runs the renderer's existing read-only uniform-pagination preflight and returns the exact overflowing chapter without changing job status or producing PDFs. `fairyteller_full_text` now invokes this guard after its atomic full-text write and before chapters 2-5 illustrations start. Normal books persist `fitControl.status=passed` and continue without a Gemini call. On overflow, one batched Gemini request rewrites only the exact failing chapter plus chapters exceeding conservative per-chapter safety budgets, preserving titles, summaries, visual briefs, events, continuity, and paragraph rhythm; the artifact is checked again, with at most one lower-target retry. The workflow never adds prose, truncates text, or falls back to smaller/mixed PDF fonts. Production API backup: `/opt/fairyteller-api/backups/20260721-131442Z-text-fit-guard/fairyteller-api.mjs.before`; n8n backup/import/after-export: `/root/fairyteller-n8n-exports/20260721-131442Z-text-fit-guard-before`, `/root/fairyteller-n8n-imports/20260721-131442Z-text-fit-guard`, and `/root/fairyteller-n8n-exports/20260721-131442Z-text-fit-guard-after`. Verified API/Code-node syntax, mocked normal and both real overflow-selection paths, live read-only preflight against one fitting and both failing jobs, matching deployed API hash, active published workflow markers, API/n8n health after restart, and no paid synthetic generation. One preflight currently costs about 10-12 seconds of wall time but no model tokens; only overflowing books make an extra text-model call.

### 2026-07-20

- Corrected the text-density regression introduced by the narrow `780-820` page target without restoring any post-generation expansion call. Chapter 1 and continuation prompts now target `850-950` characters per print page, approximately `900`, with explicit whole-chapter budgets (`3400-3800` for chapter 1, `3400-3700` for chapter 2, `4900-5200` for chapters 3-4, and `4000-4300` for chapter 5). The prompts also require 3-5 paragraphs of varied length per block instead of dense paragraph bricks. Both text normalizers now preserve model-provided escaped paragraph boundaries rather than collapsing all whitespace before dialogue repair; the full-text block-count normalizer preserves them on the valid structured-response path as well. No correction/dosage stage or additional model call was added, and the PDF renderer's existing leading and paragraph-gap settings remain unchanged. Production backup: `/root/fairyteller-n8n-exports/20260720-110140Z-story-density-paragraphs-before`; import: `/root/fairyteller-n8n-imports/20260720-110140Z-story-density-paragraphs`; after-export: `/root/fairyteller-n8n-exports/20260720-110140Z-story-density-paragraphs-after`. Verified local Code-node syntax, paragraph-preservation behavior, successful frontend build, both published production workflows containing the new targets/normalizers, and n8n health after restart. No paid synthetic generation was started.

- Fixed a child-reference failure cascade that could produce an entire book with no people. The lightweight hero-card retry now always creates a visibly illustrated neutral identity guide, even for photorealistic books, instead of producing a near-photographic private-child portrait that downstream Gemini image requests may reject. First-chapter, continuation, and cover final fallbacks no longer switch to `safe_environment_no_people` when a profiled hero is required; the existing final call instead removes identity images and renders the named hero as a privacy-safe fictional character from the written age/description, recorded as `safe_character_no_references`. Pure environment fallback remains only when no profiled hero is available. Text prompts now require chapter and cover visual briefs to choose moments renderable with the user-profiled heroes and not make invented side characters, crowds, students, or teachers mandatory. No additional model calls were added. Production backup: `/root/fairyteller-n8n-exports/20260720-required-hero-fallback-before`; import: `/root/fairyteller-n8n-imports/20260720-required-hero-fallback`; after-export: `/root/fairyteller-n8n-exports/20260720-required-hero-fallback-after`. Verified production/local parity before editing, workflow JSON and six affected Code-node syntaxes, required-hero fallback routing, removal of `safe_environment_no_people` from all three image paths, illustrated child-card retry markers, visual-brief cast locks, successful `npm run build`, five active production exports matching local code, and n8n health after restart. No paid synthetic generation was started.

- Removed automatic post-generation short-block expansion from the Fairyteller text path and moved print-volume control into the primary prompts. Chapter 1 and chapters 2-5 now require every page-sized `textBlocks[i]` to contain a narrow `780-820` character range with an approximately `800`-character target and an explicit pre-response self-check; with the fixed 25-page text plan this yields a requested total range of `19,500-20,500` characters. `Normalize Full Text` still measures actual block lengths and persists `measured_only` diagnostics with the target range, but makes no correction model call and never rewrites generated prose. This removes the recent multi-minute failed correction path while preserving visibility into underfilled blocks. Production backup: `/root/fairyteller-n8n-exports/20260720-text-block-budget-before`; import: `/root/fairyteller-n8n-imports/20260720-text-block-budget`; after-export: `/root/fairyteller-n8n-exports/20260720-text-block-budget-after`. Verified production/local parity before editing, workflow JSON and affected Code-node syntax, removal of old prompt ranges and the correction invocation, retained measured-only diagnostics, successful `npm run build`, active production exports matching local code, and n8n health after restart. No paid synthetic generation was started.

- Simplified photo-based hero reference-card recovery to exactly two semantic attempts while the private source photo is still available in memory: the existing full primary card prompt followed, only when no inline image is returned, by one compact likeness-first retry. The lightweight retry attaches the same source photo, keeps the selected book style as gentle rendering context, prioritizes distinctive facial geometry and apparent age, and removes story scenery, action, props, symbols, text, and additional people. Successfully generated cards are not retried. Transport-level retries for timeouts/rate limits remain separate and unchanged. Card metadata now records whether the result came from `primary_with_source_photo`, `primary_from_description`, or `lightweight_retry_with_source_photo`. No extra call is added to the normal success path. Production backup: `/root/fairyteller-n8n-exports/20260720-hero-card-retry-before`; import: `/root/fairyteller-n8n-imports/20260720-hero-card-retry`; after-export: `/root/fairyteller-n8n-exports/20260720-hero-card-retry-after`. Verified production/local parity before editing, Code-node and workflow JSON syntax, exactly two semantic card-generation calls, source-photo attachment and style/identity markers in the lightweight retry, successful `npm run build`, active production export matching local code, and n8n health after restart. No paid synthetic generation was started.

- Added controlled scene richness to every Fairyteller story-image path without changing hero reference cards, model-call count, image model, temperature, or aspect ratios. Chapter 1, chapters 2-5, covers, referenced retries, likeness retries, and environment fallbacks now request 3-5 location-specific supporting details, believable material/surface texture, and readable foreground/middle/background layers. The same instructions keep the hero and current action dominant, preserve the selected rendering technique instead of pushing every style toward photorealism, and forbid extra people, duplicate heroes, extra artifacts, repeated copies of the important object, readable text, random symbols, and unrelated decoration. Cover prompts additionally preserve the calm upper-third title area and an iconic rather than cluttered composition. Production backup: `/root/fairyteller-n8n-exports/20260720-scene-detail-before`; import: `/root/fairyteller-n8n-imports/20260720-scene-detail`; after-export: `/root/fairyteller-n8n-exports/20260720-scene-detail-after`. Verified production/local parity before editing, workflow JSON and Code-node syntax, required prompt markers across every primary/retry/fallback path, successful `npm run build`, active production exports, and n8n health after restart. The repository-wide lint remains blocked by pre-existing errors in untouched frontend files. No paid synthetic generation was started.

- Hardened Fairyteller cover-brief and text-volume handling in production n8n. `fairyteller_text` and `fairyteller_full_text` now normalize structured `coverArtBrief` objects into readable labeled prompt text, reject technical stringification artifacts such as `[object Object]`, `undefined`, and `null`, and fall back to the chapter visual brief/summary when needed; `fairyteller_cover` applies the same guard to all prompt context. After the complete five-chapter text exists, `fairyteller_full_text` now measures every print-page block, identifies blocks below 720 characters, and makes at most one batched Gemini correction request for only those blocks. Replacements are accepted only in the 760-950 character safety range, preserve all story facts and neighboring continuity, and target 780-840 characters. A failed or malformed correction is `failed_open`: the original story continues to rendering with diagnostic metadata instead of failing the order. Normal generations with no short blocks make no additional model call. Production backup: `/root/fairyteller-n8n-exports/20260720-technical-text-fixes-before`; import: `/root/fairyteller-n8n-imports/20260720-technical-text-fixes`; after-export: `/root/fairyteller-n8n-exports/20260720-technical-text-fixes-after`. Verified production/local parity before editing, workflow JSON and Code-node syntax, executable success and failed-open simulations, `npm run build`, `npm run lint`, active production exports containing both guards, and n8n health after restart. No paid synthetic generation was started.

### 2026-07-13

- Cleaned production disk after root usage reached 85%. Safe cleanup removed apt lists/cache, old `/tmp` entries, exited Docker backup containers, reduced systemd journals to about 200 MB, and pruned Fairyteller frontend releases from 102 to the current/latest 7. Applied the unpaid-PDF retention rule once: deleted 156 generated PDF artifacts from unpaid jobs older than 30 days, freeing about 2.52 GB while leaving paid jobs and non-PDF job data untouched. Root filesystem usage dropped from 85% to 69%, `/var/www/fairyteller/releases` dropped to 398 MB, and `/data/fairyteller/jobs` dropped to 12 GB. Installed and verified `fairyteller-cleanup-unpaid-pdfs.timer` for ongoing daily retention.

- Aligned admin text blocks with the actual post-pagination PDF pages. The per-field counters initially described stored source blocks, while the chapter paginator could move sentences between pages, so `Блок 5/6` did not match PDF pages 5/6. Successful renders now persist each page's exact text alongside utilization metrics in `render.json`. The admin editor uses these rendered page blocks only after verifying that their combined normalized text equals the current editorial source; edited or stale renders safely fall back to source blocks. During verification, another renderer mutation was found: dialogue repair logic converted correct `— Настя! — хором закричали...` into two paragraphs and changed capitalization. All renderer-side dialogue rewriting and inferred paragraph creation were removed. PDF layout now respects only explicit blank lines from the editor and otherwise performs line wrapping without editorial changes. Rebuilt `ft_1783851745821_0r25p4`: all six chapter-4 editor blocks match all six rendered PDF pages exactly character-for-character, the dialogue remains inline, the job is `done`, and the entire story fits at a uniform 10.5 pt. Backups: `/opt/fairyteller-render/backups/20260712-184033Z-admin-pdf-page-blocks/fairyteller-render-pdf.mjs.before` and `/opt/fairyteller-api/backups/fairyteller-api.mjs.20260712-184033Z-admin-pdf-page-blocks.before.bak`. Verified both Node files, completed production render, final PDF text extraction, exact editor/render page equality, active API service, and ready artifacts.

- Added live character-count guidance to every story text block in the protected admin book editor. Each textarea now shows `current characters · state` and updates immediately while typing: below 760 is highlighted as `мало текста`, 760-1050 as `норма`, and above 1050 as `риск переполнения`. The accompanying copy explicitly calls the range a per-page guideline because actual PDF capacity depends on words, paragraph structure, uniform font size, and chapter-level repagination. The counter is advisory and does not block saving. Production API backup: `/opt/fairyteller-api/backups/fairyteller-api.mjs.20260712-171216Z-admin-character-counts.before.bak`. Verified production/local API parity before editing, `node --check`, active API service, public site availability, and real editor HTML for `ft_1783851745821_0r25p4` containing correct normal/short/overflow values.

### 2026-07-12

- Preserved same-speaker dialogue continuation during PDF pagination. The renderer previously treated every punctuation-plus-dash sequence such as `. — Какой торт...` as an inferred new paragraph, corrupting correctly authored Russian dialogue of the form `— Реплика, — сказал герой. — Продолжение реплики.` It now respects the editor's paragraph structure: a new PDF paragraph is created from an actual blank line, while sentence continuation after author words remains in the same paragraph and may wrap only at the page's line width. Rebuilt `ft_1783851745821_0r25p4` and verified the target passage through final-PDF text extraction; the continuation stays inline. Removing the false paragraph gaps also allowed the whole book to return from 9.75 pt to a uniform 10.25 pt. Renderer backup: `/opt/fairyteller-render/backups/20260712-170437Z-dialogue-paragraphs/fairyteller-render-pdf.mjs.before`. Verified `node --check`, production text preflight, completed render, `done` status, extracted dialogue layout, and identical 10.25 pt min/max story font.

- Hardened admin PDF rebuilding after edited job `ft_1783851745821_0r25p4` failed because chapter 2 could not fit its four fixed text pages at the previous 10 pt uniform minimum. The paginator's greedy boundary selection was replaced with a memoized whole-chapter search that evaluates valid page splits globally instead of filling the first page and failing when later pages overflow. Text equality remains a mandatory postcondition: pagination may only move boundaries and cannot rewrite or drop text. A production preflight proved the edited 3459-character chapter still cannot physically fit at 10 pt, so the uniform lower bound was reduced to 9.5 pt; the renderer selected 9.75 pt for this book and applied it identically across all 25 story pages. The affected job was rebuilt successfully and returned to `done` / `PDF готов`, with 40 interior pages, 42 preview pages, no truncation, and unchanged story text. Renderer backup: `/opt/fairyteller-render/backups/20260712-164825Z-paginator-dp/fairyteller-render-pdf.mjs.before`. Verified local/production source parity before editing, `node --check`, production text preflight, completed production render, uniform min/max font size 9.75 pt, and ready render artifacts.

- Restored chapter-to-chapter illustration variety after the first object-canon release exposed an object/string normalization mismatch: Gemini returned structured `visualBrief` and `outfitCanon` objects, which were flattened to `[object Object]`, leaving the shared compass canon as the dominant instruction in every scene. `fairyteller_full_text` now explicitly requests an object visual brief derived strictly from the chapter's first `textBlocks` page and normalizes both new object and legacy string formats. It persists scene, visible hero numbers, assigned shot type, `useArtifactCanon`, artifact role, required objects, scale, and up to three scene-specific exclusions; object-form outfit locks are converted into compact readable lines. Chapter 2 uses a new-location wide shot, chapter 3 a medium interaction shot, chapter 4 a dynamic wide shot, and chapter 5 a calm emotional closing shot. `fairyteller_full_visuals` now includes `artifactCanon`, scale, and artifact role only when `useArtifactCanon === true`; otherwise the important detail is omitted from that chapter prompt. The text model is instructed to use the canon only when the detail is physically visible and necessary, normally in no more than two of chapters 2-5. Cover behavior remains unchanged and always uses the object canon. No additional model calls were added. Production backup: `/root/fairyteller-n8n-exports/20260712-152907Z-visual-variety-before`; import: `/root/fairyteller-n8n-imports/20260712-152907Z-visual-variety`; after-export: `/root/fairyteller-n8n-exports/20260712-152907Z-visual-variety-after`. Verified production/local parity before editing, JSON and Code-node syntax, active published marker routing, and n8n health after restart. No paid generation was started.

### 2026-07-11

- Added a compact visual-continuity contract after reviewing `ft_1783758619386_ufwmco` and `ft_1783758303424_gul8mh`. Both books completed every continuation chapter and cover on the first referenced attempt, but the key vehicle changed between toy, child-size kart and full-size sports car; covers could also invent extra child lookalikes. The existing full-text call now produces one 180-300 character `artifactCanon`, a one-line `outfitCanon`, and per-chapter `requiredObjects`, `objectScale`, and at most three scene-specific `forbiddenElements`. No additional model call was added. Continuation prompts route these short locks alongside the scene brief, prohibit unlisted people/doubles/crowds, and strengthen edge-to-edge rendering language. Cover prompts use the same object/outfit canon, prohibit extra people, and reduce whole-story context from 3200 to 1200 characters, keeping the primary request compact. Production backup: `/root/fairyteller-n8n-exports/20260711-085340Z-visual-object-canon-before`; import: `/root/fairyteller-n8n-imports/20260711-085340Z-visual-object-canon`; after-export: `/root/fairyteller-n8n-exports/20260711-085340Z-visual-object-canon-after`. Verified production/local parity before editing, workflow JSON and all Code-node syntax, active published exports, required routing markers, and n8n health after restart. No paid generation was started.

- Updated the public book paywall and checkout contact preferences. The paywall now opens after chapter 1; if the visitor closes it, it collapses to a compact payment strip and fully opens again at the end of chapter 5. Its copy now makes clear that the preview is a draft and that text/illustration edits and the final version are coordinated before printing. The `/pay` form now offers optional WhatsApp and Telegram checkboxes directly below the phone field. Selected channels are persisted with the YooKassa payment metadata and shown in the paid-order Telegram alert. Frontend release: `/var/www/fairyteller/releases/20260711-paywall-collapsed-strip-codex`; API backup: `/opt/fairyteller-api/backups/fairyteller-api.mjs.20260711-paywall-contact-channels-before.bak`. Verified local production-file parity before editing, `node --check server/fairyteller-api.mjs`, `npm run build`, `git diff --check`, API health, and public `/pay` and book routes after deployment.

- Improved the first-pass success path for continuation illustrations. `fairyteller_full_text` now asks Gemini to return a separate compact `visualBrief` and the exact `heroNumbers` visible in every chapter 2-5, while keeping `summary` reader-facing; normalization persists both fields. `fairyteller_full_visuals` now builds the primary request from that short scene brief instead of the full visual bible, chapter prose, repeated policy language, and every character description. When `heroNumbers` is available, only the matching generated hero cards are attached, reducing prompt ambiguity and payload size while retaining backward-compatible all-card behavior for old jobs. Restored a third, minimal referenced likeness retry before the final people-free environment fallback, so a transient reference refusal no longer removes the protagonist immediately. Production backup: `/root/fairyteller-n8n-exports/20260711-014607Z-visual-first-pass-before`; import: `/root/fairyteller-n8n-imports/20260711-014607Z-visual-first-pass`; after-export: `/root/fairyteller-n8n-exports/20260711-014607Z-visual-first-pass-after`. Verified production/local parity before editing, workflow JSON and all Code-node syntax, required prompt/routing markers in active published exports, and n8n health after restart. No paid generation was started.

- Corrected the continuation/cover reference contract after visually auditing `ft_1783711569220_cke85c`. Its hero card was good and chapter 1 succeeded with `primary_with_reference_cards`, but every chapter 2-5 and the cover rejected both referenced attempts and fell through to `safe_environment_no_people`; Gemini then ignored the people ban in chapter 4 and the cover and drew unrelated characters. The prior `generated fictional character` wording was therefore removed. Continuation and cover prompts now mirror the successful chapter-1 contract: attached generated character reference cards are the required visual canon, with the same face shape, hairline, hair, age, accessories, clothing colors, body type, and silhouette. All three image calls in each workflow request `IMAGE` only, matching chapter 1. When a job with references reaches the people-free fallback, chapter summaries, chapter titles, book promises, hero names, and character actions are omitted entirely; the request contains only style, world direction, location, environment, and important object. Production backup: `/root/fairyteller-n8n-exports/20260710-194309Z-visual-reference-contract-before`; import: `/root/fairyteller-n8n-imports/20260710-194309Z-visual-reference-contract`; after-export: `/root/fairyteller-n8n-exports/20260710-194309Z-visual-reference-contract-after`. Verified workflow JSON and all Code-node syntax, executable people-free prompt tests with no character-context leakage, `git diff --check`, active published exports, and n8n health after restart.

- Split the first chapter's reader summary from its image-generation brief. Previously `chapter1.summary` was explicitly requested as a camera/lighting/composition brief and the same field was printed on the chapter title page, producing photo descriptions while chapters 2-5 displayed normal plot summaries. The first-text schema now requires `chapter1.summary` as a 160-280 character reader-facing description of the chapter's events and `chapter1.visualBrief` as the separate 260-380 character illustration direction. Normalization persists both fields in preview/chapter data, while `fairyteller_visuals` routes only `visualBrief` into the chapter-1 image prompt and keeps `summary` for PDF/admin readers. Loose-response recovery falls back from `visualBrief` to `summary` for compatibility. Existing artifacts are unchanged; new generations use the split contract. Production backup: `/root/fairyteller-n8n-exports/20260710-192051Z-chapter1-summary-before`; import: `/root/fairyteller-n8n-imports/20260710-192051Z-chapter1-summary`; after-export: `/root/fairyteller-n8n-exports/20260710-192051Z-chapter1-summary-after`. Verified workflow JSON and all Code-node syntax, an executable end-to-end synthetic Gemini response proving summary/visualBrief routing, `git diff --check`, active published exports, and n8n health after restart.

- Reduced Gemini image safety refusals in continuation chapters and covers after inspecting completed test jobs `ft_1783709435207_8aa6p4` and `ft_1783709822549_8jetms`. Although both books completed, every chapter 2-5 needed a retry; one photorealistic chapter and three watercolor chapters fell through to the people-free fallback. Removed prompt language that repeatedly framed generated reference cards as private-person likeness reproduction (`private person`, `real-person`, `direct copy`, `recognizable face`, `identity-preserving`) and now describes them solely as generated fictional character designs and visual canon. Continuation image prompts no longer append the first 900 characters of chapter prose; they use a maximum 700-character visual summary after sanitizing phrases such as forbidden entry, breaking in, traps, security systems, intruders, tools, threats, and danger into calm visual equivalents. Cover whole-story context now uses the same visual sanitizer. The people-free last-resort fallback remains unchanged. Production backup: `/root/fairyteller-n8n-exports/20260710-191143Z-visual-safety-language-before`; import: `/root/fairyteller-n8n-imports/20260710-191143Z-visual-safety-language`; after-export: `/root/fairyteller-n8n-exports/20260710-191143Z-visual-safety-language-after`. Verified workflow JSON and all Code-node syntax, executable visual-scene tests, `git diff --check`, active published exports with no private-likeness language or full-prose scene input, and n8n health after restart.

- Fixed a production `fairyteller_visuals` regression introduced by visual stage 1: `normalizedWorldVisual` was evaluated before the local `bible` declaration in `Build Chapter 1 Image Prompt`, causing `ReferenceError: Cannot access 'bible' before initialization` immediately after hero-card generation. Jobs `ft_1783708406300_p6pg38` and `ft_1783708827964_u2w42f` exposed the issue; the first was also interrupted by the deployment restart, while the second full-visual workflow later failed waiting for the missing `visuals.json`. Moved normalization after the `bible` declaration and added an explicit declaration-order execution guard in validation. The two existing test jobs were intentionally left untouched for manual replacement tests. Production backup: `/root/fairyteller-n8n-exports/20260710-184600Z-visual-tdz-before`; import: `/root/fairyteller-n8n-imports/20260710-184600Z-visual-tdz`; after-export: `/root/fairyteller-n8n-exports/20260710-184600Z-visual-tdz-after`. Verified active published export order and n8n health after restart.

- Made all five story-world visual directions rendering-technique neutral so the selected illustration style is the only owner of medium and surface treatment. Removed world-level cues such as `storybook realism`, `textured painterly surfaces`, `soft bokeh`, `3D/collage`, and cinematic-render wording while preserving palette, lighting, environment, spatial composition, readable objects, movement, mood, and genre motifs for romantic, adventure, magical-school, fantasy, and cyberpunk stories. This prevents `toonflat`, `minibrick`, `watercolor`, `claymotion`, `yarncraft`, `naive`, and `celcinema` from competing with an unrelated technique embedded in the genre bible. Production backup: `/root/fairyteller-n8n-exports/20260710-183502Z-world-visual-before`; import: `/root/fairyteller-n8n-imports/20260710-183502Z-world-visual`; after-export: `/root/fairyteller-n8n-exports/20260710-183502Z-world-visual-after`. Verified production/local parity before editing, workflow JSON and all Code-node syntax, exactly five technique-neutral world visual profiles, `git diff --check`, active published export, and n8n health after restart.

- Deployed visual-generation quality stage 1 after reviewing job `ft_1783700508293_yogcai`, where chapters 2-3 fell through to `safe_environment_no_references`, replaced the two adult heroes with generic children, and the requested `photorealistic` style conflicted with the fantasy bible's painterly language. Hero reference cards now use a consistent neutral waist-up identity composition, even light, plain gray background, source-photo clothing cues, and explicit bans on invented occupation/religious/story props. Photorealistic jobs sanitize painterly/watercolor/storybook world-language conflicts and add a camera-like realism lock across chapter 1, chapters 2-5, and cover prompts. Chapter 4 uses a calm discovery/decision tableau before safety filters rather than an aggressive confrontation. Chapter and cover generation now use the primary referenced request plus one calm referenced retry; the removed third near-duplicate retry reduces latency. If both referenced attempts fail, jobs with identity references generate an environment/object/animal-only image with no people or humanoid replacements instead of accepting random substitute heroes. Production backup: `/root/fairyteller-n8n-exports/20260710-182416Z-visual-stage1-before`; import: `/root/fairyteller-n8n-imports/20260710-182416Z-visual-stage1`; after-export: `/root/fairyteller-n8n-exports/20260710-182416Z-visual-stage1-after`. Verified local/production workflow parity before editing, JSON and all Code-node syntax, targeted executable prompt tests for style locking, chapter-4 staging and people-free fallback, `git diff --check`, active published exports for `fairyteller_visuals`, `fairyteller_full_visuals`, and `fairyteller_cover`, and n8n health after restart. No paid synthetic generation was started.

- Softened the `fantasy_epic` and `cyberpunk_dream` story profiles without removing genre conflict. Fantasy now uses local, recoverable stakes, wonder, restoration, return, or reconciliation instead of absolute evil, death, bloodshed, sacrifice, world destruction, or hopelessness. Cyberpunk now centers on a technological mystery and helping a person or small community instead of repression, political persecution, arrests, war, overthrowing a system, or global catastrophe. Both profiles target an approximate `60%` adventure/discovery, `25%` tension, and `15%` warm/quiet/humorous balance with no more than two tense scenes in sequence. Added an explicit child-tone override to both `fairyteller_text` and `fairyteller_full_text`: if any active hero has `ageGroup="child"`, the entire book must be light, life-affirming, emotionally safe, and warmly resolved; this rule explicitly overrides genre atmosphere. Production backup: `/root/fairyteller-n8n-exports/20260710-174348Z-tone-guard-before`; import: `/root/fairyteller-n8n-imports/20260710-174348Z-tone-guard`; after-export: `/root/fairyteller-n8n-exports/20260710-174348Z-tone-guard-after`. Verified production/local workflow parity before editing, workflow JSON and all Code-node syntax, active published exports containing the child and genre guards, `git diff --check`, and n8n health after restart.

### 2026-07-10

- Added and deployed a shell-only manual follow-up resend mode: `node /opt/fairyteller-api/fairyteller-api.mjs --send-follow-up=<email>`. It uses the same production follow-up HTML template as the scheduled processor (rather than the generic admin-mail template), refuses recipients who unsubscribed, includes up to three ready unpaid stories, records an audit row, and applies the normal 24-hour cooldown after delivery.

- Deployed the follow-up email processor to production, then disabled it after the initial run exposed an incorrect historical backfill. `FAIRYTELLER_FOLLOW_UP_ENABLED_AT` is set to the new activation timestamp and a second rolling eligibility condition limits candidates to jobs created in the last 24 hours; only jobs satisfying both conditions are eligible. `node /opt/fairyteller-api/fairyteller-api.mjs --run-follow-ups` groups those new books by normalized email, waits three hours after the most recent generation, includes up to three finished books with separate preview/payment links, and enforces a 24-hour per-email send cooldown. It suppresses follow-up when the customer has any paid book within the previous 24 hours, rechecks payment immediately before delivery, stores only hashed-email state/audit records under `/data/fairyteller/follow-up/`, and provides signed one-click unsubscribe links that do not affect transactional purchase emails. Sends are sequential with a 750 ms default interval to respect the mail provider's rate limit. Initial production run exposed Resend throttling during a parallel send (10 delivered, 45 rejected); the processor was immediately changed to sequential delivery, redeployed, and the retry completed with 45 delivered, 24 skipped, and 0 failed. API backup before the original deploy: `/opt/fairyteller-api/backups/fairyteller-api.mjs.20260710-153216Z-follow-up-before.bak`; a second backup was made before the rate-limit fix.

- Hardened first-chapter normalization after job `ft_1783682727540_0yg1hp` returned valid Gemini JSON with 9 text blocks while the print contract requires 4. `Normalize First Chapter` now joins any non-empty mismatched block list and reflows the complete text by sentence boundaries into exactly the configured number of first-chapter blocks; empty or genuinely unrecoverable responses still fail. Production backup: `/root/fairyteller-n8n-exports/20260710-114500Z-first-chapter-reflow-before`; import: `/root/fairyteller-n8n-imports/20260710-114500Z-first-chapter-reflow`; after-export: `/root/fairyteller-n8n-exports/20260710-114500Z-first-chapter-reflow-after`. Verified workflow JSON and all Code-node syntax, `git diff --check`, active published export, and n8n health after restart.

- Raised the default generated-story length target from 15,000 to 19,500 characters in every public constructor and in the production `fairyteller_intake`/`fairyteller_text` fallback contract. This is intentionally only a target increase; no post-generation actual-length validator was added yet. Frontend release: `/var/www/fairyteller/releases/20260710-length-target-19500-codex`; n8n backup: `/root/fairyteller-n8n-exports/20260710-110039Z-length-target-before`; import: `/root/fairyteller-n8n-imports/20260710-110039Z-length-target-19500`; after-export: `/root/fairyteller-n8n-exports/20260710-110039Z-length-target-after`. Verified workflow JSON and Code-node syntax, targeted ESLint, `git diff --check`, `npm run build`, active published workflow exports with `19500` and without the old fallback, n8n health, active frontend symlink, and production `/` and `/create` responses.

- Deployed deterministic uniform story pagination for PDF books. New/no-explicit-mode renders now default to `uniform`: the renderer joins each chapter's generated blocks, preserves the exact normalized text, reflows paragraph/sentence boundaries across the existing `[4, 4, 6, 6, 5]` page map, and chooses one book-wide size from `10.5`, `10.25`, or `10` pt. It never falls below 10 pt; an outlier that still overflows fails with the exact chapter instead of silently shrinking individual pages. Added cached font-width measurement, a read-only `--text-preflight`/batch corpus mode, pagination utilization diagnostics in `render.preflight.storyFont`, the admin option `Ровный по всей книге · 10–10.5 pt`, and editor guidance that source block boundaries may differ from derived PDF pages. Production backups: `/opt/fairyteller-render/backups/fairyteller-render-pdf.20260710-045618Z-pagination-before.mjs` and `/opt/fairyteller-api/backups/fairyteller-api.mjs.20260710-045618Z-pagination-before.bak`. Verified `node --check`, targeted ESLint, `git diff --check`, `npm run build`, matching local/production hashes, API restart/health, public site health, installed-renderer default preflight, and a 50-job production corpus: 49 fit uniformly at 10-10.5 pt while the single 25k-character outlier correctly failed at the 10 pt floor. Full isolated render of prior problem job `ft_1783523896859_zg7py7` produced PDF 1.7 with embedded fonts, 41 combined/40 interior pages, `noTextTruncation=true`, and a uniform 10 pt across all 25 story pages; rendered page PNGs were visually checked for drop caps, dense text, dialogue/page transitions, and final-chapter layout.

- Deployed the follow-up systemic fix for admin PDF font-size changes. Manual story font options now mean a maximum size (`Крупный · до 11 pt`, `Обычный · до 10.5 pt`, `Компактный · до 10 pt`, `Мелкий · до 9.5 pt`) rather than an unshrinkable fixed size, so long pages safely autoshrink instead of failing preflight. The renderer records `requestedSizePt`, `minAppliedSizePt`, and `maxAppliedSizePt`; the admin editor displays the actual applied range. Admin render queueing now preserves a pending rerender request while a render is already running, so rapid repeated saves do not drop the latest font/text changes. Production backups before deploy: `/opt/fairyteller-api/backups/fairyteller-api.mjs.20260710-040401Z-font-cap-queue-before.bak` and `/opt/fairyteller-render/backups/fairyteller-render-pdf.20260710-040401Z-font-cap-queue-before.mjs`. Verified local `node --check`, targeted ESLint, `git diff --check`, `npm run build`, production `node --check`, matching deployed hashes, API restart/health, production editor HTML, and active renderer smoke on a `/tmp` copy of `ft_1783523896859_zg7py7` where `large` succeeded with `minAppliedSizePt=9` and `maxAppliedSizePt=11`.

### 2026-07-09

- Deployed systemic PDF font-size preflight diagnostics for admin rebuilds. The renderer now reports the exact overflowing story text location and fit threshold instead of an uncaught stack trace, e.g. `chapter 2, block 4, page 15, mode small, selected 9.5 pt, needs 9 pt or smaller`; the Job API preserves the previous successful render metadata while a rebuild is generating/failed, and the admin editor labels stale PDF links as the previous successful PDF after failures. The editor rebuild button is now `Сохранить все и пересобрать PDF`. Production backups before deploy: `/opt/fairyteller-api/backups/fairyteller-api.mjs.20260709-145505Z-render-preflight-before.bak` and `/opt/fairyteller-render/backups/fairyteller-render-pdf.20260709-145505Z-render-preflight-before.mjs`. Verified local `node --check`, targeted ESLint, `git diff --check`, `npm run build`, isolated production-renderer matrix on a copy of `ft_1783523896859_zg7py7` (`auto` and `balanced` pass; fixed `small/compact/regular/large` fail with specific block diagnostics), production `node --check`, matching deployed hashes, API restart/health, active renderer smoke on `/tmp` copy, and admin editor HTML for `ft_1783523896859_zg7py7`. The current production job was back to `done` with a successful balanced render at `2026-07-09T14:43:17Z`.

- Deployed a Job API admin editor fix for story PDF font-size modes. The editor no longer shows the confusing separate `Выровнять шрифт и пересобрать PDF` submit button that forced `text.printLayout.storyFontMode="balanced"` and made manually selected `large`/`small` modes appear ineffective; the remaining rebuild button now saves the selected size mode, and the editor shows `Размер в последнем PDF` from `render.preflight.storyFont`. Production API backup before deploy: `/opt/fairyteller-api/backups/fairyteller-api.mjs.20260709-143102Z-story-font-mode-before.bak`. Verified production/local API and renderer sources matched before changing them, local `node --check server/fairyteller-api.mjs`, targeted ESLint, `git diff --check`, `npm run build`, production `node --check`, `fairyteller-api.service` restart/active state, local API `/healthz`, production editor HTML for `ft_1783523896859_zg7py7`, and synthetic production-renderer smoke where `small` recorded `9.5 pt` and `large` recorded `11 pt`.

- Hardened production SSH after recurring evening slowdown correlated with brute-force traffic. Installed and enabled `fail2ban` for `sshd`, disabled SSH password and keyboard-interactive authentication, kept root login key-only, reduced unauthenticated SSH retry windows, added a persistent 1 GB swap file, and deployed SSH suspicious-activity checks to `fairyteller-service-watchdog.timer`. Verified fresh key login works, password-only login is rejected with `publickey`, `fail2ban` banned the active noisy IPs, watchdog sent a new SSH activity issue, and production site/API/n8n health checks remained `200`.

- Deployed a personal generation limit override for `aleks27134@gmail.com`: 1 free generation per rolling 3-day window. The default remains 3 free generations per rolling 24-hour window for other emails. The Job API now returns `windowMs`, `periodLabel`, and `periodScopeLabel` in `daily_limit_exceeded` responses, and the frontend limit screen renders period-aware copy such as `1 сказку за 3 дня` while preserving the `сегодня` copy for default daily limits. Frontend release: `/var/www/fairyteller/releases/20260709-personal-generation-limit-codex`; API backup: `/opt/fairyteller-api/backups/fairyteller-api.mjs.20260709-131046Z-personal-limit-before.bak`. Verified production source API matched local `HEAD` before changing it, local `node --check`, targeted ESLint, `npm run build`, `git diff --check`, local smoke for the personal override and default limit, production API restart/health, `/create` `200`, active HTML uses `index-TJ85uEsy.js`, active JS contains `periodLabel`, and direct production API smoke for the override returned `429 daily_limit_exceeded` with `limit=1`, `used=1`, `periodLabel="за 3 дня"`, `windowMs=259200000`, and unchanged job count.

- Deployed a copy update for the daily generation limit screen. The frontend now says `Бесплатный лимит на сегодня исчерпан`, shows `Вы уже создали 3 сказки сегодня`, explains that the customer can choose a finished story and оформить ее в книгу, adds the manual-help paragraph for text/illustrations/story details, changes the payment CTA to `ОФОРМИТЬ КНИГУ`, and updates the support footer to `Нужна помощь с текстом или оформлением? Напишите нам в Telegram, через форму на сайте или на books@fairyteller.ru.` The Job API payload and noindex `my-books` HTML page use the same support wording, and the API clamps `used` to the free limit so legacy/high-count emails do not display values like `8/3`. Frontend release: `/var/www/fairyteller/releases/20260709-limit-copy-codex`; API backup: `/opt/fairyteller-api/backups/fairyteller-api.mjs.20260709-001952Z-limit-copy-before.bak`. Verified `node --check server/fairyteller-api.mjs`, targeted ESLint for `server/fairyteller-api.mjs` and `GenerationLimitNotice`, `npm run build`, `git diff --check`, API health, nginx reload, production `/` and `/create` `200`, and active JS contains the new limit copy and `ОФОРМИТЬ КНИГУ`.

### 2026-07-08

- Added and deployed the customer daily generation limit flow. The Job API now limits free create jobs to `FAIRYTELLER_DAILY_FREE_GENERATION_LIMIT=3` per normalized email over a rolling 24-hour window, returns `429 daily_limit_exceeded` without creating a job, and includes signed `/api/fairyteller/my-books/:token` plus payment links and the support signature (`Telegram t.me/nikita0shch`, site form, `books@fairyteller.ru`). The public my-books page is noindex/no-store and lists all jobs for that email with preview/payment actions. The `fairyteller_intake` n8n workflow now treats Job API `429` as JSON, branches before `Start Text Pipeline`, and returns the limit payload to the frontend. The `/create`, inline constructor, legacy story constructor, and romantic form render the limit screen instead of a generic submit error. Frontend release: `/var/www/fairyteller/releases/20260708-daily-email-limit-codex`; API backups: `/opt/fairyteller-api/backups/fairyteller-api.mjs.20260708-131911Z-daily-limit-before.bak` and `/opt/fairyteller-api/backups/fairyteller-api.mjs.20260708-140240Z-daily-limit-message-before.bak`; n8n backup/export: `/root/fairyteller-n8n-exports/20260708-131911Z-daily-limit-before` and `/root/fairyteller-n8n-exports/20260708-135726Z-daily-limit-after`. Verified `node --check server/fairyteller-api.mjs`, targeted ESLint for touched server/frontend files, `npm run build`, `git diff --check`, n8n workflow JSON validation, and local API smoke: three jobs created for one email, fourth request returned `429`, only three job directories existed, and the my-books link rendered three cards with payment/support links. Production smoke verified API and n8n health, active frontend release, `/` and `/create` `200`, active JS contains the limit UI and `daily_limit_exceeded`, invalid my-books token returns `403`, and the active n8n export contains `Generation Limit Reached?` / `Respond 429 Limit Reached`. No production customer email had three jobs in the last 24 hours, so the public webhook `429` path was not exercised against real data.

### 2026-07-07

- Deployed frontend release `/var/www/fairyteller/releases/20260707-photo-examples-marquee-speed-codex` for the `/podarok/skazka-po-foto` examples carousel speed tweak. Increased `PHOTO_TALE_EXAMPLE_AUTO_SCROLL_SPEED` from `0.004` to `0.005` so the textless draggable photo marquee moves slightly faster while preserving the same drag/manual scroll behavior. Verified targeted ESLint for `PhotoFairyTaleLanding`, `git diff --check`, `npm run build`, nginx config/reload, production `/`, `/podarok/skazka-po-foto`, and `/create` `200`, active JS contains `.005`, and active production assets contain no ` 2` duplicate files.

- Deployed a Job API update for the protected manual mail form at `/api/fairyteller/books/mail`. Operators can now write the message and footer/signature with a limited safe HTML tag set for links/basic formatting, add or remove up to six CTA buttons, and send emails with a matching plain-text fallback and audit entries that record button metadata. Production API backup before deploy: `/opt/fairyteller-api/fairyteller-api.mjs.20260707-0042-admin-mail-html.bak`. Verified local `node --check`, `git diff --check`, targeted ESLint for `server/fairyteller-api.mjs`, local GET/POST smoke including HTML and no-button cases, Playwright desktop/mobile form checks with no mobile horizontal overflow, production `node --check`, `fairyteller-api.service` restart/active state, API health, public unauthenticated login page, and authenticated production form HTML containing the new controls. `npm run lint` still fails on pre-existing unrelated frontend lint issues in shadcn UI files, `useScrollReveal.ts`, `BlogAdmin.tsx`, `NarutoStory.tsx`, and `tailwind.config.ts`.

### 2026-07-06

- Deployed Fairyteller dialogue typography repair to production n8n and renderer after inspecting job `ft_1783328471690_elzdmo`, where the generated text had dash dialogue remapped into broken PDF paragraphs such as orphaned `— крикнула Женя` lines and narration glued to a dialogue paragraph. Updated `fairyteller_text` and `fairyteller_full_text` normalizers to distinguish author attributions from new dialogue turns, split narrative actions after dialogue into non-dash paragraphs, preserve the same normalization for chapter 1 when `fairyteller_full_text` assembles the complete book, and added the same repair pass to `/opt/fairyteller-render/fairyteller-render-pdf.mjs` for old artifacts. Production backup before import: `/root/fairyteller-n8n-exports/20260706-134709Z-dialogue-typography-before`; renderer backup: `/opt/fairyteller-render/backups/fairyteller-render-pdf.20260706-134709Z-dialogue-typography-before.mjs`; import: `/root/fairyteller-n8n-imports/20260706-134709Z-dialogue-typography`; after-export: `/root/fairyteller-n8n-exports/20260706-134709Z-dialogue-typography-after`. Verified workflow code syntax, renderer `node --check`, n8n/API health, active published workflow exports, and re-rendered `ft_1783328471690_elzdmo` at `2026-07-06T13:58:36.683Z` with `noTextTruncation=true`; `pdftotext` found `orphan_attribution 0`, and the known `Илья обернулся` line is now separated from the preceding dialogue by a blank paragraph break.

- Deployed Fairyteller image aspect-ratio controls to production n8n. Updated `fairyteller_visuals` and `fairyteller_full_visuals` so chapter image Gemini requests include `imageConfig.aspectRatio="1:1"`, and updated `fairyteller_cover` so cover Gemini requests include `imageConfig.aspectRatio="3:2"` plus generated PNG/JPEG dimension validation before accepting the cover image. This should prevent portrait cover art from being contain-fitted into the wide front-cover frame with empty side fields while keeping the renderer's no-head-crop `contain` behavior. Production backup before import: `/root/fairyteller-n8n-exports/20260706-011931Z-image-aspect-before`; import: `/root/fairyteller-n8n-imports/20260706-012452Z-image-aspect`; after-export: `/root/fairyteller-n8n-exports/20260706-012715Z-image-aspect-after`. Verified `gemini-2.5-flash-image` accepts `imageConfig.aspectRatio="3:2"` and returns `1248x832`, parsed all updated n8n code nodes, published/restarted `fairyteller_visuals`, `fairyteller_full_visuals`, and `fairyteller_cover`, confirmed all three remain `active: true`, and checked n8n/API health endpoints.

- Deployed frontend release `/var/www/fairyteller/releases/20260706-constructor-quick-wins-codex` for constructor quick wins. The homepage process block now follows the real flow (`Выберите жанр` -> hero/details -> preview -> print), the constructor section title is `Конструктор книги`, step 1 is `Шаг 1 из 3. Выберите жанр и добавьте детали`, location/detail fields are visually tied to the first step, hero step defaults to one required `Главный герой` with an optional `Хотите добавить ещё героя?` block, hero-name validation blocks the style step with an inline error, final submit shows a full missing-action list, the `minibrick` public style label is now `Блоки`, and the new funnel goals are emitted in addition to existing `ft_*` goals: `constructor_cta_clicked`, `constructor_first_field_started`, `genre_selected`, `hero_required_completed`, `style_step_reached`, `preview_submit_clicked`, and `preview_submit_success`. Verified `npm run build`, Playwright desktop constructor flow, Playwright mobile `/create` viewport, nginx config/reload, active symlink, production `/`, `/create`, and `/podarok/skazka-po-foto` `200`, and active JS contains the new constructor copy/errors/style label. `npm run lint` still fails on pre-existing unrelated issues in shadcn UI files, `useScrollReveal.ts`, `BlogAdmin.tsx`, `NarutoStory.tsx`, and `tailwind.config.ts`.

- Deployed frontend release `/var/www/fairyteller/releases/20260706-constructor-focus-dim-codex` for the constructor focus/copy follow-up. The constructor section now uses the shorter title `Конструктор`, the first genre legend is `Выберите жанр и добавьте детали` without `Шаг 1 из 3`, the first-step detail helper is `Укажите место действия и важную деталь, чтобы создать историю`, and the breathing hint bulb is click-toggleable without layout movement. Interacting with the constructor now dims the rest of the page; clicking outside the constructor clears the dimming and active hint focus. Verified `npm run build`, targeted ESLint for `ConstructorHint`, `FairytellerInlineConstructor`, and `DesignTest`, `git diff --check`, Playwright `/create` text/hint/dimming/outside-click behavior, nginx config/reload, active symlink, production `/`, `/create`, and `/podarok/skazka-po-foto` `200`, active JS contains the new legend/detail/dim layer strings, and active JS contains zero `Шаг 1 из 3` matches.

- Deployed frontend release `/var/www/fairyteller/releases/20260706-photo-landing-process-order-codex` for the `/podarok/skazka-po-foto` landing order update. The `Как создать книгу.` process block was moved above the constructor and renamed `Как это работает.`, while `Не шаблон. Личная история.` moved down to the former process-block position before the CTA strip. Verified `npm run build`, targeted ESLint for `PhotoFairyTaleLanding`, `git diff --check`, Playwright desktop/mobile section-order checks, nginx config/reload, production `/podarok/skazka-po-foto`, `/podarok/skazka-po-foto#process`, and `/` `200`, and the active `PhotoFairyTaleLanding` chunk contains `Как это работает` and no `Как создать книгу`.

- Deployed frontend release `/var/www/fairyteller/releases/20260706-photo-landing-examples-carousel-clean-codex` for the `/podarok/skazka-po-foto` examples update. Added a 10-image optimized WebP carousel directly after `Как это работает.`, moved the `#examples` anchor to this carousel so the `Смотреть примеры` CTA lands on it, renamed the former examples/details block anchor to `#details`, and added the FAQ item `Как создается история?` to both the photo landing and the homepage FAQ plus generated static SEO HTML. Verified targeted ESLint for `PhotoFairyTaleLanding`, `DesignTest`, and `generate-seo-pages`, `git diff --check`, `npm run build` from a clean `dist`, Playwright desktop/mobile section-order and no-overflow checks, nginx config/reload, production `/`, `/podarok/skazka-po-foto`, and `/create` `200`, static HTML FAQ presence on `/` and `/podarok/skazka-po-foto`, active JS contains `Примеры книг`, and active production assets contain no ` 2` duplicate files.

- Deployed frontend release `/var/www/fairyteller/releases/20260706-photo-examples-marquee-drag-codex` for the `/podarok/skazka-po-foto` examples carousel follow-up. Removed all visible heading/body/card-caption text from the `#examples` block, converted it into a long 10-photo marquee using three repeated cycles for seamless scrolling, added slow requestAnimationFrame auto-scroll with fractional position accumulation, and enabled pointer drag so visitors can grab the carousel and scroll it manually in either direction. Verified targeted ESLint for `PhotoFairyTaleLanding`, `git diff --check`, `npm run build`, Playwright desktop/mobile checks for 30 rendered cards, no visible section text, slow auto-scroll, manual drag movement, and no horizontal page overflow, nginx config/reload, production `/`, `/podarok/skazka-po-foto`, and `/create` `200`, active JS contains the drag carousel classes, active JS no longer contains `Примеры книг`, and active production assets contain no ` 2` duplicate files.

### 2026-07-05

- Deployed Fairyteller dialogue normalization hotfix to production n8n after reviewing test job `ft_1783235006070_982sij`, where generated prose still mixed Russian dash dialogue with quoted thoughts, remembered phrases, labels, and indirect speech. Updated `fairyteller_text` and `fairyteller_full_text` prompts to remove guillemet-wrapped direct-speech examples and forbid quote marks inside `textBlocks`; added normalizer cleanup before artifact writes to strip generated `«»`/double quotes from story text, paraphrase common `сказали друг другу "люблю"` cases, and insert paragraph breaks before inline dialogue turns. Production backup before import: `/root/fairyteller-n8n-exports/20260705-092523Z-dialogue-normalizer-before`; import: `/root/fairyteller-n8n-imports/20260705-092550Z-dialogue-normalizer`; after-export: `/root/fairyteller-n8n-exports/20260705-092842Z-dialogue-normalizer-after`. Reactivated and published both workflows, restarted `baku-n8n-docker`, and verified post-restart exports show `active: true`, `gemini-2.5-pro`, `function normalizeGeneratedStoryText`, no old `«— Реплика` examples, n8n `/healthz` ok, and Job API `/healthz` ok.

- Deployed Fairyteller text-quality model and style update to production n8n. Updated `fairyteller_text` and `fairyteller_full_text` so text generation uses `gemini-2.5-pro` instead of `gemini-2.5-flash`; visual workflows remain on `gemini-2.5-flash-image`. Added text prompt guardrails for Russian direct-speech punctuation, no indirect speech disguised as quotations, no action-only dialogue lines, dialogue capped at 30% of chapter/page block text, and less decorative atmospheric/sensory filler. Verified `gemini-2.5-pro` is available and returns `200` through the production n8n `GEMINI_API_KEY`. Production backup before import: `/root/fairyteller-n8n-exports/20260705-064030Z-text-pro-style-before`; import: `/root/fairyteller-n8n-imports/20260705-064030Z-text-pro-style`; after-export: `/root/fairyteller-n8n-exports/20260705-064030Z-text-pro-style-after`. Reactivated both workflows after import, restarted `baku-n8n-docker`, and verified post-restart exports show `active: true`, `gemini-2.5-pro`, no `gemini-2.5-flash` in the text workflows, n8n `/healthz` returned ok, local API `/healthz` returned ok, and production `/` returned `200`.

### 2026-07-04

- Deployed Fairyteller dramaturgy guardrails to production n8n and renderer. Updated `fairyteller_text`, `fairyteller_full_text`, and `fairyteller_cover` so reader-facing book blurbs are separated from cover-art briefs, internal world labels are sanitized from story text, one-hero orders stay in a solo-protagonist arc, and cover prompts keep people fully in frame with neutral/friendly expressions. Updated `/opt/fairyteller-render/fairyteller-render-pdf.mjs` so the front-cover image frame uses contain-fit instead of crop-cover and PDF summary slots prefer `readerBlurb`. Production backups: `/root/fairyteller-n8n-exports/20260704-001732Z-dramaturgy-before` and `/opt/fairyteller-render/backups/fairyteller-render-pdf.20260704-001845Z-dramaturgy-before.mjs`; import: `/root/fairyteller-n8n-imports/20260704-002055Z-dramaturgy`; after-export: `/root/fairyteller-n8n-exports/20260704-002247Z-dramaturgy-after`. Verified published workflow exports have `active: true`, n8n restarted and reactivated `fairyteller_text`, `fairyteller_full_text`, and `fairyteller_cover`, production site returned `200`, local API health returned `200`, and server renderer passed `node --check`.

- Deployed Fairyteller likeness/blurb hotfix to production n8n after inspecting test job `ft_1783125327969_54mlzm`. The job showed that the original uploaded photo is not persisted after intake; downstream images inherit likeness from generated hero reference cards, and later chapter/cover attempts had fallen back to `safe_environment_no_references`, likely aggravated by risky raw artifact wording (`Ледоруб`). Updated `fairyteller_visuals`, `fairyteller_full_visuals`, and `fairyteller_cover` to treat uploaded-photo-derived reference cards as the strongest identity source, forbid beautifying/de-aging/generic-face drift, add a reference-card safety retry before no-reference fallback, and map `ледоруб` to a safer mountaineering-object wording for prompts. Updated `fairyteller_text` and `fairyteller_full_text` so `readerBlurb` targets `140-260` characters with a hard normalizer cap instead of the previous `280-520` prompt and 700-character fallback. Production backup before import: `/root/fairyteller-n8n-exports/20260704-005158Z-likeness-blurb-before`; import: `/root/fairyteller-n8n-imports/20260704-005321Z-likeness-blurb`; after-export: `/root/fairyteller-n8n-exports/20260704-005603Z-likeness-blurb-after`. Verified all five workflows are active after `baku-n8n-docker` restart and production n8n/site/API returned `200`.

- Deployed frontend/API release `/var/www/fairyteller/releases/20260704-chapter-paywall-detail-codex`. Public Russian copy now uses `Важная деталь` instead of `Артефакт` across constructors, landing/public page text, legal pages, and blog content while preserving the internal API field name `artifact`. The unpaid `/book/:jobId` preview no longer locks the end of the book: `paywall-preview.pdf` cache version is now `paywall-preview-chapter-breaks-v1`, `/sample-pages` exposes all 42 preview pages, and `progress.chapterEndPages` drives inline non-blocking payment prompts after chapters 1-5. API backup before deploy: `/opt/fairyteller-api/fairyteller-api.mjs.20260704-011908Z-chapter-paywall-detail.bak`. Verified `npm run build`, production `fairyteller-api.service` active, local VPS health `http://127.0.0.1:3099/healthz`, active release symlink, production `/`, `/create`, `/podarok/skazka-po-foto`, and `/book/ft_1781192146034_f37ggn` return `200`, `dist`/production HTML contain no Russian `артефакт`, `/sample-pages` for `ft_1783125327969_54mlzm` returns `42/42` pages with chapter end pages `[10,16,24,32,39]`, and Playwright snapshot shows the five `Глава N дочитана` prompts followed by subsequent pages.

- Deployed frontend release `/var/www/fairyteller/releases/20260704-mobile-header-menu-codex` to remove the inert mobile hamburger button from the homepage/create header and shared gift-landing header. The release was built from clean `HEAD` with only `src/pages/DesignTest.tsx` and `src/components/LandingHeader.tsx` patched, avoiding unrelated local `/book/:jobId` changes. Verified targeted `npx eslint src/pages/DesignTest.tsx src/components/LandingHeader.tsx`, `npm run build`, nginx config, active symlink, production `/`, `/create`, and `/podarok/skazka-po-foto` `200`, active JS `open-menu=0`, and Playwright mobile checks on `/` and `/podarok/skazka-po-foto` with `menuButtons: 0`. The only console error was local DNS resolution for `mc.yandex.ru` in the test environment.

- Adjusted the unpaid `/book/:jobId` chapter-paywall UI after review: removed the inline chapter cards and restored the previous floating paywall card/strip with the original copy (`А дальше?`, `Перед вами — почти готовая книга...`, refund/rebuild small print). The same `progress.chapterEndPages` sentinels now trigger that floating card after each chapter while all 42 preview pages remain scrollable. Final production release is `/var/www/fairyteller/releases/20260704-mobile-header-floating-paywall-codex`, rebuilt after the active production symlink advanced to `/var/www/fairyteller/releases/20260704-mobile-header-menu-codex` so the active header state was preserved while the source change stayed scoped to `src/pages/Book.tsx`. Verified `npx eslint src/pages/Book.tsx`, `npm run build`, nginx config/reload, production `/book/ft_1783125327969_54mlzm` `200`, `/sample-pages` returns `42/42` pages with chapter end pages `[10,16,24,32,39]`, production `Book` chunk contains the restored floating copy and no `book-chapter-paywall-note`/`дочитана`, and Playwright showed the card hidden at page 1, visible after chapter 1, and mouse-wheel scrolling continuing to subsequent pages.

- Deployed frontend/API release `/var/www/fairyteller/releases/20260704-light-preview-sticky-paywall-codex` to smooth the mobile unpaid `/book/:jobId` PDF preview. `src/pages/Book.tsx` now virtualizes preview images by loading only a small window around the current page, uses lightweight placeholders for the remaining 42-page layout, keeps the floating payment card visible after it first appears, proxies wheel/touch scroll from the card/CTA to the page underneath, and updates the copy to `Нравится история?` / `Перед вами — готовый макет...`. `server/fairyteller-api.mjs` now uses preview page cache version `paywall-preview-pages-light-v1` with `pdftoppm -r 72 -scale-to 760 -jpegopt quality=62,optimize=y`, so existing jobs regenerate lighter JPEGs and page URLs include the cache-busting `?v=paywall-preview-pages-light-v1`. API backup before deploy: `/opt/fairyteller-api/fairyteller-api.mjs.20260704-0600Z-light-preview-paywall.bak`. Verified `npx eslint src/pages/Book.tsx`, `node --check server/fairyteller-api.mjs`, `npm run build`, production API health, nginx config/reload, active symlink, production `/book/ft_1783125327969_54mlzm` `200`, `/sample-pages` returns `42/42` pages with cache-busted URLs and chapter end pages `[10,16,24,32,39]`, regenerated page images dropped from `3.5M` to `2.3M`, and Playwright mobile `390x844` loaded only 5 initial images/37 placeholders, showed the sticky card after chapter 1, kept it visible while scrolling over the card from `bodyTop 3534` to `4934`, and reported only the known `mc.yandex.ru` DNS error.

- Deployed frontend release `/var/www/fairyteller/releases/20260704-paywall-print-check-copy-codex` to update the unpaid `/book/:jobId` floating paywall small print. The old refund/rebuild line was replaced with `Перед печатью наша команда проверит макет и свяжется с вами, чтобы согласовать финальную версию.` while preserving the sticky card behavior and lightweight preview image window from the prior release. Verified `npx eslint src/pages/Book.tsx`, `npm run build`, nginx config/reload, production `/book/ft_1783125327969_54mlzm` `200`, and production `Book` chunk contains the new small print with no old `Если вам не понравится результат` copy.

- Deployed frontend release `/var/www/fairyteller/releases/20260704-collapsed-paywall-expand-only-codex` so the collapsed unpaid `/book/:jobId` paywall strip has exactly one behavior: any click/tap on the strip expands the full card and never starts checkout directly. The collapsed visual still shows `Почти готово` and `Оплатить — 3 500 ₽`, but it is rendered as a single button instead of a button plus payment link. Verified `npx eslint src/pages/Book.tsx`, `npm run build`, nginx config/reload, production `/book/ft_1783125327969_54mlzm` `200`, production `Book` chunk has no collapsed-strip payment anchor, and Playwright confirmed the collapsed strip expands without navigation.

- Deployed frontend release `/var/www/fairyteller/releases/20260704-pay-address-pvz-copy-codex` to update the `/pay` delivery form label from `Адрес доставки` to `Адрес доставки (подберем ближайший ПВЗ)`. Verified `npx eslint src/pages/Print.tsx`, `npm run build`, nginx config/reload, production `/pay` `200`, and production `Print` chunk contains the new label.

### 2026-07-02

- Deployed frontend release `/var/www/fairyteller/releases/20260702-photo-romance-codex` to restore the `Романтическая история` option in the `/podarok/skazka-po-foto` inline constructor. The page now passes `romance` alongside `adventure`, `hogwarts`, `fantasy`, and `cyber` to `FairytellerInlineConstructor`. Verified `npx eslint src/pages/PhotoFairyTaleLanding.tsx`, `npm run build`, generated `PhotoFairyTaleLanding` chunk containing `availableWorldIds:["romance","adventure","hogwarts","fantasy","cyber"]`, nginx config, production `/podarok/skazka-po-foto` `200`, active production symlink, and no stray Finder-copy `* 2.*` / `* 3.*` artifacts in the active release.

- Deployed frontend release `/var/www/fairyteller/releases/20260702-promo-free-delivery-codex` with the top promo strip updated across the shared landing header, homepage/create header, and static `/delivery/` page to include `бесплатная доставка`. Verified `npm run build`, targeted `npx eslint src/components/LandingHeader.tsx src/pages/DesignTest.tsx`, nginx config, active production symlink, production `/` `200`, `/delivery/` containing the new promo copy, and the active JS bundle containing the updated banner text.

- Deployed frontend release `/var/www/fairyteller/releases/20260702-anchor-cta-delivery-codex` from the current dirty workbench state that matched the restored production `/podarok/skazka-po-foto` landing, avoiding the earlier clean-HEAD rollback issue. The release fixes React hash-anchor navigation globally through `HashAnchorScroll`, updates `/podarok/skazka-po-foto` process copy, adds the black `Создать свою книгу` CTA strip between `Как создать книгу` and `Вопросы и ответы`, and changes `/delivery/` intro copy to `Мы осуществляем бесплатную доставку...`. Verified `npm run build`, targeted `npx eslint src/App.tsx src/pages/PhotoFairyTaleLanding.tsx`, local preview anchor smoke across `/`, `/create`, gift landings, `/delivery/`, desktop/mobile CTA layout, nginx config, production assets `index-DuxUeRki.js` / `index-JpSJBBia.js` / `index-nT5dKdLZ.css`, production `/podarok/skazka-po-foto#process` and `#create` landing at ~96px offset, CTA/process texts, and `/delivery/#delivery-table` with the new free-delivery copy. The only console error during terminal Playwright smoke was `mc.yandex.ru` DNS resolution in the test environment.

- Rolled production back from `/var/www/fairyteller/releases/20260702-anchor-scroll-codex` to `/var/www/fairyteller/releases/20260701-style-previews-color-codex` after the anchor-scroll release was built from clean Git `HEAD` instead of the full current production/workbench state and regressed the `/podarok/skazka-po-foto` landing. Verified nginx config, active symlink, `200` for `/podarok/skazka-po-foto#create`, and restored active assets `index-D-gZyCvQ.js` / `index-Dbvv18A8.js` / `index-nT5dKdLZ.css`. The hash-anchor fix remains only as a local source change until it can be rebuilt from the correct current source state.

- Deployed frontend release `/var/www/fairyteller/releases/20260702-anchor-scroll-codex` to fix direct hash-anchor navigation on React-rendered pages. The shared `HashAnchorScroll` component in `src/App.tsx` retries `location.hash` target lookup after lazy route hydration and scrolls the section into view using the existing `scroll-mt-24` offsets. Browser smoke reproduced the production bug on `/podarok/skazka-po-foto#process` before deploy (`#process` stayed ~3154px below the viewport with the page at top), then verified the deployed page lands with `#process` ~96px from the viewport top. Verified `npm run build`, targeted `npx eslint src/App.tsx`, nginx config test, and active assets `index-DcFMvgSx.js` / `index-DflL6eBv.js` / `index-nT5dKdLZ.css`.

### 2026-07-01

- Deployed frontend release `/var/www/fairyteller/releases/20260701-legal-pages-codex` with three public legal pages: `/oferta`, `/agreement`, and `/policy`. The pages are React routes with generated static SEO HTML so nginx can serve the direct URLs through the existing `$uri/index.html` rule. Footer links were added across the homepage/create page, gift landings, blog pages, legacy footers, `/book-preview`, and static `/delivery/`; personal-data consent text in all constructors now links to `/policy`. The legal copy uses seller/operator details `Самозанятый Щуренков Никита Анатольевич`, ИНН `504311104973`, and contact `books@fairyteller.ru`; it intentionally avoids publishing the residential address and instead uses an email-first correspondence note pending legal review. Smoke verified `npm run build`, generated `dist/{oferta,agreement,policy}/index.html`, production `200` for all three legal URLs, sitemap inclusion, active frontend assets `index-DHtJbX8j.js` / `index-DZ0LF3tk.css`, and active `fairyteller-api.service`.
- Added the constructor hint bulb in source commit `b460cb4` and verified it in the active production frontend release `/var/www/fairyteller/releases/20260701-mobile-gallery-fix-codex`. The shared `ConstructorHint` component renders a gently breathing lightbulb next to the genre/plot legend, opens on hover/focus/click, and explains that more personal details produce a stronger story; it is wired into the homepage constructor, reusable inline constructors, and locked romantic constructor variants. Production smoke verified the active `index-DjPjXley.js` contains the new short hint copy.
- Deployed frontend release `/var/www/fairyteller/releases/20260701-constructor-status-copy-codex` for constructor status copy. The homepage/create constructor and shared inline constructor now show `осталось MM:SS` instead of `примерно MM:SS` during generation, and the ready state adds `Оплатите заказ, чтобы получить бумажную версию с доставкой по РФ.` above the payment CTA. The same checkout surface keeps the short `Оплатить` CTA and routes payment through `/pay?jobId=...`. Smoke verified `npm run build`, production `/` returns `200`, active assets `index-D50Fnq4K.js` / `index-nT5dKdLZ.css`, both new phrases exist in the active JS bundle, and stray Finder-copy artifacts such as `* 2.*` / `* 3.*` were removed from the deployed release after transfer.
- Deployed frontend release `/var/www/fairyteller/releases/20260701-style-previews-color-codex` for the constructor style selector. The homepage/create constructor and shared inline constructor no longer apply the grayscale hover mask to illustration-style thumbnails, so all style previews render in color immediately. Smoke verified `npm run build`, `/create` returns `200`, the active JS no longer contains the old `grayscale transition group-hover:grayscale-0` class for `aspect-[5/3]` style cards, and stray Finder-copy artifacts were removed from `dist` and the deployed release.

### 2026-06-03

- Deployed try-before-buy paywall release `/var/www/fairyteller/releases/20260611-1519-codex-paywall` on June 11, 2026. Public `full-text.json` and PDF downloads now require a paid access token, while the public sample endpoint exposes only chapters 1-2 plus the title, illustration, and short teaser for chapter 3. The generated-book UI shows this sample with a blur/fade paywall and starts server-side YooKassa checkout through `POST /api/fairyteller/jobs/:jobId/checkout`. The new `/book/:jobId` route handles pending payment returns, magic-link access, full-text display, PDF download with `access`, and resend-link requests. The Job API stores `payment.json`, verifies YooKassa succeeded notifications by fetching the payment server-to-server, issues 30-day access tokens, and sends a dedicated purchase-access Resend email. Production was updated with YooKassa env, webhook URL `https://fairyteller.ru/api/fairyteller/webhook/yookassa`, and nginx `/book/` SPA routing. Smoke checks passed: `/book/ft_smoke_paywall` returns the React app, empty YooKassa webhook returns `200`, unauthenticated `book.pdf` returns `403 Paid access required`, sample for `ft_1779609291175_25otqb` returns 2 free chapters plus locked chapter 3, and checkout returns a YooKassa `paymentId` plus `confirmationUrl`.
- Applied a production paywall hotfix after test job `ft_1781192146034_f37ggn` exposed old/direct PDF links opening a raw `Paid access required` JSON response. Unauthenticated PDF file requests without an `access` token now `302` redirect to `/book/:jobId` instead of showing JSON, and protected operator/admin PDF lists now append an admin access token so PDFs still open from the admin surface. Smoke checks: direct `preview.pdf` redirects to `/book/ft_1781192146034_f37ggn`, admin-token `preview.pdf` returns `200 application/pdf`, admin book list contains tokenized PDF links, `/sample` returns 2 free chapters plus locked chapter 3, and checkout returns a YooKassa `paymentId` plus `confirmationUrl`. Purchase email is still sent only after `payment.succeeded`; unpaid test jobs do not receive the magic-link email.
- Deployed frontend hotfix release `/var/www/fairyteller/releases/20260611-2339-codex-book-paywall-hotfix` after `/book/:jobId` without an access token still showed "Ссылка недействительна". The `/book` page now fetches the public sample and shows the same try-before-buy paywall when a job is unpaid and no `access` token is present. Smoke checks: active HTML loads `index-CAAk2ZnM.js`, Book chunk contains "Бесплатный фрагмент" and "Остальные 3 главы", direct `preview.pdf` still redirects to `/book/ft_1781192146034_f37ggn`, and `fairyteller-api.service` remains active.
- Deployed frontend polish release `/var/www/fairyteller/releases/20260612-0015-codex-pay-page-preview-polish` on June 12, 2026. The unpaid `/book/:jobId` view now presents the sample as PDF-like book pages with the first two chapters, the locked third chapter title/text teaser, illustration, and a refund/rebuild reassurance, while its CTA goes to `/pay?jobId=...&pdf=...` instead of directly starting the server-side YooKassa checkout. The `/pay` page now accepts `jobId`, normalizes relative PDF paths to `https://fairyteller.ru/...`, shows the book number to the customer, and sends `jobId`/return URLs through the SimplePay form. Smoke checks: server build passed, active release contains the new Book and Print chunks, unauthenticated `preview.pdf` still redirects to `/book/ft_1781192146034_f37ggn`, and the sample endpoint for that job returns 2 chapters plus the locked chapter image. Note: the earlier automatic purchase-access email is tied to the server-side `/checkout` + YooKassa webhook flow; after moving the CTA to SimplePay `/pay`, payment-confirmation/email automation still needs a dedicated SimplePay notification alignment before relying on it for hands-off fulfillment.
- Reworked the unpaid book experience again in release `/var/www/fairyteller/releases/20260612-0020-codex-real-pdf-paywall` after the HTML imitation did not match the generated PDF. The Job API now exposes unauthenticated `/api/fairyteller/jobs/:jobId/sample.pdf`, built from the real `preview.pdf` by copying the cover/interior pages through the chapter-3 teaser area and caching it as `files/paywall-preview.pdf`; the full `preview.pdf`/`book.pdf` remain protected and still redirect to `/book/:jobId` without paid access. The unpaid `/book/:jobId` page embeds this real PDF sample in the browser PDF viewer and places a blurred fixed paywall CTA to `/pay?jobId=...&pdf=...` over the bottom of the viewport. Smoke checks for `ft_1781192146034_f37ggn`: `/sample.pdf` returns `200 application/pdf` with `%PDF-1.7`, the cached `paywall-preview.pdf` exists on production, direct `/files/preview.pdf` still returns `302 /book/ft_1781192146034_f37ggn`, `fairyteller-api.service` is active, and the active Book chunk contains `sample.pdf#view=FitH`, `book-pdf-frame`, and "Продолжение уже готово".
- Deployed performance/UX paywall release `/var/www/fairyteller/releases/20260612-0040-codex-light-preview-pages`. Installed `poppler-utils` on the production VPS and added public `/api/fairyteller/jobs/:jobId/sample-pages` plus `/sample-pages/:fileName` endpoints. The API renders the cached `paywall-preview.pdf` into `files/paywall-preview-pages/page-XX.jpg` via `pdftoppm` at 920px/quality 72, then serves a lightweight JSON list and lazy-loadable JPEG pages instead of embedding the heavy browser PDF viewer. For smoke job `ft_1781192146034_f37ggn`, the old `paywall-preview.pdf` is 15 MB while the new JPEG page cache is 1.8 MB across 18 pages; `/sample-pages` returned 18 pages, `page-01.jpg` returned `200 image/jpeg` at 121 KB, and `fairyteller-api.service` stayed active. The frontend now uses these image pages on the light `#fffaf0` background, shows the paywall only when the reader reaches the bottom, hides it when scrolling upward, and updates the guarantee copy to "Если вам не понравится результат — бесплатно соберем для вас несколько вариантов книги или вернем деньги."
- Deployed SimplePay fulfillment release `/var/www/fairyteller/releases/20260612-0050-codex-simplepay-mail-flow`. The `/pay` SimplePay form now submits `jobId`, `customerNumber`, and `orderNumber` so YooKassa can return the job id in HTTP payment notifications. The Job API now accepts legacy YooKassa form notifications at `POST /api/fairyteller/webhook/yookassa-form`, validates the MD5 signature with `FAIRYTELLER_YOOKASSA_SHOP_PASSWORD` / `YOOKASSA_SHOP_PASSWORD`, acknowledges `checkOrder`, and on `paymentAviso` marks the job as paid, creates/refreshes the access token, writes `payment.json`, and sends the existing purchase-access email with the full-book magic link via Resend. Smoke checks: API `node --check` passed, service stayed active, form webhook returns XML (`checkOrderResponse`) and rejects bad MD5 with `code="1"`, the active `/pay` bundle includes `customerNumber`/`orderNumber`, and the success modal says the email will be sent after YooKassa confirms the payment. Activation note: production currently has no `FAIRYTELLER_YOOKASSA_SHOP_PASSWORD` configured, so real SimplePay notifications will be rejected until the YooKassa NVP/MD5 shop password is added to `/etc/fairyteller/api.env` and the SimplePay HTTP notification URL is set to `https://fairyteller.ru/api/fairyteller/webhook/yookassa-form`.
- Switched `/pay` away from legacy SimplePay/MD5 to API-created YooKassa payments in release `/var/www/fairyteller/releases/20260612-0115-codex-api-checkout-pay`. The `/pay` page still collects email, phone, recipient, address, and PDF URL, but now posts them to `POST /api/fairyteller/jobs/:jobId/checkout`; the Job API creates a YooKassa v3 payment with `metadata.jobId` and customer fields, returns `confirmationUrl`, and the browser redirects to YooKassa. The existing configured `payment.succeeded` webhook at `/api/fairyteller/webhook/yookassa` is now the canonical fulfillment trigger: it verifies the payment server-to-server, marks the job paid, creates the magic-link access token, writes `payment.json`, and sends the purchase-access email. Smoke checks: `/pay` no longer contains the SimplePay action/script, active release is `/var/www/fairyteller/releases/20260612-0115-codex-api-checkout-pay`, `fairyteller-api.service` is active, `FAIRYTELLER_YOOKASSA_SECRET_KEY` is configured, and a checkout smoke for `ft_1781192146034_f37ggn` returned a present payment id plus a `yoomoney.ru` confirmation URL. Note: the previous MD5 shop-password flow is no longer required for the main purchase path.
- Re-enabled render-ready customer email on production by setting `FAIRYTELLER_SEND_RENDER_READY_EMAIL=1` in `/etc/fairyteller/api.env` and updating the completion email links so "Открыть превью" goes to `/book/:jobId` and "Оплатить заказ" goes to `/pay?jobId=...&pdf=...`. This restores the post-generation email funnel: after normal PDF render completion, customers receive the preview/paywall link and payment CTA. Manual admin re-renders still use `skipCustomerEmail: true` to avoid duplicate customer emails after operator edits. Sent a manual paid-access smoke email for `ft_1781192146034_f37ggn` to `zoowall@yandex.ru`; Resend returned `sent`, and the job `payment.json` now contains a paid access token for that test recipient through July 12, 2026.
- Prepared PageSpeed/Lighthouse performance patch from the June 3, 2026 mobile and desktop PageSpeed PDF reports for `https://fairyteller.ru/`. The reports showed mobile Performance `58` with FCP `5.7s`, LCP `6.8s`, Speed Index `11.9s`, and desktop Performance `72` with FCP `2.1s`, LCP `2.2s`, Speed Index `5.0s`; TBT and CLS were already healthy.
- Reduced first-screen network pressure on the home page by replacing the heavy 957x453 PNG logo import in `DesignTest` with a compact 260x124 WebP asset, rendering only the active hero image instead of all hidden carousel hero images, and adding explicit image dimensions plus eager/high-priority loading only for the visible hero image.
- Deferred non-critical startup work by lazy-loading the public support chat after browser idle and scheduling Yandex Metrika initialization after `window.load`/idle instead of fetching `mc.yandex.ru` during the initial render path.
- Left the large global CSS cleanup as a follow-up because `src/index.css` still contains shared legacy constructor and route-specific styles. PageSpeed flags unused CSS around `153-156 KiB`, but safe extraction should be done with visual regression coverage for the old landing, romantic, wizard, and constructor screens.

### 2026-05-23

- Added local dependency-free Fairyteller Job API.
- Added API docs and environment example.
- Added npm scripts `dev:api` and `start:api`.
- Created four n8n skeleton workflows for the migration pipeline.
- Verified local API smoke test with create/status/update/artifact/full-job flow.
- Deployed `fairyteller-api.service` to VPS and enabled it at boot.
- Added nginx proxy for `https://fairyteller.ru/api/fairyteller/`.
- Verified production create/status smoke test. Smoke job: `ft_1779512912560_fa954f24`.
- Added exact print shop requirements: PDF 1.7, embedded fonts, RGB, no protection, 41 pages, page 1 `268.5 x 136.0 mm`, pages 2-41 `136.0 x 136.0 mm`.
- Injected `FAIRYTELLER_API_TOKEN` into the Docker n8n container environment; previous container backup: `baku-n8n-docker-bak-20260523051638`.
- Connected `fairyteller_intake` to the production Job API while keeping the public intake workflow inactive.
- Connected `fairyteller_text` to Job API status updates and `text.json` artifact writing.
- Connected `fairyteller_visuals` to Job API status updates and `visuals.json` artifact writing.
- Connected `fairyteller_render_publish` to Job API status updates and `render.json` artifact writing with the print-shop page contract.
- Published internal sub-workflows `fairyteller_text`, `fairyteller_visuals`, and `fairyteller_render_publish`.
- Verified end-to-end n8n placeholder smoke test through manual intake execution. Smoke execution: `326`; smoke job: `ft_1779513917368_j7xu2m`; final public status: `done`.
- Added `/create` frontend compatibility for the new async n8n response contract: `jobId`, `statusUrl`, and public status polling in the success overlay while preserving the Make webhook as the default endpoint.
- Added `.env.example` with frontend migration variables.
- Deployed frontend release `/var/www/fairyteller/releases/20260523-053332-codex-async-status` and repointed `/var/www/fairyteller/current` to it. Production `/create` still uses the legacy Make endpoint by default.
- Replaced the `fairyteller_text` placeholder builder with an OpenAI Responses API first-chapter generator and JSON normalizer; published active version `b83a24c7-1745-4404-b07f-e34032a27c3b`.
- Verified that the current n8n `OPENAI_API_KEY` is present but invalid; real text smoke is blocked until the key is replaced.
- Added Gemini API environment variables to the Docker n8n container, recreated n8n with the existing data volume, and verified Gemini API access without printing the key.
- Switched `fairyteller_text` from OpenAI to Gemini as the primary first-chapter generator and published active version `6db34909-8976-42df-9bea-84f6d1a1ed85`.
- Verified manual intake smoke with Gemini first-chapter generation. Smoke execution: `330`; smoke job: `ft_1779515670105_344zxe`; final status: `done`; generated title: `Серебряный компас над Невой`; `text.json` contains 5 first-chapter blocks from `gemini-2.5-flash`.
- Added Job API file artifacts: authenticated `PUT /api/fairyteller/jobs/:jobId/files/:fileName` and public `GET /api/fairyteller/jobs/:jobId/files/:fileName`.
- Switched `fairyteller_visuals` from placeholder-only to real first-chapter image generation with Gemini and published active version `07a5ffc0-2d15-41f7-b33d-25b493413860`.
- Verified manual intake smoke with first-chapter text and image. Smoke execution: `334`; smoke job: `ft_1779516199604_fpktms`; final status: `done`; generated image: `chapter-1.png`, `1024 x 1024`, RGB, about `1.3 MB`.
- Observed smoke timing for `ft_1779516199604_fpktms`: first chapter ready in about `29s`, first image ready about `7s` later, total time from job creation to `done` about `36.4s`.
- Added `/create` success overlay rendering for the first-chapter preview and illustration when the n8n status API returns preview artifacts.
- Deployed frontend release `/var/www/fairyteller/releases/20260523-061016-codex-first-chapter-preview` and repointed `/var/www/fairyteller/current` to it. Production `/create` still uses the legacy Make endpoint by default.
- Published `fairyteller_intake` as an active production webhook.
- Added nginx same-origin proxy from `https://fairyteller.ru/webhook/` to Docker n8n on `127.0.0.1:5680`.
- Verified HTTPS intake smoke through `POST /webhook/fairyteller/create`. Smoke job: `ft_1779516843392_7ybt48`; final status: `done`; first chapter title: `Компас и утренний Петербург`; first image status: `ready`.
- Rebuilt and deployed production frontend with `VITE_FAIRYTELLER_CREATE_URL=/webhook/fairyteller/create`. Release: `/var/www/fairyteller/releases/20260523-061507-codex-n8n-create-switch`.
- Fixed `fairyteller_text` JSON parse failures in `Normalize First Chapter` by adding Gemini `responseSchema`, stricter JSON-string prompt rules, and normalizer repair for control characters inside strings. Published active version `730aebcf-54a5-45c2-b8e1-35b63d7aeb84`.
- Verified repair smoke through public intake. Smoke job: `ft_1779517701337_9crdia`; final status: `done`; first chapter title: `Шепот Прибоя и Древние Знаки`; first image status: `ready`.
- Added sanitized workflow export for `fairyteller_text` at `n8n/workflows/fairyteller_text.workflow.json`.
- Updated `fairyteller_intake` to preserve hero photo metadata from webhook binary uploads. Published active version `b3e41497-f707-48d2-8935-4dd68ec55f09`.
- Added a portraitizer stage to `fairyteller_visuals`: build hero reference sheet request, generate `hero-reference-sheet.png`, store it as a Job API file, and feed it into first chapter image generation. Published active version `321456b9-4c12-48d3-a922-5af94be484b5`.
- Fixed the `minibrick` visual style mapping so the Lego-like option produces brick-built minifigure/toy diorama prompts instead of falling back to the Disney-style prompt.
- Verified minibrick smoke through public intake. Smoke job: `ft_1779518387625_1duomz`; final status: `done`; files generated: `hero-reference-sheet.png` and `chapter-1.png`, both `1024 x 1024`; visual check confirmed brick/minifigure style.
- Verified photo-upload smoke through public intake with `hero1_photo`. Smoke job: `ft_1779518617754_ftw6ah`; final status: `done`; this confirmed the portraitizer can read webhook binary data before generating the hero reference sheet.
- Added sanitized workflow exports for `fairyteller_intake` and `fairyteller_visuals`.
- Replaced short n8n world labels with Make-derived world profiles for `romantic_story`, `adventure_classic`, `fantasy_epic`, `cyberpunk_dream`, and a cleaned `hogwarts_world` profile. The old `disney_light` key remains a legacy alias to `romantic_story`, and `new_year` remains an alias to `hogwarts_world` inside `fairyteller_text`.
- Updated the frontend world value from `new_year` to `hogwarts_world` in the main constructor and the wizard page.
- Deployed frontend release `/var/www/fairyteller/releases/20260523-151440-codex-hogwarts-world-key` and repointed `/var/www/fairyteller/current` to it.
- Hardened `fairyteller_text` `Normalize First Chapter` against Gemini malformed JSON: it now retries parse with comma/control-character repairs and falls back to extracting five first-chapter blocks from the raw response instead of failing the whole job.
- Synced production workflow exports for `fairyteller_intake`, `fairyteller_text`, and `fairyteller_visuals`.
- Verified public smoke with `hogwarts_world`, `chapters=4`, minibrick style, location `закрытая библиотека...`, and artifact `латунный жетон...`. Smoke job: `ft_1779520304316_ov65b1`; final status: `done`; `text.json` contains `chapterPlan` length `4`; first chapter title: `Шепот Старой Библиотеки`.
- Added world-profile handoff from text to visuals: `fairyteller_text` now persists `worldTitle`, `worldEnv`, `worldMotifs`, `worldConflict`, and `worldVisual` into `text.bible`; `fairyteller_visuals` injects those fields into the chapter-1 image prompt as `World direction` while keeping the selected illustration style as the hard rendering/material constraint.
- Verified world/style smoke with `cyberpunk_dream` + `minibrick`. Smoke job: `ft_1779521136379_fyau2h`; final status: `done`; `text.json` contains `worldVisual`; `visuals.json` image prompt contains both `World direction` and `brick-miniature`.
- Fixed frontend create submission fallback: `StoryConstructor` and `RomanticStoryForm` now default to `/webhook/fairyteller/create` instead of the legacy Make webhook when `VITE_FAIRYTELLER_CREATE_URL` is not injected at build time.
- Deployed frontend release `/var/www/fairyteller/releases/20260523-163709-codex-n8n-default-endpoint`; verified the production bundle no longer contains the old Fairyteller Make create webhook and public POST smoke returns `202`. Smoke job: `ft_1779525490110_d50hb6`.
- Hardened `fairyteller_text` malformed-JSON recovery again: `Normalize First Chapter` now extracts `textBlocks` with a tolerant string scanner from `"textBlocks": [` so it can recover when Gemini omits commas between array string elements.
- Verified tolerant parser smoke. Smoke job: `ft_1779526045123_62etdm`; final status: `done`; `text.json` contains 5 first-chapter blocks, `chapterPlan` length `4`, `worldTitle=Adventure Classic`, and `chapter-1.png`.
- Restored Make-style visual handling for references and style: frontend now offers `Фотореализм` as the default style; n8n uses the incoming `illustration_style_prompt` instead of short internal style summaries; original uploaded hero photos are read once in `fairyteller_intake` as private in-memory `photoRefs` and then attached directly to Gemini image requests in `fairyteller_visuals`.
- Removed raw n8n binary propagation across Fairyteller sub-workflows. This avoids `Failed to restore binary data ID` failures from temporary upload files while keeping base64 photo refs out of persisted public job artifacts (`order.json`, `text.json`, `visuals.json`, and `status.json`).
- Verified photorealistic photo-reference smoke through public intake. Smoke job: `ft_1779529334093_1q6il1`; final status: `done`; `order.json` has `hero_photo data0 image/png hasPhoto=true`; `hero-reference-sheet.png` and `chapter-1.png` were generated; `visuals.json` prompt contains `Use the attached original user photos` and photorealistic style instructions.
- Adjusted the reference architecture: original user photos are now used only by the portraitizer to create `hero-reference-sheet.png`; chapter images consume that generated reference sheet as the character canon and do not attach original photos directly. Verified smoke job: `ft_1779530348001_xveuec`; final status: `done`; `portraitSheet.photoRefCount=1`; final chapter prompt contains `generated character reference sheet` and does not contain `attached original user photos`.
- Hardened `fairyteller_text` against another Gemini malformed JSON variant: `Normalize First Chapter` now recovers from truncated/unterminated strings by splitting the raw response into five chapter blocks when JSON repair fails, and the first-chapter request allows `maxOutputTokens=9000`. Verified text smoke job: `ft_1779533953410_nxc57k`; `artifacts/text.json` exists with 5 text blocks and title `Шепот Старых Книг`.
- Fixed a syntax regression in `fairyteller_text` `Normalize First Chapter`: newline cleanup now uses `replaceAll` instead of a regex literal that could be serialized into an invalid multi-line regular expression in n8n. Verified smoke job: `ft_1779534789892_nmu16b`; `artifacts/text.json` exists with 5 text blocks and title `Первый Снег и Старый Компас`.
- Hardened `fairyteller_visuals` portraitizer: `Normalize Hero Reference Sheet` no longer fails the whole job when Gemini returns no inline image. It records the portrait sheet as `failed`, writes a tiny technical placeholder file for the file-write step, and `Build Chapter 1 Image Prompt` only attaches a reference sheet when `portraitSheet.status === 'ready'`; otherwise chapter image generation continues from text hero descriptions. Verified smoke job: `ft_1779535143483_lkbj1j`; final status `done`; `portraitSheet ready`; `chapter-1.png ready`.
- Tightened photo-reference behavior: when uploaded photos are present, portraitizer must produce a real `hero-reference-sheet.png`; it now uses a stronger mandatory image prompt, lower temperature, `TEXT+IMAGE` response modalities, and one in-node retry with the same photo refs before failing the job explicitly. This prevents silent delivery of unrelated chapter art when photo likeness is required. Verified smoke jobs: `ft_1779536054965_3hwkz0` and `ft_1779536230568_tyk2rp`; both finished `done` with `portraitSheet ready`, `photoRefCount=1`, and `chapter-1.png ready`.
- Added the visual-consistency contract to `fairyteller_visuals`: `Build Chapter 1 Image Prompt` now creates `visualBible`, injects it into the chapter-1 prompt, and `Normalize Chapter 1 Image` writes it into `visuals.json`. Queued future image slots (`cover`, `chapter_2`-`chapter_5`) now carry `referenceSheetUrl`, `visualBibleVersion`, and a consistency rule requiring the same portrait sheet and visual bible. Verified smoke job: `ft_1779538331952_8q4dt9`; `visuals.visualBible.referencePolicy=portrait_sheet_is_required_identity_canon`; all queued visual slots point to the same `hero-reference-sheet.png`.
- Added `fairyteller_full_text` as the background continuation workflow for chapters 2-5. It receives the first-chapter payload, asks Gemini for structured continuation chapters, combines them with chapter 1, writes `artifacts/full-text.json`, and patches `artifacts.fullText` without moving the public status backward. Gemini HTTP requests in text, full-text, and visuals now retry transient provider failures up to 3 times with a 10 second pause. Verified smoke job: `ft_1779539483364_1d0z2z`; final status `done`, `preview.imageStatus=ready`, `artifacts.fullText.status=ready`, and `full-text.json` contains 5 chapters with 5 text blocks each.
- Added the user-gated continuation path: `fairyteller_continue` is a new n8n webhook at `/webhook/fairyteller/continue`; it accepts `jobId`, fetches protected `text.json`, patches `artifacts.fullText.status=generating`, and starts `fairyteller_full_text`. The frontend success overlay now shows a "Хочу всю историю" button after the first chapter preview. Verified smoke job: `ft_1779540291674_2djg5i`; before continue, final public status had `preview.imageStatus=ready` and no `artifacts.fullText`; after POST `/webhook/fairyteller/continue`, `artifacts.fullText` moved `generating -> ready` and `full-text.json` contained 5 chapters with 5 blocks each.
- Hardened chapter-1 image normalization in `fairyteller_visuals`: if Gemini returns a successful response without inline image data, `Normalize Chapter 1 Image` now retries once with a simpler image-only prompt and the same generated reference sheet before failing the job with a clear `imageStatus=failed` message. Verified smoke job: `ft_1779540991824_gjmvrd`; final status `done`, `preview.imageStatus=ready`, `chapter-1.png` written.
- Hardened `fairyteller_full_text` malformed JSON parsing for continuation runs. A user-triggered continue request on `ft_1779540714365_uihv8x` had reached `artifacts.fullText.status=generating` but failed when Gemini omitted separators inside the structured chapter JSON; `Normalize Full Text` now falls back to loose chapter/block extraction. Re-ran `/webhook/fairyteller/continue` for that job and verified `artifacts.fullText.status=ready`; `full-text.json` contains 5 chapters with 5 blocks each.
- Shifted the continuation UX from email delivery to in-browser reading for the current preview stage. `full-text.json` is now publicly readable by unguessable `jobId`, and the success overlay fetches it when `artifacts.fullText.status=ready`, then shows chapters 2-5 one at a time with chapter switch buttons. Deployed frontend release `/var/www/fairyteller/releases/20260523-211058-codex-full-story-tabs`; verified public `full-text.json` read for `ft_1779540714365_uihv8x` returns 5 chapters.
- Unarchived and activated `fairyteller_full_text` (`C2Pcin7lctSY5nc2`) so it is visible in the n8n UI and available at `/workflow/C2Pcin7lctSY5nc2`.
- Hardened `fairyteller_full_text` `Normalize Full Text` against Gemini returning more than five text blocks for a chapter: the normalizer now coerces continuation text to exactly five layout blocks before validation. Re-ran `/webhook/fairyteller/continue` for `ft_1779542137791_hnqw7t`; verified `artifacts.fullText.status=ready`, `full-text.json` contains chapters `[1,2,3,4,5]`, and every chapter has 5 text blocks.
- Added `fairyteller_full_visuals` (`FVFullVisuals20260523`) and wired `fairyteller_full_text` to start it asynchronously after `full-text.json` is ready. The workflow fetches `visuals.json`, reuses `visualBible` and `hero-reference-sheet.png`, generates `chapter-2.png` through `chapter-5.png`, writes them as Job API files, updates `visuals.json`, and exposes public status in `artifacts.fullVisuals`.
- Added authenticated Job API base64 reads for generated files so n8n can reuse `hero-reference-sheet.png` as a stable inline reference without depending on n8n binary download behavior.
- Updated the frontend full-story reader to show chapter illustrations when `artifacts.fullVisuals.images` becomes ready. Deployed frontend release `/var/www/fairyteller/releases/20260523-220014-codex-full-visuals-code`.
- Verified full-visual smoke on `ft_1779542137791_hnqw7t`: `artifacts.fullVisuals.status=ready`, four generated files exist (`chapter-2.png` through `chapter-5.png`, each about 2.6-2.7 MB), and `visuals.json` now has ready image jobs for chapters 1-5 with cover still queued.
- Added `fairyteller_cover` (`FVCover20260523`) and wired `fairyteller_full_visuals` to start it asynchronously after chapter 2-5 images are ready. It uses `full-text.json`, `visuals.json`, `visualBible`, and `hero-reference-sheet.png` to generate a no-text cover-art background and stores it as `cover-spread.png`.
- Verified cover smoke on `ft_1779542137791_hnqw7t`: `artifacts.cover.status=ready`, `cover-spread.png` exists, `visuals.json` has ready image jobs for chapters 1-5 plus cover. Gemini returned the cover source as `1024 x 1024`; the print-accurate page-1 spread (`268.5 x 136 mm`) must be composed in the HTML/CSS PDF render layer rather than treating this raw image as the final cover spread.
- Added the first working PDF renderer at `server/fairyteller-render-pdf.mjs`, deployed it to `/opt/fairyteller-render/fairyteller-render-pdf.mjs`, and exposed authenticated `POST /api/fairyteller/jobs/:jobId/render-pdf`.
- Wired `fairyteller_cover` to invoke the PDF render endpoint after `cover-spread.png` is ready. The render step writes `cover.pdf`, `interior.pdf`, and `artifacts/render.json`, then exposes `artifacts.render`, `artifacts.coverPdf`, and `artifacts.interiorPdf` in public job status.
- Verified PDF smoke on `ft_1779542137791_hnqw7t`: `cover.pdf` is PDF 1.7 with 1 page at `268.5 x 136.0 mm`; `interior.pdf` is PDF 1.7 with 40 pages at `136.0 x 136.0 mm`; both download publicly through Job API file URLs. Deployed frontend release `/var/www/fairyteller/releases/20260523-231419-codex-pdf-links` to show PDF links when `artifacts.render.status=ready`.
- Fixed PDF text truncation in the renderer: story text pages now fit each full text block by reducing font size within bounds and fail render preflight instead of silently cutting lines. Added `render.preflight.noTextTruncation=true`.
- Added the provided Google Slides PDF exports as renderer master backgrounds and deployed them to `/opt/fairyteller-render/templates`. Interior pages now use the book paper texture master, and cover render uses the romantic cover master with generated cover art placed into the `COVER_IMG` area. Re-rendered `ft_1779550092673_8ltt9c`; `cover.pdf` and `interior.pdf` keep the required page sizes/page counts and no longer silently drop overflowing story text.
- Added `server/render-layouts/fairyteller-template-v1.json` and deployed it to `/opt/fairyteller-render/render-layouts/fairyteller-template-v1.json`. The renderer now reads explicit boxes/colors/page-plan from this layout contract, writes `render.layoutVersion=fairyteller-template-v1`, and enforces the expected five text blocks per chapter before composing PDFs. Re-rendered `ft_1779550092673_8ltt9c`; render stayed ready with cover page count `1`, interior page count `40`, and `render.preflight.expectedTextBlocksPerChapter=5`.
- Added combined print PDF output. The renderer now writes `book.pdf` by joining the generated cover and interior PDFs into a single 41-page mixed-size PDF, while keeping `cover.pdf` and `interior.pdf` for debugging. The Job API exposes `artifacts.bookPdf`, and the frontend shows a primary `Печатный PDF` link. Verified on `ft_1779550092673_8ltt9c`: `book.pdf` has 41 pages, first page `268.5 x 136.0 mm`, page 2 and last page `136.0 x 136.0 mm`.
- Replaced the PDF-template approximation with a PPTX-extracted renderer contract (`fairyteller-pptx-v2`). The cover now uses the clean PPTX background, places cover art only inside the white frame, draws title/subtitle/summary without extra colored backing, and keeps the existing `by FairyTeller` mark instead of duplicating it. Interior pages use the PPTX book assets and deterministic page sizing. Deployed renderer/assets/fonts/layout to `/opt/fairyteller-render`, re-rendered `ft_1779550092673_8ltt9c`, and verified `book.pdf` remains 41 pages with `render.preflight.noTextTruncation=true`.
- Added paragraph-aware typography to interior text pages. The renderer preserves explicit paragraph breaks when they exist, infers visual paragraphs from sentence groups when n8n returns flat text blocks, draws first-line indents, and distributes spare page height into bounded paragraph spacing for short text pages. Re-rendered `ft_1779550092673_8ltt9c`; `book.pdf` remained 41 pages with `render.preflight.noTextTruncation=true`.
- Added fairy-tale drop caps to the first text page of each chapter. The renderer removes the opening letter from the first paragraph, redraws it as a large embedded red serif letter, and shapes the first lines of text around it. Re-rendered `ft_1779550092673_8ltt9c`; `book.pdf` remained 41 pages with `render.preflight.noTextTruncation=true`.
- Updated the early interior pages and web preview UX. Interior page 4 now combines the subtitle and short book summary; page 5 is a blank dedication page with `Посвящается...` and handwriting lines. The `/create` success overlay now uses a book-style reader: it opens from a cover, shows chapter opener/text pages as spreads, keeps the "Хочу всю историю" action inside the reader, and links to the final print PDF when render is ready. Deployed frontend release `/var/www/fairyteller/releases/20260524-144653-codex-book-reader-preview`; re-rendered `ft_1779550092673_8ltt9c` with `render.preflight.noTextTruncation=true`.
- Changed the interior PDF to the fixed 40-page rhythm: page 1 title/year, page 2 subtitle plus story summary, page 3 handwritten dedication page, page 4 table of contents, then each of the five chapters gets 7 pages exactly (`chapter title`, `full-page illustration`, `5 text pages`). Page 40 is the QR/coauthor page. Chapter starts are now `5, 12, 19, 26, 33`; first text pages with red drop caps are `7, 14, 21, 28, 35`. Updated `server/render-layouts/fairyteller-pptx-v2.json`, deployed renderer/layout to `/opt/fairyteller-render`, re-rendered `ft_1779550092673_8ltt9c`, and verified `book.pdf` is 41 pages, `interior.pdf` is 40 pages, and `render.preflight.noTextTruncation=true`.
- Updated the web book reader to mirror the print rhythm: each chapter now opens as a spread with a title/teaser page on the left and a full-page illustration on the right, followed by text pages. Deployed frontend release `/var/www/fairyteller/releases/20260524-154330-codex-40-page-book-plan`; browser smoke on `https://fairyteller.ru/create?codexSmoke=40page` loaded the fresh `index-Cydf7pxL.css` asset and showed no console errors.
- Reworked the interior PDF rhythm again for true physical opener spreads: removed the early dedication page, moved the table of contents to page 3, and set chapter opener spreads to pages `4/5`, `10/11`, `16/17`, `24/25`, and `32/33`. The upstream n8n text contracts now generate and normalize chapter text blocks as `[4, 4, 6, 6, 5]`; chapter image prompts ask Gemini to keep key faces/hands/details away from gutter/trim zones. Deployed the four affected workflows (`fairyteller_text`, `fairyteller_full_text`, `fairyteller_visuals`, `fairyteller_full_visuals`) plus renderer/layout to production; backup before workflow import: `/root/fairyteller-n8n-backups/20260524-172625-before-spread-openers`. Smoke render job copy `ft_codex_spread_smoke_20260524` produced `book.pdf` with 41 pages, `interior.pdf` with 40 pages, `render.preflight.expectedTextBlocksByChapter=[4,4,6,6,5]`, `render.preflight.chapterStartPages=[4,10,16,24,32]`, and `render.preflight.noTextTruncation=true`.
- Hardened the full-visuals/render tail after a production test job (`ft_1779609291175_25otqb`) exposed two failures: Gemini returned transient `503` during chapter 2-5 image generation, and an overlong chapter opener teaser blocked PDF render. `fairyteller_full_visuals` now retries Gemini image requests up to 4 times with backoff, and the renderer clamps chapter opener teasers instead of failing on non-story teaser overflow. Deployed the workflow and renderer to production, re-ran `/webhook/fairyteller/continue` for the job, and verified public status: `fullText=ready`, `fullVisuals=ready`, `cover=ready`, `render=ready`, `bookPdf=/api/fairyteller/jobs/ft_1779609291175_25otqb/files/book.pdf`, `render.preflight.expectedTextBlocksByChapter=[4,4,6,6,5]`, and `render.preflight.noTextTruncation=true`.
- Polished the `/create` generated-book preview UX according to `docs/art-director-ux-designer.md`: removed the red cover placeholder and "open book" gate, replaced technical status/toast copy with book-facing language, made the reader appear directly as a responsive spread, constrained spread sizing to fit the viewport without internal scrollbars, and fixed the old `.mixer-chassis > *` CSS rule that was overriding the preview modal's fixed positioning.
- Deployed frontend release `/var/www/fairyteller/releases/20260524-141100-codex-preview-polish` and repointed `/var/www/fairyteller/current` to it. Production smoke on `https://fairyteller.ru/create?codex=f6badde` loaded `assets/index-Bq0YTsKH.css` and `assets/index-AGMzZNvb.js` with no browser console errors.
- Tightened n8n image-generation targets in `fairyteller_visuals`, `fairyteller_full_visuals`, and `fairyteller_cover`. Interior chapter prompts now explicitly generate for the `136 x 136 mm` square page and write `targetPageSizeMm=[136,136]`, `targetAspectRatio="1:1"`, and `role="interior_full_page_right_spread"` into image metadata. Cover prompts now generate front-cover framed art at about `1.46:1` instead of a full wraparound spread. Production backup before import: `/root/fairyteller-n8n-backups/20260524-image-page-size`; n8n was restarted and all three workflows are active.
- Aligned the web book reader text pagination with the print PDF contract: print-ready chapter `textBlocks` now render as one preview page per block instead of being re-split by web character limits. The loose web splitter remains only for the early single-block first-chapter preview before the full print-ready text is available.
- Cleaned the generated-book preview surface: removed secondary cover/interior PDF debug links, removed footer helper copy and "create another story" from the modal, added a close button, added a print/payment CTA to `https://fairyteller.ru/print` when the final PDF is ready, and replaced the first-chapter waiting state with an animated book/progress treatment.

- Added `preview.pdf` as a customer/browser PDF artifact: the renderer splits the print cover spread into square front/back preview pages around the existing square interior, Job API exposes `artifacts.previewPdf`, and the web reader uses it for the PDF button before falling back to print `book.pdf`. Job API also stores submitted emails in `/data/fairyteller/leads.jsonl`, can send Telegram operations notifications, and can send customer completion email through server-side environment variables without committing secrets. The preview currently fixes browser geometry but still embeds print-weight assets, so the next speed pass should add a compressed/rasterized web preview.
- Telegram operations messages now include direct `preview.pdf` and print `book.pdf` links when available. Failures reported through normal job status updates notify Telegram, and PDF-render endpoint failures now also mark the job `failed` with the render error before returning the API error.
- Deployed the preview/ops update to production: frontend release `/var/www/fairyteller/releases/20260525-1126-codex-preview-pdf-ops`, API source `/opt/fairyteller-api/fairyteller-api.mjs`, renderer source `/opt/fairyteller-render/fairyteller-render-pdf.mjs`. Re-rendered smoke job `ft_1779609291175_25otqb`; public status exposes `artifacts.previewPdf`, `preview.pdf` has 42 square pages at `136 x 136 mm`, and `book.pdf` remains the 41-page print artifact. Customer mail is wired but intentionally records `mail_provider_not_configured` until a mail-provider API key/from-domain are configured.
- Replaced the final web reader surface with the real `preview.pdf` once PDF render is ready, and replaced the unstable full-text HTML spread during render with a clean waiting state. The early first-chapter preview still uses the lightweight HTML reader until the customer asks for the full book. The print CTA now reads `Купить печатную книгу`, opens `/print` in a new tab, and production frontend release `/var/www/fairyteller/releases/20260525-140237-codex-pdf-preview-reader` is live.
- Added a protected noindex service page at `/api/fairyteller/books` in the Job API. It lists generated PDF artifacts from `/data/fairyteller/jobs/*/files` (`preview.pdf`, `book.pdf`, `cover.pdf`, `interior.pdf`), requires the existing `FAIRYTELLER_API_TOKEN` through bearer/header or a POST login form that sets an HttpOnly cookie, sends `X-Robots-Tag: noindex, nofollow, noarchive`, and uses `Referrer-Policy: no-referrer`.
- Added optional passwordless access for the same PDF list through `/api/fairyteller/books/:secret`, where `:secret` is configured only on production as `FAIRYTELLER_ADMIN_BOOKS_SECRET` in `/etc/fairyteller/api.env`. This keeps the normal token login available while giving operators a bookmarkable no-token URL.
- Removed the intermediate web book preview from the default `/create` UX. After submit, the user now sees one staged full-generation overlay with an approximate timer; `fairyteller_text` automatically starts `fairyteller_full_text` alongside first visuals, and the UI links the final print `book.pdf` when render completes. Strengthened text density prompts to target one print-ready block per page (`780-1050` chars for chapter 1, `820-1050` chars for chapters 2-5), strengthened image prompts to forbid rendered typography/white margins, and moved the decorative `Конец` label up/right on page 39.
- Changed the `/api/fairyteller/books` login form to use a separate operator password from `FAIRYTELLER_ADMIN_BOOKS_PASSWORD` instead of asking for the API token. Bearer/header API-token access remains available for technical checks, and the password value must stay only in production environment config.
- Added inferred hero age groups to the visual pipeline. `fairyteller_intake` now stores `ageGroup`/`ageGroupSource` for each hero from explicit future fields or from free-form descriptions, and `fairyteller_visuals`, `fairyteller_full_visuals`, and `fairyteller_cover` pass those labels into the portraitizer, visual bible, chapter-image prompts, and cover prompt so secondary heroes keep stable adult/child/teen/senior proportions.
- Replaced the single critical portrait-sheet identity path with `per_hero_reference_cards_v1`. `fairyteller_visuals` now generates one reference card per hero from the original photo and hero metadata, stores those cards as Job API files, and writes them into `visuals.visualBible.heroReferenceCards`; chapter 1, chapter 2-5, and cover generation attach those cards inline as the primary identity canon. The combined `hero-reference-sheet.png` is now optional fallback/overview material. Deployed `fairyteller_visuals`, `fairyteller_full_visuals`, and `fairyteller_cover` to production, restarted Docker n8n, verified all three are active, and kept backup `/root/fairyteller-n8n-backups/20260525-155221-hero-cards`.
- Restored the ready-state PDF preview in `/create` without reintroducing the unstable HTML book spread: once render is ready, the modal hides the status/title header, opens `preview.pdf` in a larger frame, moves the print CTA to the bottom center as `Оплатить печатную версию`, and shows lightweight print/delivery popups. Deployed frontend release `/var/www/fairyteller/releases/20260525-163439-codex-pdf-preview-cta`; nginx root now points to that release.
- Hardened `fairyteller_text` after production job `ft_1779729265248_8he7z7` hit Gemini `503 high demand` during "Пишем первую главу" and left the public job stuck at `text_generating`. The Gemini request node now performs longer controlled retries and marks the job `failed` on exhausted provider errors; `Normalize First Chapter` now also marks the job `failed` on unrecoverable response-shape errors. Deployed to production n8n, restarted `baku-n8n-docker`, verified workflow `kCtpw2d7pEOI3QRF` active, and kept backup `/root/fairyteller-n8n-backups/20260526-014428`. The stuck job was manually patched to `failed` with a user-safe Gemini-overload message.
- Added explicit hero age-group selection to `/create` (`Ребенок`, `Подросток`, `Взрослый`) and sends `heroN_age_group` to the n8n intake for every filled hero. Hardened visual reference generation after production jobs `ft_1779761155173_uq2c9l` and `ft_1779763092303_h1rz3w`: individual hero-card failures from Gemini no longer immediately collapse the whole visual run; they become `source_photo_fallback`, the private source photo is used only to build the combined reference sheet, and chapter/cover workflows attach both ready cards and the combined sheet whenever the card set is partial.
- Simplified the final `/create` PDF preview: removed the secondary `PDF` button and the right-side print/delivery tip bubbles, kept only the `preview.pdf` frame plus the centered `Оплатить печатную версию` CTA, and added an animated book-page loader while the embedded PDF viewer initializes.
- Hardened the full-visuals/render tail after production jobs reached `85%` with `Gemini did not return an inline image ... IMAGE_OTHER`. `fairyteller_visuals` no longer starts the legacy `fairyteller_render_publish` placeholder after chapter 1, `fairyteller_full_visuals` can fall back to an existing ready chapter image for a single failed later-chapter image instead of aborting the book, and the Job API now infers a public `failed` state from failed critical artifacts when no real PDF artifact exists. Re-ran `/webhook/fairyteller/continue` for stuck job `ft_1779811711746_r1rkcb` and verified it completed with `preview.pdf` and `book.pdf`.
- Stabilized Gemini image generation after job `ft_1779864011360_afn08q` showed repeated chapter art from fallback reuse. Chapter 1, chapter 2-5, and cover image workflows now sanitize risky story wording, treat generated hero cards as privacy-safe stylized identity guidance, and add a final no-reference environment/scene retry before falling back to an existing image. The full-visuals fallback now rotates among existing non-fallback images when possible instead of repeatedly choosing the same source. Deployed to production n8n; backup before import: `/tmp/fairyteller-image-stability-20260527-173803/backups`.
- Current production frontend baseline is `/var/www/fairyteller/releases/20260527-0909-codex-create-status-center`, with `/var/www/fairyteller/current` repointed to it. This release locks in the boutique Fairyteller redesign on `/` and `/create`: real book-object header imagery, the three-step constructor bound to the Job API, age-group fields for heroes, updated genres/styles, centered generation status, no legacy Lovable fallback, and the examples carousel/photos. It also includes `/podarok/dlya-pary`, route-specific title/description/canonical/OG/Twitter/JSON-LD output, generated static SEO HTML for key routes, updated `sitemap.xml`, and Yandex/OAI-friendly `robots.txt`. Production smoke on May 27, 2026 verified `/create` serves the new constructor without the removed "Компас над старым городом" preview block, Yandex.Metrika `109116448` is present, `/podarok/dlya-pary` returns pair landing metadata/JSON-LD, and `sitemap.xml` includes `/create`, `/podarok/dlya-pary`, and blog routes.
- Split Telegram bot roles in the Job API on May 27, 2026. Website support chat now uses the chat-specific `FAIRYTELLER_CHAT_TELEGRAM_*` env group and production polling, while generation progress/failure alerts use `FAIRYTELLER_ALERT_TELEGRAM_*`. Production was updated from the saved pre-switch env backup so support chat remains on the support bot and generation alerts return to the operations bot; `/healthz` verified after restart.
- Deployed frontend release `/var/www/fairyteller/releases/20260527-1547-codex-couple-inline-constructor-light-seo` for `/podarok/dlya-pary`. The page now embeds the book constructor as the third screen, defaults the couple flow to two adult heroes, locks the submitted `world` to the romantic story profile, removes the visible world picker, and keeps only a compact light romantic-format note. Production smoke verified both `/podarok/dlya-pary` and `/podarok/dlya-pary/` return the pair page title/canonical and the static SEO HTML exists in the active release.
- Deployed frontend release `/var/www/fairyteller/releases/20260527-1620-codex-couple-footer-hero-sync` for `/podarok/dlya-pary`. The couple landing page footer now matches the main page footer exactly: logo block, shared Fairyteller description, anchor navigation, VK/Dzen links, create CTA, and copyright line. Production smoke verified the active release, pair static SEO HTML, pair title/canonical, and the active `CoupleGiftLanding` asset contains the shared footer copy while the previous final CTA block is absent.
- Deployed frontend release `/var/www/fairyteller/releases/20260527-1640-codex-clickable-gift-tiles` for the homepage gift-idea grid. The six "Повод начинается с человека" tiles are now full-card links with arrow affordances; "Любимому человеку" and "На годовщину" point to `/podarok/dlya-pary`, while the other occasion tiles point to `/create`. Production smoke verified the homepage asset contains the new pair landing link.
- Renamed the romantic world key cleanup on May 27, 2026: frontend constructors now submit `romantic_story` as the canonical world key, while n8n keeps `disney_light` as a legacy alias for existing jobs/bookmarks. `fairyteller_text` and `fairyteller_full_text` now carry a stronger romcom-style relationship arc: warm collision or existing-couple tension, forced joint task, funny complications, almost-confession, non-cruel misunderstanding, vulnerability choice, and a small final gesture/new shared ritual. Production n8n backup: `/root/fairyteller-n8n-backups/20260527-081232Z-romantic-story-before`; after-export: `/root/fairyteller-n8n-exports/20260527-081506Z-romantic-story-after`. Deployed frontend release `/var/www/fairyteller/releases/20260527-1622-codex-romantic-story-key-clean`; active assets contain `romantic_story` and no `disney_light`.
- Deployed frontend release `/var/www/fairyteller/releases/20260527-1705-codex-empty-root-clickable-tiles` after the old visible SEO/no-JS fallback reappeared inside `#root`. Production HTML for `/`, `/create`, `/podarok/dlya-pary`, `/romantic`, `/8march`, and blog static pages now keeps `#root` empty while retaining route-specific title, description, canonical, OG/Twitter tags, and JSON-LD in `<head>`. The source `index.html` and SEO page generator were updated so future builds do not reintroduce visible fallback content behind the React app. This release also keeps the homepage gift tiles as cache-busted full-card links: "Любимому человеку" and "На годовщину" route to `/podarok/dlya-pary`, while the other tiles route to `/create`.
- Deployed cache-bust frontend release `/var/www/fairyteller/releases/20260527-1715-codex-cachebust-clickable-tiles` after browser testing showed some sessions still using the old non-clickable homepage occasion-grid chunk. The homepage now loads fresh `index-_nRWBKKt.js` and `DesignTest-7OGxZveJ.js`; production browser smoke verified the six "Повод начинается с человека" tiles are real links, and clicking "Любимому человеку" navigates to `/podarok/dlya-pary`.
- Deployed frontend release `/var/www/fairyteller/releases/20260527-1740-codex-anniversary-landing` for the second SEO gift cluster page: `/podarok/na-godovshchinu`. The page targets "подарок на годовщину" with route-specific title/description/canonical/JSON-LD, static SEO HTML, sitemap entry, and an embedded inline constructor that limits the story choice to three plots: `adventure_classic`, `fantasy_epic`, and `romantic_story`. The homepage "На годовщину" occasion tile now routes to `/podarok/na-godovshchinu`. Production smoke verified 200 HTML, empty `#root` fallback, sitemap inclusion, and browser-rendered constructor choices without the extra Hogwarts/cyberpunk worlds.
- Tuned `fantasy_epic` text dramaturgy/ToV in production n8n on May 27, 2026. The plot structure remains epic, but `fairyteller_text` now frames fantasy through concrete roads, cities, rules of magic, artifact price, practical tasks, and clear choices instead of pushing constant fate/light/dark/heart metaphors. `fairyteller_full_text` now carries the same continuation rule for chapters 2-5: each chapter advances through a clear task, obstacle, magic rule or artifact cost, and a concrete hero choice. Backup: `/root/fairyteller-n8n-backups/20260527-092809Z-fantasy-tov-before`; after-export: `/root/fairyteller-n8n-exports/20260527-0933-fantasy-tov-after`. Verified `baku-n8n-docker` health and active workflows `fairyteller_text`/`fairyteller_full_text`.
- Deployed frontend release `/var/www/fairyteller/releases/20260527-2159-codex-couple-photo-cleanup` for `/podarok/dlya-pary`. The pair landing page keeps the existing color system but replaces the hero collage with a consistent `snow-route` product-photo set and moves the illustrated open-spread proof into the examples block. Production smoke verified the active release symlink, 200 response, static pair SEO HTML, and rendered image URLs for `snow-route-hands`, `snow-route-flatlay`, `snow-route-shelf`, `snow-route-table`, and `love-city-open`.
- Deployed stability/UI release `/var/www/fairyteller/releases/20260527-2305-codex-payment-timer-visuals-fallback`. The `/create` constructor and embedded landing constructors now show elapsed/estimated remaining generation time based on a 4-minute production baseline, and the ready state places `ОПЛАТИТЬ И ОТПРАВИТЬ В ПЕЧАТЬ` next to `Открыть превью`. Production API now treats `heroReferences` failure as non-fatal because portrait references are degradable, while `fairyteller_visuals` no longer patches/throws a job failure when the combined hero reference sheet fails after partial per-hero cards; it writes a skipped portrait sheet and continues toward `visuals.json`. Backups: n8n `/root/fairyteller-n8n-backups/20260527-visuals-no-fatal-fallback-before/fairyteller_visuals_before.json`, API `/opt/fairyteller-api/fairyteller-api.mjs.bak-20260527-visuals-no-fatal`. Smoke verified frontend asset `index-BQzIL3Cd.js`, active `fairyteller_visuals`, removed fatal reference message from exported workflow, and active `fairyteller-api.service`.
- Deployed frontend/API release `/var/www/fairyteller/releases/20260528-0118-codex-pay-link` on May 28, 2026 to switch the payment destination back to `/pay`. Generation-ready CTAs and customer email links now use `https://fairyteller.ru/pay?pdf=...`; `/print` remains mounted as a compatibility alias for older links, and payment-page view notifications use `/api/fairyteller/pay/payment-page-view`.
- Added full-book continuation hardening on May 28, 2026: `fairyteller_full_text` now claims a `fullText: generating` slot before calling Gemini and skips duplicate launches when the slot is already `generating` or `ready`; `fairyteller_visuals` also starts `fairyteller_full_text` as a fallback after chapter-1 visuals are ready, so a missed `fairyteller_text` fan-out cannot leave a job stuck at `visuals_ready`. The frontend ready-state CTA now also accepts final job `status=done` / `stage=complete` when a PDF URL exists, so the print-payment button appears even if the render artifact shape arrives through the top-level PDF fields.
- Added a branded customer completion email template on May 28, 2026 in the Job API. The email now follows the current black/white/peach/yellow Fairyteller site language with a compact peach header and no top black strip, uses the title "Ваша книга почти готова", keeps "Открыть книгу" as a quieter secondary CTA, highlights "Оплатить заказ" through `/pay?pdf=...`, includes Telegram/site contact links, plain-text fallback, and `Reply-To` through `FAIRYTELLER_MAIL_REPLY_TO`; it still sends only after real PDF render completion.
- Updated the post-payment email on June 12, 2026. It no longer renders the story inside the email; the customer receives a direct protected PDF link (`preview.pdf?access=...`, falling back to `book.pdf`/`/book/:jobId` only if needed), subject `Ваша история готова — спасибо за заказ`, support/refund copy, next-step delivery timing, and the existing four-photo product gallery. Production API was deployed and a smoke resend to `zoowall@yandex.ru` for `ft_1781192146034_f37ggn` returned `status=sent`.
- Updated the post-payment email delivery copy on July 1, 2026. The "Что дальше?" section now says delivery timing depends on the destination city; the HTML version links that phrase to `https://fairyteller.ru/delivery/`, and the plain-text version includes the same URL. Production API backup before deploy: `/opt/fairyteller-api/fairyteller-api.mjs.20260630-140634.bak`; a test resend for `ft_1781192146034_f37ggn` to `zoowall@yandex.ru` returned `status=sent`.
- Added a subtle book-length tracker to the `/book/:jobId` paywall preview on June 12, 2026. The Job API now returns `progress` from `/sample-pages` (`availablePages`, `totalPages`, `availableChapters`, `totalChapters`), and the preview page shows a sticky light progress pill above the rasterized PDF pages. Production release `/var/www/fairyteller/releases/20260612-0528-codex-book-progress-tracker` is active; smoke on `ft_1781192146034_f37ggn` returned `18/42` pages and `2/5` chapters.
- Moved the unpaid `/book/:jobId` paywall to the end-of-book preview on June 30, 2026. The Job API now rebuilds `paywall-preview.pdf` with cache version `paywall-preview-end-of-book-v1`, exposing all preview pages except the final page; existing cached samples are invalidated through `paywall-preview.meta.json`. The frontend places the paywall sentinel immediately before the last available rasterized page, so the lock appears on the apparent penultimate page instead of after chapter 2. Current production release is `/var/www/fairyteller/releases/20260630-0930-codex-paywall-copy-free-delivery`; smoke on `ft_1781192146034_f37ggn` returned `41/42` pages, `5/5` chapters, and the lock copy now says "Перед вами — почти готовая книга. Оплатите заказ, чтобы получить бумажную версию с бесплатной доставкой по РФ."
- Restored the static `/delivery/` page on June 30, 2026 after the `20260630-0930-codex-paywall-copy-free-delivery` release was built without the delivery directory and made the page return nginx 404. Active production release is now `/var/www/fairyteller/releases/20260630-1402-codex-restore-delivery-page`, based on the paywall-copy release with `/delivery/index.html` restored and `sitemap.xml` containing `https://fairyteller.ru/delivery/`. Smoke verified both `/delivery` and `/delivery/` return `200`.
- Deployed frontend release `/var/www/fairyteller/releases/20260630-1408-codex-faq-delivery-links-restore` on June 30, 2026 to restore FAQ links to `/delivery/` after the paywall-copy release was built from sources without those links. Homepage, `/create`, gift landing pages, and legacy promo FAQ accordions now render the delivery timing sentence with linked `здесь`; route FAQ JSON-LD includes the full `https://fairyteller.ru/delivery/` URL, and sitemap includes `/delivery/`. Browser smoke verified the link on desktop and mobile for `/`, `/create`, `/podarok/dlya-pary`, `/podarok/na-godovshchinu`, `/podarok/rebenku`, `/podarok/skazka-po-foto`, `/podarok/zhene`, `/podarok/mame`, `/romantic`, `/8march`, `/wizard`, and `/indian`.
- Deployed static SEO fallback release `/var/www/fairyteller/releases/20260701-0354-codex-seo-noscript-fallback` on July 1, 2026 after Yandex Webmaster's text version for `/podarok/skazka-po-foto` showed only title/description and almost no body text. `scripts/generate-seo-pages.mjs` now keeps the React root empty for JS users but inserts route fallback HTML into `<noscript data-fairyteller-seo-fallback>`. Blog route fallback pulls article markdown from `src/data/blogPosts.ts` through the TypeScript AST so Yandex sees real article text instead of only title/description/links. Production smoke verified `/podarok/skazka-po-foto` `200` with fallback and ~126 visible words, `/podarok/rebenku` ~158, `/blog` ~562, `/blog/personalnaya-kniga-na-zakaz` ~399, `/blog/imennaya-kniga-dlya-rebenka` ~300, and `/delivery/` still `200`.
- Deployed frontend release `/var/www/fairyteller/releases/20260701-0429-codex-metrika-goals` on July 1, 2026 after adding Yandex Metrica JavaScript goals for the Direct funnel: `ft_constructor_start`, `ft_generate_submit`, `ft_preview_ready`, `ft_checkout_start`, and `ft_payment_success`. The shared helper lives in `src/lib/metrika.ts`; purchase success also pushes an ecommerce purchase for one printed Fairyteller book with `revenue=3500` and `currencyCode=RUB`. Goals are fired from the landing constructor, legacy constructor, `/book/:jobId` paywall links, and `/pay` checkout/success return. Local TypeScript passed; local in-place Vite build stalled with 0% CPU, but a clean temp checkout built successfully and was deployed. Production smoke verified `/`, `/podarok/skazka-po-foto`, `/podarok/rebenku`, `/book/ft_smoke_paywall`, `/pay?jobId=ft_smoke_paywall`, and `/delivery/` return `200`, and the deployed `metrika` chunk contains all five goal identifiers.
- Deployed static favicon release `/var/www/fairyteller/releases/20260701-favicon-hat-codex` on July 1, 2026 from the active production release `/var/www/fairyteller/releases/20260701-metrika-goal-retry-codex`. Replaced the legacy Lovable favicon with a Fairyteller wizard-hat set: `favicon.ico` with 16/32/48/64/128/256 PNG entries, `favicon.svg`, real 16/32 PNGs, `apple-touch-icon.png` 180x180, and `android-chrome` 192/512 manifest icons. Patched all generated/static HTML favicon tags in the copied release and verified public hashes for `/favicon.ico`, `/favicon.svg`, `/site.webmanifest`, and `/android-chrome-192x192.png`.
- Added a protected email database view on May 28, 2026. The Job API still appends every valid submitted address to `/data/fairyteller/leads.jsonl` during job creation, and now exposes a deduplicated operator view at `/api/fairyteller/books/leads` plus CSV export at `/api/fairyteller/books/leads.csv` behind the existing books-admin auth.
- Added a compact four-image product example strip to the customer completion email on May 28, 2026. Optimized public assets now live under `/images/email/`, and the Job API inserts them immediately above the final black support/footer block.
- Added a protected manual email form on June 10, 2026 at `/api/fairyteller/books/mail`. Operators can send one-off branded emails through the configured Resend sender (`FAIRYTELLER_MAIL_FROM`, currently `books@fairyteller.ru`), with optional CTA button and a local audit log in `/data/fairyteller/mail-sends.jsonl`; inbound mail for `books@fairyteller.ru` still requires separate MX/mailbox or forwarding setup.
- Fixed Yandex Webmaster soft-404 warning on May 31, 2026 by changing the active production nginx config in `/etc/nginx/sites-enabled/fairyteller` and syncing `/etc/nginx/sites-available/fairyteller`. Static/generated routes now resolve through `try_files $uri/index.html $uri $uri/ =404`, while explicit SPA-only routes (`/pay`, `/print`, `/indian`, `/blog-admin`, `/loveproportion`, `/book-preview`, `/naruto-story`, `/wizard`, `/design-test`) still serve `index.html`. Verified random unknown URLs return real `404 Not Found`; known public pages, `robots.txt`, and `sitemap.xml` return `200`. Backups: `/root/nginx-codex-backups/fairyteller.bak_codex_20260531115932_real_404` and `/root/nginx-codex-backups/fairyteller.sites-enabled.bak_codex_20260531120035_real_404`.
- Landing page standard fixed on May 31, 2026 from `/podarok/dlya-pary?safariHeroFix=202605280919`: black/white/peach/yellow editorial layout, the shared homepage-style top header from `LandingHeader` (black promo strip, mobile menu icon, logo, `Как это работает / Создать / Примеры / FAQ / Журнал`, and `Заказать` CTA), shared footer, hero text plus optimized WebP product photos, embedded constructor above the intent blocks, real book-photo examples lower on the page, route-specific SEO metadata/JSON-LD/static SEO HTML, empty `#root`, sitemap entry, and internal links from relevant blog/content surfaces. New landings should keep the physical printed book as the visible differentiator and avoid hidden/visible SEO-only copy blocks.
- Deployed frontend release `/var/www/fairyteller/releases/20260531-2333-codex-couple-hero-slider` on May 31, 2026. `/podarok/dlya-pary` now uses a homepage-style autoplay hero image area with the three "Город влюбленных сердец" photos instead of a three-tile collage, and its lower examples use the `kogda-chudo-zhdet` Marocco photos. Added `/podarok/skazka-po-foto` with route metadata, JSON-LD, sitemap entry, print-book-focused copy, real book photos from `list-nad-drevogradom` and `zagadka-ovsyannikovskogo-leta`, embedded constructor, and targeted CTA buttons in `/blog/sozdat-skazku-po-foto`. Production smoke verified 200 responses, empty `#root`, sitemap inclusion, and the active pair landing chunk contains the autoplay hero and new assets.
- Deployed frontend release `/var/www/fairyteller/releases/20260531-2344-codex-home-example-carousel` on May 31, 2026. The homepage "Одна и та же история может быть очень разной" carousel now uses the optimized WebP set from `~/Desktop/Для примеров`, loops through three repeated cycles, scrolls via `requestAnimationFrame` for slower smoother movement, pauses while hovered, and keeps pointer-drag/manual horizontal scrolling.
- Deployed frontend release `/var/www/fairyteller/releases/20260601-0007-codex-child-tile-photo-landing` on June 1, 2026. `/podarok/skazka-po-foto` now follows the shared landing template more closely: homepage-style autoplay hero photo area, constructor moved up before proof/examples, and a lower examples block matching `/podarok/dlya-pary` with detail tiles on the left and three real book photos on the right. The homepage "Ребенку" occasion tile now links to `/podarok/skazka-po-foto`.
- Deployed frontend release `/var/www/fairyteller/releases/20260601-0015-codex-photo-landing-seo-template` on June 1, 2026. `/podarok/skazka-po-foto` now mirrors the `/podarok/dlya-pary` landing rhythm more closely: hero, meaning block, embedded constructor, occasion tiles, examples/details, process, FAQ, and shared footer. Visible copy and generated static SEO HTML now target the Yandex Webmaster cluster around "персональная сказка", "создать сказку по фото", "сказка где ваш ребенок главный герой", "книга на заказ где ты главный герой", and "создать персонализированную сказку или мультфильм" without reintroducing visible SEO-only fallback content.
- Deployed frontend release `/var/www/fairyteller/releases/20260601-0022-codex-photo-worlds-sitemap` on June 1, 2026. The `/podarok/skazka-po-foto` embedded constructor now exposes every available story world except the romantic story: adventure, Hogwarts-style world, fantasy, and Russian cyberpunk. The sitemap entry for `/podarok/skazka-po-foto` was refreshed to `lastmod` 2026-06-01.
- Deployed frontend release `/var/www/fairyteller/releases/20260601-0038-codex-photo-hero-mechanic` on June 1, 2026. The `/podarok/skazka-po-foto` hero slideshow now uses the three "Пустынный механик и забытый голос" brick-world product photos (cover, open spread, and back cover), optimized as JPEG assets for the production build.
- Deployed frontend release `/var/www/fairyteller/releases/20260601-0448-codex-anniversary-hero-zvezdopad` on June 1, 2026. The `/podarok/na-godovshchinu` hero now matches the shared landing-page standard: one full-bleed autoplay photo area with manual dot controls, using the three "Звездопад над Волгой" product photos (open spread, back cover, and cover) optimized as JPEG assets.
- Deployed sitemap-only release `/var/www/fairyteller/releases/20260601-0510-codex-anniversary-sitemap` on June 1, 2026. The `/podarok/na-godovshchinu` sitemap `lastmod` was refreshed to `2026-06-01` after the hero photo update.
- Deployed frontend release `/var/www/fairyteller/releases/20260602-0125-codex-child-gift-landing` on June 2, 2026. Added `/podarok/rebenku` for the broader "подарок ребенку" intent. The page follows the landing standard: full-photo autoplay hero using the "Тайны заколдованной карты" product photos, embedded child-focused constructor with non-romantic worlds, comparison against ordinary gifts, lower examples using "Лист над Древоградом" photos, route-specific SEO metadata/static HTML, sitemap entry, and the homepage "Ребенку" tile pointing to the new URL.
- Updated `/podarok/rebenku` SEO targeting on June 2, 2026 from the broad "подарок ребенку" wording to the "сказка с именем ребёнка / именная книга для ребёнка" cluster. The change is copy and metadata only: canonical remains `/podarok/rebenku`, layout and landing structure are unchanged.
- Added `/podarok/zhene` on June 5, 2026 for the "подарок жене на день рождения от мужа" cluster. The page follows the shared landing standard from `/podarok/dlya-pary`: shared header/footer, full-photo autoplay hero, embedded constructor, print-book differentiation, example photos, route-specific title/description/canonical/Product JSON-LD, sitemap entry, and generated static HTML with an empty `#root` to avoid visible SEO fallback screens.
- Added `/podarok/mame` on June 10, 2026 for the "подарок маме на день рождения" cluster. The page follows the shared landing standard from `/podarok/dlya-pary`: shared header/footer, full-photo autoplay hero, embedded constructor, print-book differentiation, example photos, route-specific title/description/canonical/Product JSON-LD, sitemap entry, and generated static HTML with an empty `#root`.
- Tuned `cyberpunk_dream` and PDF dialogue typography on June 2, 2026. n8n now frames Russian cyberpunk through concrete city tech, routes, cameras, old vehicles, data glitches, technical obstacles, and clear human choices, with explicit instructions to reduce epithets/metaphors/neon/cold-system abstraction. Text prompts now require dialogue dashes as `— ` with a following space and forbid lone closing dashes at block ends. The renderer normalizes generated dialogue dashes, removes standalone trailing dashes, infers paragraph breaks around dialogue turns, and disables drop caps when a text page starts with dialogue. Production source-of-truth snapshot: `/root/fairyteller-n8n-exports/20260602-0029Z-cyberpunk-pdf-before`; n8n backup/import: `/root/fairyteller-n8n-backups/20260602-0038Z-cyberpunk-pdf-before` and `/root/fairyteller-n8n-imports/20260602-0038-cyberpunk-pdf`; n8n after-export: `/root/fairyteller-n8n-exports/20260602-0041Z-cyberpunk-pdf-after`; renderer backup: `/opt/fairyteller-render/backups/fairyteller-render-pdf.20260602-0038Z-cyberpunk-pdf-before.mjs`. Verified `baku-n8n-docker` health and active `fairyteller_text`/`fairyteller_full_text`.
- Added protected book-photo storage to the Fairyteller admin on July 2, 2026. `/api/fairyteller/books/storage` stores folders under `/data/fairyteller/book-photo-storage`, supports individual image uploads and browser folder uploads, preserves safe relative paths, creates tokenized noindex public share galleries at `/api/fairyteller/books/storage/share/:folderId/:token`, and lets operators delete individual files or whole folders. Default limits are 50 MB per image and 512 MB per upload request, configurable through `FAIRYTELLER_ADMIN_STORAGE_FILE_MAX_BYTES` and `FAIRYTELLER_ADMIN_STORAGE_UPLOAD_MAX_BYTES`.
- Fixed the admin storage upload UX on July 2, 2026 after a real folder upload appeared to simply refresh the page. The form no longer asks for a manual folder title; new folders infer their title from the uploaded source folder path, and the upload JavaScript now renders non-redirect error responses instead of silently reloading. Production nginx `/api/fairyteller/` `client_max_body_size` was raised from `20M` to `512M` in both sites-enabled and sites-available; `nginx -t` passed, nginx was reloaded, and a 21 MB HTTPS storage upload smoke succeeded and was deleted.
- Fixed the Safari-specific admin storage submit bug on July 2, 2026: the hidden input named `action` shadowed `HTMLFormElement.action`, so Safari posted to `/api/fairyteller/books/[object HTMLInputElement]`. Storage upload JavaScript now reads the form target via `getAttribute('action')`.
